Authoring `.github/workflows/check.yml` (TK-005) required checking GitHub's actual hosted Windows runner image (via `actions/runner-images`' `Windows2025-Readme.md` on GitHub) rather than assuming: GNU Make is absent from the default software list, so a Windows CI job invoking the root `Makefile` needs an explicit `choco install make` step (Chocolatey is present on the image by default). Git 2.55+ is present, which bundles Git Bash — this makes `shell: bash` a valid, working step-level shell on `windows-latest`, letting the same Unix-shell-syntax `Makefile` (`&&`, `cd`, `rm -rf`) run unmodified on both `macos-latest` and `windows-latest` without a separate Windows-specific Makefile or shell rewrite.

This has not been confirmed by an actual GitHub Actions run — no commit has been pushed to trigger one (Claude's session cannot push; see `CLAUDE.md`'s git-administration prohibition). `actionlint` (with its bundled `shellcheck` pass) confirms the workflow is schema-valid and its embedded shell steps are clean, but that is static validation only, not proof the Windows leg's native build (Tracktion Engine + JUCE via CMake) actually succeeds — RSK-001 already flags that as the highest-risk, least-proven part of the toolchain.
