A passing `check (macos-latest)` run (`0847885`, TK-009's closing run) carries real compiler warnings worth knowing about before assuming a future one is new or Corytm's own. All of them, verified by grepping the actual CI log for warning-emitting file paths, originate in vendored `_deps/*-src` trees — none in `src/backend/audio/proof_schema.cpp`, `toolchain_proof.cpp`, or any other Corytm-owned file:

- `-Wnontrivial-memcall` (20 occurrences, the most frequent) — JUCE's vendored HarfBuzz, `_deps/juce-src/modules/juce_graphics/fonts/harfbuzz/hb-null.hh`.
- `-Wimplicit-const-int-float-conversion` (14 occurrences) — Tracktion Engine, three files under `_deps/tracktion_engine-src/modules/tracktion_engine/{audio_files,utilities}/`.
- `-Wdeprecated-declarations` (15 occurrences) — protobuf's own source calling its own deprecated APIs internally, six files under `_deps/protobuf-src/src/google/protobuf/` (`reflection_ops.cc`, `text_format.cc`, `util/field_comparator.cc`, `util/field_mask_util.cc`, `util/type_resolver_util.cc`, `wire_format.cc`).
- One CMake-level `CMP0141` policy deprecation warning, emitted by protobuf's own `CMakeLists.txt` setting that policy to `OLD` itself — not something Corytm's own CMake does.
- One `npm warn allow-scripts fsevents@2.3.3 (install: (install scripts present))` — informational, `fsevents` is a standard transitive macOS-only native dependency of the frontend's file-watching toolchain (Vite/Rollup), not a red flag.

None of these are actionable without patching or suppressing warnings in vendored third-party source (JUCE, Tracktion Engine, protobuf) — `CLAUDE.md` §8's "fix the cause, don't suppress" applies to Corytm-owned code; there is no Corytm-owned cause here to fix, and patching a pinned upstream dependency's source directly would create an ongoing merge burden against every future version bump for no corresponding benefit. If a future `make check` run's warning output looks different from the above (a genuinely new warning, a new file, a much higher count), that's the signal something changed — update this entry or promote it, don't just re-diagnose from scratch.
