# EP-011 — Real Desktop Project Persistence

Status: done
Milestone: MS-002

## Objective

Realize ADR-011's local JSON persistence format as real project create/open/save capability, closing MS-002's last unaddressed manual-editing thread: EP-010 proved one hardcoded move against an in-memory fixture project, not real project creation, opening, or persistence. Mirrors this Milestone's own established shape (EP-009 → EP-010: prove the mechanism, then build real UI on top of it) applied to persistence specifically.

## Scope / Out of scope

In scope, as distinguishable increments — not necessarily separate Features (see Notes):

1. Corytm Engine gains a real persistence module (`corytm.engine.persistence`) implementing ADR-011's versioned local JSON envelope: `save_project`/`load_project`.
2. The Desktop channel (ADR-010) carries real `CreateProjectCommand`/`SaveProjectCommand`/`OpenProjectCommand` commands and their result events, added to `project.proto`'s existing `Command`/`Event` oneof per ADR-011, backed by the persistence module and — for Open — the already-proven `MaterializeProjectCommand`/native-render path.
3. Tauri resolves a real filesystem path via a native OS file dialog (`@tauri-apps/plugin-dialog`) and a minimal Desktop UI lets a human create, save, and open a project through actual UI controls.

Out of scope (mirrors ADR-011's own explicit deferrals): any track/clip authoring UI beyond the existing move-clip fixture trigger; project-bundle/directory formats or external resource references; multiple concurrent projects or windows; autosave, crash recovery, or undo-history persistence; migration tooling or any backward-compatibility guarantee for Alpha-era files; cloud/sync persistence; Dorian/provider wiring; any visual/UX design investment beyond function.

## Acceptance criteria

A human can launch the real Desktop app, create a new project, save it to a real file through a native save dialog, and open a previously-saved file through a native open dialog — observing the reopened project's genuine re-render (sample count, peak amplitude) through the Native Audio Runtime — proven end to end by a combination of automated tests (per-increment, mirroring this Milestone's established practice) and, where real native-dialog/process behavior can't be exercised headlessly, a human confirmation step distinct from and in addition to automated evidence.

## Dependencies

EP-009 (done) — the Desktop channel this Epic extends. EP-010 (done) — the one existing real Desktop command (`MoveClipCommand`) this Epic's protocol change must not regress. ADR-011 (accepted) — the persistence format this Epic realizes.

## Notes

Selected as MS-002's next Epic after ADR-011 resolved the local-first persistence-strategy decision `docs/project/status.md` had left the Milestone's next step blocked on. Dorian-model/provider wiring remains separately blocked on an explicit, not-yet-made provider-selection decision (`docs/product/strategy.md` §10) and is not this Epic's concern. No further Protected Product Decision (`CLAUDE.md` §5) is implicated: ADR-011 already settled the storage mechanism itself; this Epic only builds the real commands/UI on top of it.

Feature/Task-level detail is refined now, at the point this Epic begins execution, per `docs/project/plan.md`'s Rolling-Wave Planning.

**Sizing note (2026-09-02):** mirroring EP-010's own precedent (backend command/event wiring proven first, native-dialog/UI built on top of it second), this Epic's scope splits into two Features: **FT-023** (this session) — the persistence module and the Desktop channel's new commands, proven by automated tests alone, no native file dialog or new UI — and a second, not-yet-created Feature for the native file dialog and its minimal UI, deferred to a future session. Unlike FT-022 (which added its UI trigger as a second Task within the same Feature), the backend half here is larger on its own merits (a new persistence module, three new command/event pairs, and — see FT-023's own Notes — restructuring the Desktop channel from a single command per connection to a multi-command session) and a new native OS dependency (`@tauri-apps/plugin-dialog`) is exactly the kind of genuinely new toolchain surface this project's own history (RSK-002, RSK-010) treats as worth isolating rather than bundling.

**FT-023 planned (2026-09-02):** created and decomposed into TK-025 (Engine persistence module) and TK-026 (Desktop channel commands/multi-command session). See `docs/project/features/FT-023.md`.

**FT-023 implementation complete, local validation passing (2026-09-02):** both Tasks done; a full clean `make check` passes end to end. This Epic stays `active`, pending FT-023's own delivery commit being pushed and a real, independently-verified passing Windows GitHub Actions run (`RSK-015`) before FT-023 reaches `done`. Once it does, this Epic's remaining scope — a native OS file dialog and minimal UI to trigger create/open/save — becomes the next Feature to refine and execute.

**FT-023 reconciled to `validating` (2026-09-02):** the delivery commit (`72a3141`) is confirmed pushed to `origin/main`, and a real Windows/macOS `check` Actions run (`33673521046`) is confirmed in progress against it. This Epic's remaining scope (native file dialog + minimal UI) is not started yet: it would call the exact `create_project`/`save_project`/`open_project` commands and the Rust-side `Event`-oneof decode that `RSK-015` names as the still-unconfirmed surface, so per `CLAUDE.md` §6's dependency-safety check, starting it now would depend on this pending evidence turning out correct — it waits for FT-023's real Windows confirmation rather than starting in parallel.

**Dependency-safety check corrected, FT-024 started (2026-09-02):** the block above was reviewed against its own first real use and judged too conservative — mere consumption of `RSK-015`'s named surface, not an actual invalidation risk to it, was what triggered the wait. `CLAUDE.md` §6 was amended (user-approved) so implementation dependency alone no longer blocks; only a plausible failure that would materially invalidate an assumption the new Feature relies on does. Re-applied here: FT-023-shaped failures, per this project's own risk history, are almost entirely internal/platform-specific fixes that preserve the already-locally-validated `Command`/`Event` contract this Epic's next Feature reuses (the same connect/send/receive pattern `RSK-014` already closed cross-platform for `move_clip`), so the corrected check does not block. **FT-024 (Native File Dialog and Create/Open/Save UI)** was created accordingly as this Epic's remaining scope, decomposed into TK-027/TK-028 — see `docs/project/features/FT-024.md`.

**FT-023 reconciled to `done` (2026-09-02):** real Windows CI (run `33673521046`, commit `72a3141`) independently confirmed passing, with `RSK-015`'s specific surface verified from the actual log text on both platforms — see FT-023's own Notes. This Epic's earlier real-Windows-run confirms the corrected dependency-safety check's own prediction: FT-023 resolved cleanly, with no failure of any kind, so FT-024 (already implemented in parallel by this point) needed no pause/reconciliation.

**FT-024's human confirmation: failed, root-caused, fixed, then passed (2026-09-02):** the first real attempt (New Project → Save → quit → relaunch → Open) failed on Open with `failed to read event: early eof` — not recorded as passed. Root cause traced to `corytm.runtime.session._connected_native_runtime` raising on `native_runtime`'s exit code alone, even when the accurate `Event` (a legitimately empty, zero-track render) had already been read — the first time anywhere in this project that a zero-track project reached the real materialize/render path. Fixed via TDD (two new tests reproducing the exact failure first); the user then repeated the confirmation and it passed, with real returned evidence matching the expected empty-project outcome exactly. See FT-024's own Notes for the full trace. This Epic's Acceptance criteria — a human creating, saving, and reopening a project through real native dialogs, observing genuine (if legitimately silent, for an empty project) re-render evidence — is now satisfied on real evidence, pending only FT-024's own delivery commit being pushed and a real Windows CI run confirming `RSK-016`.

**FT-024 reconciled to `validating` (2026-09-02):** a later session independently confirmed (not from the user's report alone) that the delivery commit (`f2099f2`) is already pushed to `origin/main` (local `HEAD` matched exactly via `git fetch`), and that a real `check` Actions run (`33681012671`) is genuinely `in_progress` against it on both platforms. This Epic stays `active` — it does not itself close until FT-024 does. No further scope remains for this Epic beyond FT-024's own resolution.

**FT-024 reconciled to `done` (2026-09-02):** the same real `check` run (`33681012671`, commit `f2099f2`) completed and was independently confirmed passing on both platforms, with `RSK-016`'s own named surface (the multi-command sequence over the persistent connection) verified from the actual downloaded log text, not just the job conclusion — see FT-024's own Notes for the full evidence. **This Epic is done.** Its own Acceptance criteria (a human creating, saving, and reopening a project through real native dialogs, with the whole path also confirmed cross-platform in CI) are now fully satisfied on real evidence.
