# EP-016 — Harness Procedure Extraction: State Reconciliation, Next-Work Selection, Lifecycle Review, Harness Review

Status: done

## Objective

Implement the user-approved harness-architecture review's decisions: extract four repeated, complex, previously-undocumented-as-such procedures into narrow skills (`state-reconciliation`, `next-work-selection`, `lifecycle-review`, `harness-review`), simplify `CLAUDE.md` only where those skills genuinely replace existing procedural prose, and keep every constitutional rule, Protected Decision, and ownership boundary in place unchanged — reconciled with the testing-governance work (EP-015) so both harness changes follow one consistent canonical-doc/skill/pointer pattern.

## Scope / Out of scope

In scope: `.claude/skills/{state-reconciliation,next-work-selection,lifecycle-review,harness-review}/SKILL.md`; the two `CLAUDE.md` §6 passages the review identified as procedural elaboration rather than constitutional rule (the dependency-safety-check paragraph, and the "autonomous" interaction-mode bullet); `docs/project/format.md`'s two existing cross-references to "the dependency-safety check (`CLAUDE.md` §6)", updated to point at the skill that now carries that procedure.

Out of scope (explicitly deferred or rejected by the review, not to be created as part of this Epic): `roadmap-review` skill, `feature-shaping` skill, a specialized Product Manager agent of any kind, a new permanent agent type for `harness-review`'s own use of read-only exploration, any change to Protected Decision categories, ownership boundaries, or any other constitutional content beyond the two identified passages.

## Notes

No Milestone: cross-cutting harness/process work, not a product-outcome slice of MS-003, mirroring EP-007 and EP-015's own precedent. Follows directly from the user-approved harness-architecture review conducted earlier this session (consultative analysis, not itself a PMO record).
