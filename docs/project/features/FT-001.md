# FT-001 — Python Development Foundation

Status: done
Epic: EP-001

## Objective

Establish a uv-managed Python project with pytest, Pyright (strict), and Ruff (lint + format) configured and passing — uv project initialization, pytest running against a minimal placeholder test, Pyright strict passing, Ruff lint and format both passing, and only the minimal `src/backend/core/` path this genuinely requires — proving the toolchain before any domain code exists.

## Acceptance criteria

A uv-managed project exists. `pytest` runs successfully. Pyright strict reports zero errors. Ruff lint and format both pass. No product/domain functionality is implemented.

## Dependencies

None.

## Notes

Excludes Bandit/security scanning (later, if justified), Corytm Engine/Dorian/Runtime domain code, and the unified quality interface (FT-004).
