# FT-002 — Frontend Development Foundation

Status: done
Epic: EP-001

## Objective

Establish a React/TypeScript frontend project with strict typing and passing build/test/lint tooling: a minimal TypeScript-strict React project under `src/frontend/desktop/`, a working build, and test/lint tooling passing against a minimal placeholder — proving the toolchain without designing the product interface or committing to a desktop shell technology.

## Acceptance criteria

The project builds successfully. TypeScript strict mode reports zero errors. A test runner is configured and passing. A linter is configured and passing. No product UI or design-system implementation exists.

## Dependencies

None.

## Notes

Excludes design-system/component-library selection, state-management choice, real UI screens, Storybook or equivalent, and the desktop shell technology (Tauri or otherwise remains unresolved per `docs/technical/architecture.md`'s Unresolved section).
