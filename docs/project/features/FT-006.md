# FT-006 — Harness Validation Exercise

Status: done
Epic: EP-003

## Objective

Execute one genuine, useful, non-product repository Task through the full DISCOVER → PLAN → IMPLEMENT → REVIEW → VALIDATE → SYNC → CLOSE protocol, as the concrete exercise proving EP-003's Objective: that a fresh Claude Code session with no conversation history can carry out real repository work end-to-end under Corytm's governance.

## Acceptance criteria

A real, useful Task (not contrived) is selected and executed to Done. The canonical local quality interface (`make check`) passes against the resulting change. `status.md` and any other affected PMO records are correctly synchronized at Close. The executing session recommends next work without relying on prior conversation history. No Git staging, commit, push, or merge occurs during the exercise.

## Dependencies

None (EP-003's own dependency on EP-002 already governs this Feature).

## Notes

Follows FT-005's precedent of not restating an Epic-level dependency at Feature level.
