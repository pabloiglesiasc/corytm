# FT-026 — Wire Dorian's move_clip Tool to a Live Groq Model

Status: done
Epic: EP-013
Milestone: MS-002

## Objective

Implement EP-013's full scope: the provider-neutral `ModelProvider` boundary, the `GroqProvider` adapter, `corytm.dorian.agent`'s orchestration of `move_clip`'s existing tool/validation boundary, and the live evaluation harness proving GPT-OSS 20B does this reliably.

## Scope / Out of scope

In scope — see EP-013 for the full rationale; as implemented here:

- `src/backend/core/src/corytm/dorian/providers/base.py`: `ToolSpec`, `ToolCall`, the `ModelProvider` protocol, and `tool_spec_from_model` (builds a `ToolSpec` from an existing tool's own Pydantic proposal model).
- `src/backend/core/src/corytm/dorian/providers/groq.py`: `GroqProvider`, implementing `ModelProvider` against the official `groq` SDK's `AsyncGroq` client; the only module depending on `groq`'s types or response shape.
- `src/backend/core/src/corytm/dorian/agent.py`: `MOVE_CLIP_TOOL_SPEC` (built from `corytm.dorian.tools.MoveClipProposal`) and `propose_move_clip(instruction, provider)`, returning a validated `MoveClipProposal` or `None`.
- `src/backend/core/pyproject.toml`: adds `groq` as a runtime dependency; registers a `live_llm` pytest marker for the network-dependent evaluation.
- A live evaluation module, skipped automatically without a real `GROQ_API_KEY`, driving `propose_move_clip` against representative simple move-clip instructions.
- `docs/technical/architecture.md`: Model Routing and Human/Dorian Shared Application Path sections synchronized to reflect this first concrete provider wiring.

Out of scope: everything EP-013 names as out of scope (routing/registry, tier mapping, BYOK, other tools, Desktop UI, unconditional 120B upgrade).

## Acceptance criteria

- `tool_spec_from_model` produces a `ToolSpec` whose `parameters` matches `MoveClipProposal.model_json_schema()` exactly, covered by a colocated test.
- `GroqProvider.propose_tool_call` correctly translates a Groq-shaped tool-call response into a `ToolCall` (tool name, parsed JSON arguments), and returns `None` when the model makes no tool call — covered by tests against an injected fake client, no live network call.
- `propose_move_clip` returns a validated `MoveClipProposal` when a fake `ModelProvider` returns a valid `move_clip` call, returns `None` when the provider returns no call or a different tool name, and propagates `pydantic.ValidationError` when the provider's arguments fail `MoveClipProposal`'s own validation — covered by tests using a fake `ModelProvider`, no network.
- `make check-python` (pyright strict, ruff, pytest, all excluding `transport`) passes end to end with zero new warnings.
- A human, holding a real `GROQ_API_KEY`, runs the live evaluation and confirms GPT-OSS 20B reliably selects `move_clip` and produces valid arguments for representative simple instructions — or, if it does not meet the bar, confirms GPT-OSS 120B does, per ADR-013's documented fallback path.

## Dependencies

EP-013 (this Feature's Epic). EP-008 (done) — `corytm.dorian.tools.MoveClipProposal`/`move_clip`, reused unchanged as the schema source and the trusted execution path this Feature's orchestration feeds into.

## Notes

Decomposed into two Tasks per the Task-creation Threshold: **TK-031** (the provider-neutral boundary and the Groq adapter — a genuinely new technical surface: this project's first external network dependency and first third-party API secret, worth isolating and settling on its own) and **TK-032** (orchestration wiring `move_clip`'s existing schema/validation through that boundary, plus the live evaluation harness — builds on TK-031, and is the one requiring the human-run live evaluation).

**TK-031 and TK-032 both implemented and locally validated (2026-09-03):** see each Task's own Notes for full detail. `corytm.dorian.providers.base`/`.groq` gave Dorian its `ModelProvider` boundary and its first concrete adapter (`GroqProvider`, against the real `groq` SDK's own types); `corytm.dorian.agent.propose_move_clip` wires `MoveClipProposal` through it, reusing ADR-004's validation boundary unchanged. `make check-python` passed end to end with zero new warnings; `git status` confirmed only this Feature's own named files changed. `docs/technical/architecture.md`'s Model Routing, Human/Dorian Shared Application Path, and Unresolved sections were synchronized.

No fresh cross-platform CI evidence is required to close this Feature, per EP-008's own precedent: no native/schema/frontend code was touched, and `groq` (the one new dependency) is pure Python with no native extension — there is nothing new for a Windows CI run to disprove that EP-008's own reasoning didn't already cover for this kind of change.

**Evidence (per `docs/project/format.md`'s Evidence Traceability):**
- Delivery commit pushed: **confirmed** — commit `5875da3` ("Give Dorian's move_clip a live Groq model (EP-013/FT-026); fix FT-025's real Windows/macOS CI regressions (RSK-017)"), independently confirmed via `git fetch origin main`/`git rev-parse` matching `origin/main` exactly.
- Human-run live evaluation (`test_agent_live_eval.py`, a real `GROQ_API_KEY`): **passed** — the user ran `GROQ_API_KEY=... uv run pytest -m live_llm -v src/corytm/dorian/test_agent_live_eval.py` and reported `2 passed`: both `test_a_simple_move_instruction_produces_a_valid_proposal` and `test_a_differently_phrased_instruction_still_produces_a_valid_proposal` passed against the default model (`openai/gpt-oss-20b`, `GroqProvider()`'s default) — GPT-OSS 20B reliably selected `move_clip` and produced valid, correctly-parsed arguments for both representative instructions on the first attempt. ADR-013's `openai/gpt-oss-120b` fallback path was not needed.

Both required evidence items now read passed/confirmed, with no fresh cross-platform CI required (per this Feature's own reasoning above). **This Feature is done.**

**FT-025/EP-012 reverted `validating → active` concurrently (2026-09-03):** its Windows CI evidence failed for reasons implicating its own Tauri/Rust and asyncio-timeout surface — see `RSK-017`, EP-013's own Notes, and FT-025's Notes. Confirmed this Feature's assumptions are unaffected (no dependency on the Desktop-channel ACL/timeout surface FT-025 touches), so per `CLAUDE.md` §6 this Feature continued unpaused rather than being reworked against a corrective fix. That regression fix and this Feature's own implementation shared one delivery commit (`5875da3`); a fresh cross-platform CI run (`33719557924`) is genuinely `in_progress` against it as of this entry — required for FT-025/`RSK-017`, not for this already-done Feature.
