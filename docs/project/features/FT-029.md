# FT-029 — Extract State-Reconciliation, Next-Work-Selection, Lifecycle-Review, and Harness-Review as Skills

Status: done
Epic: EP-016

## Objective

Create the four approved skills, simplify `CLAUDE.md` §6 exactly where they replace existing procedural prose (keeping every constitutional rule, state model, and Protected Decision list unchanged), synchronize `format.md`'s two existing cross-references, and validate the resulting canonical Continue-Corytm flow by actually invoking the new skills against this repository's real current state.

## Scope / Out of scope

Mirrors EP-016's own scope (see `docs/project/epics/EP-016.md`).

## Acceptance criteria

- `.claude/skills/state-reconciliation/SKILL.md`, `.claude/skills/next-work-selection/SKILL.md`, `.claude/skills/lifecycle-review/SKILL.md`, `.claude/skills/harness-review/SKILL.md` all exist, matching the established repository skill format (`ui-ux-review`/`testing-review`: frontmatter `description` only, a ground-truth pointer to canonical doc(s), a numbered checklist or structured procedure, an Output section, an Out of scope section) — none restates a canonical document's own durable content; each points to it.
- `next-work-selection` uses qualitative, evidence-cited comparison against `docs/project/plan.md`'s existing Next-Work Selection Priority list — no numerical scoring or weighted rubric introduced anywhere. `docs/project/plan.md` itself is not edited; the skill only applies it.
- `state-reconciliation` states evidence-first semantics explicitly (establish what is actually true from real evidence before updating any PMO/status summary) and defines the fast path: when it confirms an existing Active Feature has safe, executable work remaining, the session continues that Feature directly without invoking `next-work-selection`.
- `next-work-selection` carries the dependency-safety-check procedure relocated from `CLAUDE.md` §6 (the plausible-invalidation-vs-proximity judgment, the RSK-002–015 precedent, the infrastructure-only-failure nuance, and the retroactive pause-and-reconcile instruction) essentially intact — not reinvented, not summarized away.
- `lifecycle-review`'s own trigger condition is stated narrowly and explicitly: invoked whenever a lifecycle-phase transition is being recommended or a Milestone's outcome is being cited as evidence for one — never during ordinary Milestone close-out where phase exit isn't implicated.
- `harness-review` states its trigger (explicit request, or opportunistic only on concrete evidence of inconsistency — never by default on Continue Corytm) and its proportional-delegation rule (a narrow, localized contradiction is inspected directly; a broad cross-artifact audit may delegate its read-heavy discovery phase to the existing general-purpose/Explore agent mechanism — no new permanent agent type is introduced for this).
- `CLAUDE.md` §6 changes in exactly two places: the dependency-safety-check paragraph is replaced by a compact statement of the rule itself (proximity/consumption alone never blocks; only a plausible invalidation of an already-established assumption does) plus a pointer to `next-work-selection`; the "autonomous" interaction-mode bullet is simplified to reference `state-reconciliation`/`next-work-selection` (with the fast path named inline, since it governs which of the two skills a session even needs) instead of spelling out the full procedure. Every other line of `CLAUDE.md` — constitutional state model, Protected Decisions, ownership table, CPO Mandate, all of §1–§5 and §7–§11 outside these two passages — is verified unchanged via `git diff`.
- `docs/project/format.md`'s two existing "(`CLAUDE.md` §6)" cross-references to the dependency-safety check are updated to point at `next-work-selection` instead; no other line of `format.md` changes.
- The resulting flow is validated by actually invoking `state-reconciliation` and (if its fast path does not apply) `next-work-selection` against this repository's real current state as of this Feature's own close — not merely inspected for well-formedness.

## Notes

Decomposed into two Tasks: TK-037 (the `CLAUDE.md`/`format.md` constitutional consolidation — a real decision boundary touching the protected constitution, worth getting exactly right and isolating before anything is built on top of its new wording) and TK-038 (authoring the four skills, which quote the post-TK-037 wording and therefore depend on it). Executed in that order; both `done` — see each Task's own Notes.

### Review

`git diff CLAUDE.md`/`docs/project/format.md` confirmed confined to exactly the intended passages (§2 above). Cross-checked the four skills' Out-of-scope sections against each other to confirm no overlapping triggers or restated content (detail in TK-038's Notes). No comments, no suppression directives, no scope creep beyond one deliberate, disclosed correction (TK-037's Notes: an initial mis-attribution of the retroactive pause-and-reconcile guidance to `format.md`, caught and corrected to `next-work-selection` before proceeding).

### Validate — live flow exercise, not inspection alone

Per this Feature's own acceptance criteria, the resulting flow was exercised for real rather than merely read back:

**`state-reconciliation`**, invoked against this session's actual state (FT-029 active, TK-037/TK-038 done), independently re-verified rather than assumed — and surfaced a real, unrelated discrepancy this session would otherwise have missed: FT-027's Evidence field still read the CI run `33754233276` as `in_progress`, but `gh run view --json headSha,status,conclusion` showed it had genuinely completed — `conclusion: success` on both `check (macos-latest)` and `check (windows-latest)`, matched by `headSha` exactly against FT-027's delivery commit (`2b37ab5`). Per this project's own established practice for a first-of-its-kind native surface, the downloaded full log was read directly rather than trusting the conclusion field alone: `playback_proof` passed on both platforms (`6.94s` on macOS — a real device-driven playback window; `0.08s` on Windows — confirming the designed soft-skip path fired for a runner with no usable audio device, not a false pass), zero failures, zero new warnings. Reconciled synchronously, exactly as the skill specifies: **FT-027 closed `validating → done`**, **`RSK-018` closed**, EP-014's own dependency-safety-check wait resolved (its Notes updated accordingly). This is real, demonstrated value from the skill on its very first live use, independent of and prior to this Feature's own Close. `state-reconciliation`'s fast path then correctly applied to FT-029 itself: safe, executable work remaining (Close only) — continue directly, no `next-work-selection` needed for this Feature.

**`next-work-selection`** was still exercised, framed explicitly as a validation-only run against the state the next session will actually face (no Feature Active, FT-027/RSK-018 just resolved, EP-014's second Feature — wiring the now-proven playback mechanism into `native_runtime`'s real command protocol and a minimal Desktop Play/Stop UI — still un-created) — its own conclusion was not acted on. Grounded in real evidence, not assumed: read `docs/project/milestones/MS-003.md`'s actual Outcome statement and `docs/product/roadmap/alpha.md`'s Real-Time Audio & Transport category directly. Conclusion: EP-014's second Feature is the clear next candidate — it directly completes MS-003's own Outcome (priority 1), its only dependency (FT-027) is now genuinely done (priority 2), it is Alpha's most evidently unmet exit condition per `docs/project/plan.md` (priority 3), and it integrates an already-proven native mechanism through an already-proven Desktop-channel/UI pattern (EP-011/EP-012) rather than opening new native risk (priority 4) — a smaller, lower-risk increment than FT-027 itself, consistent with this project's own established "prove the mechanism, then wire it" shape. No Protected Decision is implicated (formalizing already-approved MS-003/EP-014 scope). **Not implemented** — out of scope for this Feature; left for the next session.

A full local `make check` was not run for this Feature: no product code, tests, or build configuration changed — everything in scope is documentation/skill/constitution content. Each skill's frontmatter/content was confirmed well-formed by the Skill tool successfully loading and displaying all four (registered and invocable immediately after each was written).

### Sync

`CLAUDE.md`, `docs/project/format.md`, and the four new skills are this Feature's own knowledge-impact surface, applied during implementation. Beyond its own declared scope, this Feature's live validation exercise produced real knowledge-impact on FT-027/EP-014/RSK-018/`status.md` (documented above and in each record's own Notes) — a direct, disclosed consequence of exercising `state-reconciliation` for real, not scope creep into unrelated Feature territory chosen arbitrarily.

**FT-029 and EP-016 are closed `done`** — no external evidence is required for a documentation/skill/constitution Feature with no product-code or CI surface.
