# MS-002 — First Sound

Status: achieved

## Outcome

A user can create or open a Corytm project and produce actual sound through the full real stack via direct manual editing (user → desktop → Python core → Corytm Engine → Runtime → Native Audio Runtime → Tracktion Engine → sound), and Dorian can perform a real semantic edit through the same governed application path with an observable effect on that sound (user → Dorian → Corytm Engine → Runtime → sound) — Alpha's two end-to-end product paths (`docs/project/plan.md`), both real and demonstrable.

## Notes

Was `planned`, not `active`, until the two foundational architecture boundaries both product paths depend on were resolved: the desktop shell technology (ADR-006 — Tauri 2) and the Python↔C++ transport (ADR-007 — Protobuf over a local loopback-socket transport), both decided with explicit user approval under `CLAUDE.md` §5. EP-004, this Milestone's first Epic, is done (see `docs/project/status.md`): both ADRs are proven working in practice via minimal plumbing (Tauri shell, schema/codegen, transport, sidecar spawn/shutdown).

EP-005 (First Sound: Minimal Track and Clip Through Tracktion), this Milestone's second Epic, is `done`: a real Python entry point owning a minimal `Project`/`AudioTrack`/`AudioClip` crosses the ADR-007 transport, and the Native Audio Runtime genuinely materializes it as a real Tracktion `Edit` and renders real, deterministic, non-silent sound — offline, with no audio device, plus a real playable WAV file confirmed produced. Both required acceptance boundaries are satisfied: real Windows CI confirmation (independently verified from real job logs, not the user's report alone) and the human-only local audible smoke test EP-005's own scope called for as a distinct, non-automatable acceptance boundary. See `docs/project/epics/EP-005.md` and `docs/project/status.md` for the full evidence trail.

This Milestone's Outcome is still not fully reached even once EP-005 closes: EP-005 deliberately proves only a synthetic-fixture, one-track/one-clip slice driven by a hardcoded script, not a real user-facing project-creation flow, real imported audio, or Dorian's semantic-edit path.

**EP-006 (First Edit: Incremental Clip Move Through a Live Native Session)**, this Milestone's third Epic, is `done` (2026-09-02): both of this Outcome's still-unreached halves (manual Desktop editing, Dorian's semantic edit) depend on Corytm Engine expressing an edit as an incremental command rather than resending the whole project, which EP-005 deliberately left unproven (`docs/technical/architecture.md`'s "Engine Services and Incremental Updates" section named this direction without a concrete instance). EP-006 proved exactly one edit operation — moving a clip — end to end through three increments (Engine operation + wire schema, native live-session isolation, real transport wiring), each confirmed by real, independently-verified cross-platform CI evidence. See `docs/project/epics/EP-006.md` and `docs/project/status.md` for the full evidence trail.

This Milestone's Outcome is still not fully reached even once EP-006 closes: neither half — real Desktop project-creation/editing UI, nor Dorian's semantic-edit path — has been built yet; EP-006 only proved the shared command/live-session mechanism both future paths depend on.

**EP-008 (Dorian's First Semantic Edit)**, this Milestone's fourth Epic, is `done` (2026-09-02): it took on the Dorian half of this Outcome, exposing EP-006's move-clip mechanism through Dorian's first trusted semantic tool (ADR-004) — `corytm.dorian.tools.move_clip` — rather than a direct call. See `docs/project/epics/EP-008.md`.

This Milestone's Outcome is still not fully reached even once EP-008 closes: Dorian's tool boundary is real, but no model/provider is wired up to actually call it yet (proposals are still constructed directly, standing in for a model's output), and no real Desktop project-creation/editing UI exists — both remain open toward this Milestone's full Outcome.

**EP-009 (Desktop Command Channel)**, this Milestone's fifth Epic, is `done` (2026-09-02): it proved the one mechanism real Desktop editing UI has nowhere to build on without — a typed, versioned, authenticated channel from Tauri's Rust core to the real Python core sidecar, fully independent of the Python↔Native Audio Runtime transport (ADR-010). Both increments (Rust Protobuf codegen toolchain; the real socket round trip, with `corytm`'s own entry point gaining its first persistent server mode) are confirmed by real, independently-verified cross-platform CI evidence. See `docs/project/epics/EP-009.md`.

This Milestone's Outcome is still not fully reached even once EP-009 closes: the channel exists, but no real UI-facing domain command travels over it yet (only a proof-of-mechanism message), and no Desktop UI component/state-management/rendering choice has been made — real Desktop project-creation/editing UI is now unblocked, not yet built. Dorian-model/provider wiring remains the other open thread, deferred pending an explicit provider-selection decision.

**EP-010 (Desktop's First Real Domain Command)**, this Milestone's sixth Epic, is `done` (2026-09-02): a human can now launch the real Desktop app, watch it spawn and connect to the Python core sidecar over EP-009's channel, click a real UI control, and observe a real Corytm Engine edit (a clip moving) applied and re-rendered through the Native Audio Runtime — confirmed by real, independently-verified cross-platform CI evidence plus a human macOS confirmation (`ps` process evidence for spawn/shutdown; a real GUI click for the command itself). See `docs/project/epics/EP-010.md` and `docs/project/status.md` for the full evidence trail, including a real CI hang the first delivery commit exposed and its corrective fix.

This Milestone's Outcome is still not fully reached even once EP-010 closes: this is one hardcoded move against an in-memory fixture project, not real project creation, opening, or persistence — and no model/provider is wired up to call Dorian's tool yet. Both remaining threads (real Desktop project-creation/persistence UI; Dorian-model/provider wiring) are candidates for this Milestone's next Epic, and both plausibly touch a `CLAUDE.md` §5 protected boundary (the local-first persistence strategy; an explicit provider-selection/BYOK decision, `docs/product/strategy.md` §10) — neither was decided silently; see `docs/project/status.md`'s latest entry for the recommendation put to the user.

**EP-011 (Real Desktop Project Persistence)**, this Milestone's seventh Epic, is `done` (2026-09-02): once ADR-011 settled the local-first persistence strategy, a human can now launch the real Desktop app, create a new project, save it to a real file through a native save dialog, quit and relaunch, and reopen that file through a native open dialog — observing genuine re-render evidence through the Native Audio Runtime. Proven by a combination of automated tests (Engine persistence module, Desktop-channel create/save/open commands, the persistent multi-command connection) and a human confirmation step for the real native-dialog behavior automation cannot exercise, plus real, independently-verified cross-platform CI evidence for both delivery commits. See `docs/project/epics/EP-011.md` and `docs/project/status.md` for the full evidence trail, including a real product-path bug (an empty, zero-track project's exit code being misread as a hard failure) found by the human confirmation step, root-caused, and fixed.

This Milestone's Outcome is still not fully reached even once EP-011 closes: EP-011 proves project creation, saving, and opening, but not genuine user-driven track/clip authoring (every prior Feature, including EP-011's own acceptance path, still only ever exercises the one hardcoded fixture clip or an empty project) — `docs/technical/architecture.md`'s own Unresolved section already named this gap. **EP-012 (Real Track/Clip Authoring)** was created to take on exactly this thread (see `docs/project/epics/EP-012.md`); Dorian-model/provider wiring remains this Outcome's other open thread, still blocked on an explicit, not-yet-made protected provider-selection decision.

EP-012/FT-025 reached `validating` (implementation, local validation, and human confirmation of the real UI all passed; only a real Windows CI run remains, per `docs/project/status.md`), functionally satisfying this Outcome's manual-editing half. With the implementation slot free and no other unblocked work under this Outcome, the Dorian-model/provider decision was put to the user, who approved a specific, deliberately narrow and temporary choice — recorded as `decisions/ADR-013.md` (Groq-hosted GPT-OSS, behind a provider-neutral `ModelProvider` boundary). **EP-013 (Dorian's First Live Model Call)** was created to implement it (see `docs/project/epics/EP-013.md`): once both this Outcome's halves are genuinely evidenced (EP-012/FT-025's Windows CI; EP-013's human-run live evaluation), this Milestone's Outcome is fully reached.

That Windows CI run completed with real failures on both platforms, directly implicating FT-025's own fixes (see `RSK-017`, FT-025's Notes); FT-025/EP-012 reverted `validating → active` to fix them and this Outcome's manual-editing half is not yet finally evidenced. EP-013/FT-026's own implementation is unaffected (see EP-013's Notes) and continues concurrently toward this Outcome's Dorian-edit half.

**EP-013 (Dorian's First Live Model Call) is done (2026-09-03):** the user ran FT-026's human-run live evaluation with a real `GROQ_API_KEY` and it passed (`2 passed`, default `openai/gpt-oss-20b`, no fallback needed); the delivery commit (`5875da3`, also carrying FT-025's regression fix) is confirmed pushed. This Outcome's Dorian-semantic-edit half (user → Dorian → Corytm Engine → Runtime → sound) is now genuinely, fully evidenced end to end — the trusted tool boundary (EP-008) driven by a real live model (EP-013), not a hand-constructed stand-in. This Milestone's Outcome is not yet fully reached: the manual-editing half still awaits fresh cross-platform CI confirming FT-025/`RSK-017`'s regression fix (run `33719557924`, `in_progress` against `5875da3` as of this entry).

**FT-025/EP-012 reconciled `validating → done` (2026-09-03):** a later session independently confirmed run `33719557924`'s real outcome — `conclusion: success` on both `check (macos-latest)` and `check (windows-latest)` against the exact delivery commit `5875da3` — and, going beyond the job conclusion, grepped the actual downloaded log content confirming both previously-failing tests (the Windows ACL test, the macOS idle-gap stress test) now genuinely pass, with zero `FAILED`/`error[`/`panicked` and no new warnings anywhere. `RSK-017` closed on this evidence. This Outcome's manual-editing half (user → desktop → Python core → Corytm Engine → Runtime → Native Audio Runtime → Tracktion Engine → sound, via genuine user-driven track/clip authoring) is now, alongside EP-013's already-evidenced Dorian-edit half, fully and independently evidenced. **This Milestone's Outcome is fully reached. MS-002 is `achieved`.**

This also satisfies `docs/project/plan.md`'s Alpha Exit criteria verbatim: both of Alpha's named end-to-end product paths are now real and demonstrable, each backed by real, independently-verified evidence rather than local reasoning or a single platform. This is a lifecycle-phase-transition question (`CLAUDE.md` §5's "major lifecycle-definition changes"), not decided here — see `docs/project/status.md` for the recommendation put to the user.

**Correction (2026-09-03): the paragraph immediately above is superseded, not withdrawn from the record.** The user reviewed the Alpha → Beta transition recommendation it fed and judged Alpha's then-current exit criteria genuinely underspecified — satisfiable by a narrow, offline-only, single-tool proof never intended to stand in for Alpha's real bar. Following a full product-governance consolidation, `docs/project/plan.md`'s Current Phase: Alpha section and the new `docs/product/roadmap/` (canonical capability-by-phase map) now state Alpha's exit criteria precisely: every `Desktop — Alpha` capability `docs/product/roadmap/alpha.md` names realized and validated, plus three conditions no capability list alone guarantees — genuine real-time audio-device playback (not merely the offline rendering this Milestone's Outcome already proved), the roadmap's Alpha-assigned arrangement/editing/audio-clip/mixer capabilities realized in aggregate (not an isolated Add-Track/Add-Clip proof), and a vertically-integrated Desktop Dorian product surface (not the backend-only tool/model proof this Milestone's Outcome evidences). **This Milestone's own Outcome remains genuinely `achieved` — that specific, narrower claim is not reversed — but achieving it does not mean Alpha is complete, and the "satisfies Alpha's Exit criteria verbatim" claim above no longer holds under the corrected criteria.** Corytm remains in Alpha. Shaping the next Milestone against `docs/product/roadmap/alpha.md`'s still-unrealized capabilities (real-time audio is the most evidently unmet) is deliberately left for a following session, not decided in this same pass.
