# Risk Register

A risk is something uncertain that may happen. A blocker is something already preventing or affecting work right now — blockers live on the affected work item's own Status/Blocker field, never here.

## Format

`RSK-001` | Description | Likelihood (low/med/high) | Impact (low/med/high) | Status (open/monitoring/materialized/closed) | Mitigation | Related work

## Current Risks

`RSK-015` | TK-026 makes the Desktop channel a multi-command session for the first time (previously exactly one command per connection), and makes Rust decode a discriminated `project_proto::Event` oneof response for the first time (previously always the one known `ClipMovedEvent` message directly) — a genuinely new combination of already-proven pieces (the `Command`/`Event` envelope itself, `native_runtime.cpp`'s own session-loop precedent, `prost`'s oneof codegen for `project.proto`). This project's own history (RSK-002 through RSK-014) is direct, repeated precedent against assuming a clean local pass predicts real Windows CI behavior for a new combination | low | med | open | Locally verified on macOS: three new automated Python tests drive the real Desktop channel session end to end (create/save; open+re-materialize+move-clip; move-clip alone with no prior command), all passing; the updated `move_clip` Rust command (now sending/decoding the enveloped `Command`/`Event`) passes `cargo test` with zero new warnings; a full local `make check` (all five targets, including `make check-desktop`'s release build) passes end to end. Awaiting a real, independently-verified passing Windows GitHub Actions run before this risk closes | EP-011, FT-023, TK-026

`RSK-014` | TK-024 adds Tauri's app-command ACL/permission system to this project for the first time — a `#[tauri::command]` registered via `.invoke_handler(tauri::generate_handler![...])`, an auto-generated `permissions/autogenerated/move_clip.toml`, and a `capabilities/default.json` entry (`"allow-move-clip"`) — plus a real frontend-triggered async round trip (JS `invoke` → Rust command → a fresh `TcpStream` connect/authenticate/send/receive against the already-spawned sidecar) driven by an actual GUI click, none of which has been exercised on Windows from this session. This project's own history (RSK-002 through RSK-012) is direct, repeated precedent against assuming a clean macOS pass predicts real Windows behavior for a genuinely new combination | low | med | closed | One real run (`33657379249`, commit `c88d7a6`) gave no evidence either way — `cargo test` hung at `desktop_channel_tests::move_clip_command_moves_the_fixture_clip_and_renders_the_effect` before this risk's own ACL/round-trip surface was exercised. Root cause diagnosed and fixed (see FT-022's Notes: a genuine cross-platform `asyncio.Server.wait_closed()` teardown deadlock plus a `Makefile` target-order gap). **Closed:** the corrective fix was pushed as `e3bd5a3` and independently confirmed via `gh run view`/`gh api` (run `33666555272`) — `check` passed on both `check (macos-latest)` (8m3s) and `check (windows-latest)` (~28m32s); both downloaded job logs show `desktop_channel_tests::move_clip_command_moves_the_fixture_clip_and_renders_the_effect ... ok` inside `test result: ok. 3 passed; 0 failed`, zero hang-watchdog lines, zero `error[`, and zero warnings touching `lib.rs` — the real ACL/round-trip surface this risk tracked genuinely ran and passed on both platforms | EP-010, FT-022, TK-024

`RSK-013` | TK-023 makes Rust's `prost`/`prost-build` compile `project.proto` for the first time — a materially different shape of generated code (nested/repeated composite messages: `Project.tracks`, `AudioTrack.clips`) than `desktop.proto`'s single flat `DesktopProofMessage`, which is all TK-020/RSK-010's real Windows CI confirmation actually validated for Rust codegen. This project's own history (RSK-005's near-identical concern for C++/protoc, and RSK-002/003/004's repeated pattern of a new toolchain surface behaving differently on Windows) is direct precedent against assuming this is fine by analogy without real evidence | low | med | closed | Same inconclusive run as `RSK-014` above (`33657379249`, commit `c88d7a6`) — the hang happened before this risk's own surface was meaningfully exercised beyond a successful build step. **Closed:** same real evidence as `RSK-014` above (run `33666555272`, commit `e3bd5a3`) — both platforms' `check` passed, `project.proto`'s Rust codegen compiled, linked, and round-tripped correctly as part of the now-passing `move_clip_command_moves_the_fixture_clip_and_renders_the_effect` test on both `macos-latest` and `windows-latest` | EP-010, FT-022, TK-023

`RSK-012` | TK-022 makes production `run()` — not only a test — spawn a real child process and manage its lifecycle for the first time: `tauri::async_runtime::spawn` fired from `.setup()`, `Manager`-based shared state (`app.manage`/`app.state`) read from both a background task and a `RunEvent` callback, and `Builder::build(context)?.run(callback)` replacing every prior Task's simpler `.run(context)` so the callback can observe `RunEvent::ExitRequested`/`Exit`. None of these individual APIs is exotic, but this exact combination — async work started during setup, cross-task shared state, and app-lifecycle event handling — has not been exercised on Windows from this session. This project's own history (RSK-002 through RSK-011) is direct, repeated precedent against assuming a clean macOS pass predicts real Windows app behavior for a genuinely new combination | low | med | closed | Locally verified: `cargo build --lib` and `cargo build --release --bin app` both clean with zero warnings; `cargo test` (all 3 tests, including the refactored `desktop_channel_round_trips_over_the_second_transport` now driving the shared production function) passes; `make check-desktop` and a full clean `make check` pass end to end. **Real (non-mock) macOS app confirmed by the user:** ran `npm run tauri dev`, and `ps aux | grep -i "corytm serve"` showed both the `uv run` launcher and the spawned Python process genuinely running while the app was open; after closing the window (the real close path, which fires `RunEvent::ExitRequested`/`Exit` — not `Ctrl+C`, which would bypass it), the same check returned zero matches, confirming clean shutdown. **Closed:** independently confirmed via downloaded job logs (`gh run view`/`gh api`, run `33644724394`, commit `09aa8b9`) — `check` passed on both `check (macos-latest)` and `check (windows-latest)`; both real logs show `desktop_channel_round_trips_over_the_second_transport ... ok` inside `3 passed; 0 failed`, and `tests/test_entrypoint.py .` genuinely passing inside `6 passed, 28 deselected` on both platforms; a grep of both full logs for `warning:` touching `lib.rs`/any other `src-tauri` file and for `FAILED`/`error[` returned zero matches (macOS's 185 total warnings all confirmed under vendored `_deps/juce-src`) | EP-010, FT-021, TK-022, ADR-006, ADR-010

`RSK-011` | TK-021 makes Rust a raw TCP socket client authenticating against a real spawned Python server for the first time — the mirror image of every prior Python-as-client proof (RSK-003's `asyncio` subprocess+socket client, RSK-009's live multi-command session), and a genuinely new combination: `corytm`'s own real entry point now branches into a persistent server mode, Python opens a second concurrent `asyncio.start_server` listener port, and Rust parses a stdout handshake line it has never had to parse before. Only exercised on macOS from this session; this project's own history (RSK-002 through RSK-010) is direct, repeated precedent against assuming a clean macOS run predicts Windows behavior for a new process/socket combination | low | med | closed | Locally verified end to end: the real `cargo test` round trip (`desktop_channel_round_trips_over_the_second_transport`) passes, receiving a genuinely distinct reply payload proving real server-side decode/re-encode; `corytm`'s existing no-argument behavior (`test_entrypoint.py`) is confirmed unaffected; a full clean `make check` (all five targets, plus `make check-desktop`) passes with zero new warnings. **Closed:** independently confirmed via downloaded job logs (`gh run view`/`gh api`, run `33637336480`, commit `d88aabd`) — `check` passed on both `check (macos-latest)` and `check (windows-latest)`; both real logs show `test desktop_channel_tests::desktop_channel_round_trips_over_the_second_transport ... ok` inside `3 passed; 0 failed`, and `tests/test_entrypoint.py .` genuinely passing inside `6 passed, 28 deselected` on both platforms; a grep of both full logs for `warning:` touching the changed files and for `FAILED`/`error[` on Windows returned zero matches | EP-009, FT-020, TK-021, ADR-010

`RSK-010` | TK-020 gives Tauri's Rust core a Protobuf codegen toolchain for the first time: `build.rs` performs a network download (an official `protoc` release archive), a SHA256 verification, and a zip extraction — all new build-time logic that has only run on macOS from this session. This project's own history (RSK-002 through RSK-009) is direct precedent for new build-toolchain surfaces behaving differently, or failing outright, on Windows in ways local reasoning cannot predict. Specifically untested on Windows: whether the `protoc.exe` entry path inside the release zip (`bin/protoc.exe`) resolves correctly under the `zip` crate; whether `ureq`'s TLS stack reaches GitHub Releases cleanly from a Windows GitHub Actions runner; and whether anything beyond the existing `#[cfg(unix)]`-guarded chmod step is actually needed for the extracted binary to be directly executable by `prost-build` on Windows | low | med | closed | Locally verified end to end on macOS from a genuinely clean state (`cargo clean` then rebuild): the protoc download/SHA256-verify/zip-extraction/codegen pipeline succeeded, `cargo test` (including the new `desktop_proof_message_round_trips` test) and the full `make check-desktop` (release build + DMG bundling + `cargo test`) both passed with zero new compiler warnings. **Closed:** independently confirmed via downloaded job logs (`gh run view`/`gh api`, run `33618388669`, commit `136abd7`) — `check` passed on both `check (macos-latest)` and `check (windows-latest)`; both real logs show `test desktop_proto_tests::desktop_proof_message_round_trips ... ok` inside `2 passed; 0 failed`, and a grep of both full logs for `warning:` touching `build.rs`/`lib.rs`/the desktop crate returned zero matches. All three specifically-named untested Windows mechanisms are confirmed resolved | EP-009, FT-019, TK-020, ADR-010

`RSK-009` | TK-018 introduces a genuinely new combination: a `Command`/`Event` protobuf envelope that did not exist before, `native_runtime.cpp`'s first multi-command loop (replacing its previous single-command-then-exit lifecycle), and a `tracktion::Edit` kept alive in memory across a real process's full connection lifetime rather than built-and-discarded per command. None of the three individual mechanisms is new in isolation (the envelope's inner messages, `buildEdit`/`moveClip`/`renderEdit`, and the socket transport are each already separately proven), but this project's own history (RSK-002 through RSK-007) is direct precedent for a new combination of already-proven pieces behaving differently across macOS and Windows in ways local reasoning cannot predict | low | med | closed | Locally verified end to end (`ctest` 5/5, `pytest -m transport` 5/5). **Closed:** independently confirmed via downloaded job logs (`gh run view`/`gh api`, run `33609898160`, commit `128e298`) — `check` passed on both `check (macos-latest)` and `check (windows-latest)`, with both real logs showing `tests/test_incremental_edit.py ..` inside `5 passed, 23 deselected` and `5/5 Test #5: live_session_proof ... Passed` inside `100% tests passed out of 5` — the new envelope/multi-command-loop/live-session combination genuinely ran and behaved correctly on both platforms, not just macOS | EP-006, FT-017, TK-018

`RSK-008` | TK-016 diagnosed why the prior CI Optimization work's Windows `check` job showed zero sccache compiler-cache activity for both native MSVC C/C++ and Cargo/Rust, using the real downloaded logs of run `33564596456` (commit `7ebb101`). The MSVC/C/C++ half is conclusively explained and not expected to recur: CMake's Visual Studio (MSBuild) generator has never honored `CMAKE_<LANG>_COMPILER_LAUNCHER` (only the Makefile/Ninja generators do — tracked upstream at CMake's own `gitlab.kitware.com/cmake/cmake` issue #19326), reproduced identically by other users hitting the same symptom (`mozilla/sccache` issues #957, #1097, both still open), and sccache — unlike ccache/clcache — has no mode to masquerade as `cl.exe` for MSBuild's `CLToolExe` mechanism (`mozilla/sccache` issue #1063, still open: sccache errors `cannot find binary path` when invoked without an explicit compiler argument). There is currently no configuration that routes MSVC C/C++ compilation through sccache while keeping the Visual Studio generator; the dead `-DCMAKE_C_COMPILER_LAUNCHER`/`-DCMAKE_CXX_COMPILER_LAUNCHER`/`-DCMAKE_MSVC_DEBUG_INFORMATION_FORMAT`/`-DCMAKE_POLICY_CMP0141` flags (the last also independently wrong — CMake requires `CMAKE_POLICY_DEFAULT_CMP0141`, confirmed against CMake's own CMP0141 docs and against the CI log's own "CMake Warning (unused-cli)" — a typo apparently copied from `mozilla/sccache`'s own README, which carries the identical typo) were removed from the Windows configure flags rather than left silently ineffective. **Update (real run `33594758288`, commit `64f6264`):** the Cargo/Rust half is now conclusively explained, and was never actually a caching failure. `SCCACHE_LOG=sccache=debug`/`SCCACHE_ERROR_LOG` diagnostics captured the sccache daemon's own timeline directly: it started once, genuinely served the whole Cargo/Rust build with real hits (`stats after compile result: ServerStats { compile_requests: 756, ..., requests_executed: 615, cache_hits: {"Rust": 522}, cache_misses: {"Rust": 82} }` — a ~86% Rust hit rate), then sat idle for exactly 10 minutes 5 seconds after the last Rust compile before logging `shutting down due to being idle or request` — sccache's own documented default `SCCACHE_IDLE_TIMEOUT` (600s). That gap exists precisely because this same Task's MSVC fix (removing the confirmed-dead Windows C/C++ launcher flags) means the ~17-minute native `cmake --build` phase that follows generates no sccache traffic of its own — nothing refreshes the daemon's idle clock before the job ends. By the time `mozilla-actions/sccache-action`'s post-step calls `sccache --show-stats`, it silently talks to a brand-new, empty auto-spawned server, not the one that did the real work — a stats-reporting artifact, confirmed to be the same explanation behind every prior "zero requests" observation across both runs. Fix applied: `SCCACHE_IDLE_TIMEOUT: "0"` (sccache's own documented setting to disable the timeout) added to the job env; the now-served-their-purpose diagnostic scaffolding (`SCCACHE_LOG`/`SCCACHE_ERROR_LOG`, the explicit start-server step, the log-dump step) was removed. See `.claude/memory/sccache-idle-timeout-final-stats.md`. Separately, the native CMake target-graph duplication (the same Tracktion/JUCE translation units compiled once per target — confirmed empirically via both the Windows MSBuild log's per-source compile counts and the macOS Makefile-generator log's per-target `CMakeFiles/<target>.dir/` object paths) was evaluated for consolidation into a shared library and genuinely attempted locally; the attempt was rejected on real evidence, not assumption — because JUCE's module `INTERFACE_SOURCES` propagate through any further `PUBLIC`-linked real (non-`INTERFACE`) library to whichever target ultimately links it, wrapping the JUCE/Tracktion targets in a new shared library did not eliminate the duplicate compiles (an actual local rebuild showed a fifth, unused copy added on top of the original four), and the only way to stop that propagation (linking the wrapped targets `PRIVATE`) would also silently stop propagating the include directories, compile definitions, and OS-framework link requirements every consumer's own source still needs — a correctness risk (silent undefined-symbol link failures on whichever platform's JUCE module needs a system framework not manually re-exported) disproportionate to a build-time optimization mostly already absorbed by a working sccache. The duplicated Tauri release-build + `cargo test` Rust compilation (confirmed empirically: `cargo test` recompiles the identical dependency crates `npm run tauri build` just compiled, because the two use different Cargo profiles/`target/` subdirectories) was left unchanged for the same reason — its real fix is the same Rust/sccache question above, once genuinely resolved, rather than a separate profile-unification change that would risk silently changing test semantics (`debug_assertions`/overflow-checks differ between the release and test/dev profiles) | low | low | closed | **Closed:** the user pushed `2f1f02a` and ran it. Independently confirmed via `gh run view`/`gh api` (run `33597567001`) — `check` passed on both platforms, and Windows's plain, uninstrumented `sccache --show-stats` (no special diagnostic logging needed) now reports real, accurate cumulative stats matching the debug-log snapshot almost exactly: `Compile requests 759`, `Compile requests executed 615`, `Cache hits 522` (Rust), `Cache misses 82` (Rust), `Cache hits rate 86.42 %`, also surfaced via GitHub's own `##[notice]86% - 522 hits, 82 misses, 0 errors` annotation. macOS held its improvement with no regression (`870` requests, `99.17 %` overall). Both platforms' native `ctest` reported `100% tests passed out of 4`. `SCCACHE_IDLE_TIMEOUT=0` conclusively fixes the Windows Cargo/Rust stats-reporting artifact this risk tracked | EP-007, FT-015, TK-016

`RSK-007` | FT-013/TK-014 is the first time this Epic's pieces (schema/projection from TK-012, `materialize()` from TK-013, and the FT-009/TK-010 process-spawn/transport pattern) run together end to end in one real process — a new combination, even though none of the individual mechanisms is new. `native_runtime.cpp`'s connection design specifically splits "receive raw bytes" (off the message thread) from "call `materialize()`" (on the message thread, deliberately) to satisfy a constraint TK-013 only discovered by hitting it; a subtly wrong assumption in that specific wiring would not necessarily show up on macOS alone | low | med | closed | Locally verified end to end: the automated `test_first_sound.py` round trip passes against the real built `native_runtime` binary, and running the real `main.py` entry point produced a genuine, valid, correct-length WAV file with the expected peak amplitude. **Closed:** independently confirmed via downloaded job logs (`gh run view`, run `33528900894`, commit `602db7e`, not the user's report alone) — `check` passed on both `check (macos-latest)` and `check (windows-latest)`, and both logs show `pytest -m transport` actually collecting and running `tests/test_first_sound.py`, printing `2 passed, 14 deselected` on each platform. The full end-to-end combination this risk tracked now has real, independently-verified cross-platform evidence, not just a passing job conclusion | EP-005, FT-013, TK-014

`RSK-006` | FT-012/TK-013 introduces the Epic's first genuinely new native *behavioral* surface (not just a build-toolchain change): a real headless `tracktion::Engine`, `Edit` construction, `insertWaveClip`, and `EditRenderer::render` against a synthetic audio fixture — including a just-discovered lazy MIDI-input-device scan triggered during render setup, unrelated to the already-known `autoInitialiseDeviceManager()` audio-device gate. This project's own history (RSK-002/003/004) is direct precedent for real device/driver-adjacent behavior differing across macOS and Windows in ways local reasoning cannot predict | med | med | closed | Locally verified end to end on macOS: `materializer_proof` renders deterministically (forced single-threaded via `EngineBehaviour::getNumberOfCPUsToUseForAudio() = 1`), producing correct sample counts and consistent peak amplitude across two independent runs with identical input, with zero new warnings. The render pipeline needed a real fix already (dispatch-loop pumping while waiting for the background render thread, discovered by instrumentation after an initial hang, not assumed) — direct evidence this surface behaves in genuinely non-obvious ways worth treating cautiously. **Closed:** independently confirmed via downloaded job logs (`gh run view`, run `33528900894`, commit `602db7e`) — the real Windows log shows `4/4 Test #4: materializer_proof ............... Passed 1.17 sec` inside `100% tests passed out of 4`, i.e. the real Tracktion `Edit`/render/device-manager surface (including the MIDI-scan-during-render behavior) genuinely ran and rendered deterministically on Windows, not just macOS | EP-005, FT-012, TK-013

`RSK-005` | EP-005's Protobuf schema (`project.proto`, TK-012) uses nested, repeated composite messages (`Project.tracks`, `AudioTrack.clips`) — a materially different shape of protoc-generated code (arena-backed repeated-field/sub-message accessors) than `proof.proto`'s single flat message, which is what FT-008/TK-009's real Windows CRT/ABI fixes (RSK-002) were actually validated against. Assuming RSK-002's closure covers this new shape by analogy, without real evidence, is exactly the mistake this Task family has already been burned by twice | low | med | closed | Same CMake pattern reused exactly (`project_proto` mirrors `proof_proto`'s custom-command shape and `PUBLIC cxx_std_17` requirement precisely); `project_schema` (mirroring `proof_schema`) round-trips a full nested command/event locally with zero warnings on a clean build. **Closed:** independently confirmed via downloaded job logs (`gh run view`, run `33528900894`, commit `602db7e`) — the real Windows log shows `3/4 Test #3: project_schema ................... Passed 0.01 sec` inside `100% tests passed out of 4`, i.e. the nested/repeated message shape genuinely builds, links, and round-trips on real Windows CI, not by analogy to RSK-002's unrelated closure | EP-005, FT-011, TK-012

`RSK-001` | Tracktion Engine + JUCE native build/integration across macOS and Windows may prove substantially more difficult or expose platform-specific issues, affecting FT-003/EP-002/Pre-alpha timing | med | med | closed | Materialized once (Windows CI failed under MinGW auto-detection) and was fixed by dropping `-G Ninja` from `check-native` (TK-006). A real GitHub Actions run now passes on both macOS and Windows — the risk this entry tracked (native build/integration proving substantially more difficult or platform-specific) did not hold beyond the one fixed issue; no further platform-specific native build risk is currently open | EP-001, FT-003, EP-002, FT-005, TK-006
`RSK-002` | Adding a Rust/Tauri build toolchain (ADR-006) and Protobuf codegen integrated into both the CMake native build and the uv-managed Python project (ADR-007) — two new cross-platform build-toolchain surfaces at once — may expose macOS/Windows-specific integration issues similar in kind to RSK-001's, affecting EP-004 timing | med | med | closed | Rust/Tauri half remains fully proven on both platforms (TK-008, confirmed by a real passing GitHub Actions run). The Protobuf half **materialized**: the real Windows `check` run on `bef35c2` (TK-009's commit) failed at link time — `error LNK2038: mismatch detected for 'RuntimeLibrary': value 'MT_StaticRelease' doesn't match value 'MD_DynamicRelease' in proof_schema.obj` (156 errors across 50 `libprotobuf.lib` objects) — and separately the job ran ~53 minutes (macOS: ~15 min, unaffected). Root cause of the link failure, confirmed by reading protobuf's own `CMakeLists.txt` at the vendored tag: with `protobuf_BUILD_SHARED_LIBS` off (our case), protobuf unconditionally sets the directory-scoped `CMAKE_MSVC_RUNTIME_LIBRARY` to static (`MultiThreaded...` without `DLL`) whenever `protobuf_MSVC_STATIC_RUNTIME` (a `cmake_dependent_option` defaulting **ON** for static libs) is on — forcing `/MT` on every protobuf/abseil target while our own targets, defined in a sibling directory scope that never sets this variable, keep CMake's own default `/MD`. Root cause of the duration regression, confirmed empirically: (a) protobuf's `FetchContent` did a full-history git clone (measured locally: 2m35s / 355MB) where a shallow clone at the pinned tag is 12.5s / 118MB; (b) `protobuf_BUILD_PROTOC_BINARIES` (default ON) unconditionally forces `protobuf_BUILD_LIBPROTOC` on, and upstream's `libprotoc` target bundles **every** language's codegen backend (java/kotlin/objectivec/php/python/ruby/rust/csharp/upb) as one inseparable target — confirmed by the compile log and by reading `protoc.cmake`/`libprotoc.cmake` — even though Corytm only ever needs `--cpp_out`; (c) without `EXCLUDE_FROM_ALL`, `cmake --build`'s default `ALL` target also builds abseil's full ~200-target tree (test/debug utilities we never link against), not just the ~70 targets `libprotobuf` actually uses. Fix applied and locally re-validated on macOS (TK-009 reopened, see its Notes for the full fix and verification): `protobuf_MSVC_STATIC_RUNTIME OFF` (fixes the CRT mismatch; a no-op on non-MSVC, confirmed by reading the same upstream logic — it is unconditional on platform, but `CMAKE_MSVC_RUNTIME_LIBRARY` itself is only consulted by MSVC); `protobuf_BUILD_PROTOC_BINARIES`/`protobuf_BUILD_LIBPROTOC`/`protobuf_BUILD_LIBUPB` all `OFF` (confirmed `libprotobuf.cmake` has zero dependency on libupb); `protoc` obtained instead via upstream's official prebuilt release binaries (`protoc-36.1-osx-aarch_64.zip` / `protoc-36.1-win64.zip`, SHA256-pinned) rather than built from source; `GIT_SHALLOW TRUE` and `EXCLUDE_FROM_ALL` (requires the `cmake_minimum_required` bump to 3.28, well within both the local 4.4.3 and CI's `lukka/get-cmake@v4.4.2` toolchains) added to protobuf's `FetchContent_Declare`. A genuinely clean local macOS rebuild (post-fix) confirmed: no `libprotoc`/`protoc`/`protoc-gen-upbdefs`/`protoc-gen-upb_minitable`/unused-absl targets are built at all (verified by diffing the actual target list); configure dropped from 347.7s to 204.1s; full clean configure+build+ctest completed in under 9 minutes with both proofs passing. **Update, next real Windows run (`567f353`, this fix's commit):** independently confirmed via `gh run view`/`gh api` (run `33483815957`) — the CRT fix worked: zero `LNK2038` occurrences anywhere in the log. Duration improved materially (macOS 10m58s; Windows 31m19s, down from ~53 min) but a **new** link failure surfaced further into the build: `proof.pb.obj : error LNK2001: unresolved external symbol "class google::protobuf::internal::GlobalEmptyStringConstexpr const google::protobuf::internal::fixed_address_empty_string"`, `proof_schema.exe : fatal error LNK1120: 1 unresolved externals`; `libprotobuf.vcxproj` itself built successfully. Root cause, confirmed by reading protobuf `v36.1`'s `src/google/protobuf/port.h` and by a local standalone reproducer against libc++: `fixed_address_empty_string`'s actual type is chosen per-translation-unit via an SFINAE probe (`GlobalEmptyStringConstexpr::HasConstexprDefaultConstructor`) whose result depends on the active C++ standard — false under C++17, true under C++20, confirmed identically on libc++ too. `proof_schema` was compiled at `cxx_std_20` while `libprotobuf` is forced to `cxx_std_17` by protobuf's own `protobuf_configure_target`, so the two TUs disagreed on the symbol's type/mangled name. This is a genuine ODR violation on **both** platforms; MSVC's decorated names encode a global variable's type and correctly refused to link the mismatch, while the Itanium C++ ABI (macOS/Linux) does not encode a plain variable's type in its mangled name, so the same violation silently "succeeded" on macOS with no build-time symptom (see `.claude/memory/protobuf-generated-code-cxx-standard-abi.md`). Fix: `proof_schema`'s `target_compile_features` changed `cxx_std_20` → `cxx_std_17`, matching `libprotobuf`'s own forced standard exactly — confirmed via `flags.make` locally (`-std=gnu++17` on both sides now) and a clean rebuild passing both `ctest`s. For duration, protobuf's `FetchContent` was additionally switched from a git clone (`GIT_REPOSITORY`/`GIT_TAG`/`GIT_SHALLOW`) to the official source tarball (`URL`/`URL_HASH`, matching the already-established `protoc_binary` pattern) — file count is nearly identical either way (~3500 files, confirmed by direct comparison), so this is a reproducibility/simplicity improvement more than a proven speed win; local timing was inconclusive (204s → 432s across repeated local runs, most plausibly network variance) and Windows CI itself showed no measurable configure-time improvement from `GIT_SHALLOW` alone in the prior run either (347.7s → 355.3s) — the real Windows configure-time bottleneck is most likely per-file checkout/write overhead (~3500 files either way, none of `GIT_SHALLOW`/tarball reduce file *count*, only history/transport overhead), which would require a real sparse-checkout mechanism to address and was not attempted this session (higher complexity/risk than justified before correctness is confirmed). **Closed.** The user pushed the ABI fix as `0847885` and ran it: independently confirmed via `gh run view`/`gh api` (run `33489961970`) — `check` passed on both `check (macos-latest)` (13m15s) and `check (windows-latest)` (30m33s), zero link errors of any kind. Both toolchain halves this risk tracked (Rust/Tauri, Protobuf/CMake+uv) are now genuinely proven cross-platform by real, independently-verified CI evidence — not a local-only claim. Windows duration went ~53min → ~31min → ~30.5min: the CRT/build-surface fix (TK-009's first reopening) delivered the real improvement; the second fix's tarball-vs-git swap, honestly flagged as unproven for speed at the time, made no further measurable difference — consistent with that session's own prediction that the real remaining floor is per-file checkout overhead, not addressed by either change. Not chased further: correctness was the priority and is now confirmed; CI duration was never this risk's subject (only cross-platform correctness was) and, at ~13/~31 min, is not itself blocking further EP-004 work | EP-004, FT-007, FT-008, TK-008, TK-009, ADR-006, ADR-007
`RSK-003` | The ADR-007 transport proof (FT-009/TK-010) adds a genuinely new cross-platform surface — Python's `asyncio` subprocess spawning/socket I/O talking to a JUCE `InterprocessConnection` client over a real OS loopback socket — that has only been exercised on macOS from this session; RSK-002's own history (two real, platform-specific-only failures caught solely by Windows CI, invisible on macOS) is direct precedent for not assuming this is fine without real confirmation | low | med | closed | Closed on real, independently-confirmed evidence, not assumption: the real GitHub Actions run on `c7a0919` (`gh run view`/`gh api`, run `33496256757`) shows `tests/test_transport_proof.py .` / `1 passed` on **both** `check (macos-latest)` and `check (windows-latest)` — the actual round trip ran and passed on Windows, not merely "the job succeeded." Both mitigating factors held: JUCE's socket/thread code paths worked identically to macOS, and Python's Proactor-default event loop supported the subprocess+loopback-socket combination with no special handling needed. Warning profile checked too (per the same session's request): no warnings from `transport_proof.cpp`/`proof_proto` on either platform, and the overall third-party warning counts/types match `.claude/memory/native-build-third-party-warnings.md` exactly, including with `juce_events`/`juce_core` newly compiled for this Task — no new category introduced | EP-004, FT-009, TK-010, ADR-007
`RSK-004` | The Tauri sidecar proof (FT-010/TK-011) adds another genuinely new cross-platform surface, untested on Windows from this session: a Rust test spawning `uv` (found via inherited `PATH`) as a child process via `tauri-plugin-shell`, itself spawning Python, with a stdout-line-based readiness signal and stdin-based shutdown message. One specific, real risk was already found and fixed locally (see below) precisely because this pattern hadn't been exercised before — direct precedent (RSK-002, RSK-003) against assuming the rest is fine without real confirmation | low | med | closed | A genuine bug was already caught and fixed during this session's own DISCOVER/IMPLEMENT, not merely anticipated: `tauri-utils`' `read_line` (confirmed by reading its actual source) includes the line delimiter byte in what it delivers, so an exact `line == b"READY"` comparison silently never matched and hung until timeout; fixed with a `.trim()`-based comparison chosen specifically because it also tolerates Windows' CRLF text-mode stdout translation, which macOS/Linux never exercises — but that CRLF handling itself remains unconfirmed on real Windows. `Command::new("uv")`'s `PATH` resolution from within a Rust-spawned test process is new combined surface (this session has run `uv` directly from a shell many times, and has run Rust-spawned subprocesses via JUCE/C++, but never a Rust test spawning `uv` specifically) even though `check.yml`'s Windows job already relies on `uv` being on `PATH` for its own steps. **Update:** a real Windows failure did occur, and it materialized as a genuinely different mechanism than either anticipated risk above — not the `read_line`/CRLF issue (already fixed) and not `PATH` resolution, but `STATUS_ENTRYPOINT_NOT_FOUND` (0xc0000139) on process *launch* of the `cargo test` harness itself, before any test code runs. Confirmed via `gh run view`/`gh api` (run `33501836929`) and via real upstream evidence (`tauri-apps/tauri` issues #11028/#13419/#13954, and Tauri's own real `examples/api/src-tauri/build.rs`, fetched directly): a known, documented upstream Tauri limitation — `tauri-build`'s automatic Windows manifest embedding (declaring the `Microsoft.Windows.Common-Controls` 6.0.0.0 dependency) is scoped per-`[[bin]]`-target and never reaches Cargo's auto-generated test harness for a `[lib]` target, so the harness loads the legacy COMCTL32 v5 and fails at the loader level when linked-in Tauri Windows-UI code references a v6-only export — unrelated to this project's own dependency choices. Fixed by replicating Tauri's own proven pattern in `build.rs`/`windows-app-manifest.xml` (see TK-011's Notes for the full mechanism and sourcing). Locally verified as far as macOS allows (`cargo check --target x86_64-pc-windows-msvc` clean; no regression in `make check`); the actual Windows loader behavior remained unconfirmed. **Closed:** the user pushed the fix as `41d2b9c` and ran it. Independently confirmed via `gh run view`/`gh api` (run `33512738298`), not taken on the user's report alone: `check` passed on both `check (macos-latest)` (18m14s) and `check (windows-latest)` (39m6s), and the Windows log shows the test harness genuinely starting this time and printing `test tests::sidecar_shuts_down_via_explicit_protocol ... ok` — the exact executable that previously died at the loader level before any test code ran. Full-log searches for `STATUS_ENTRYPOINT_NOT_FOUND` and for `duplicate resource`/`LNK1`/`LNK2`/`fatal error` (the two failure modes this fix specifically targeted, including issue #10154's bin-scoped-plus-bare-embed combination) returned zero matches on either platform. Warnings checked too: none touch `build.rs`, `windows-app-manifest.xml`, `lib.rs`, `sidecar_proof.py`, `Cargo.toml`, or the `Makefile` on either platform; the MSVC `libprotobuf` warnings present on Windows pre-date this Task (already present in the prior passing run, `0847885`/`33489961970`). Both risks this entry tracked (the read_line/CRLF shutdown-signal risk and the loader-level manifest gap) are now genuinely resolved by real cross-platform evidence | EP-004, FT-010, TK-011, ADR-006
