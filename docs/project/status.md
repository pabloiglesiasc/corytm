# Project Status

Last updated: 2026-09-03

**Lifecycle phase:** Alpha (transitioned from Pre-alpha, 2026-08-31, on explicit user approval per `CLAUDE.md` §5)
**Active milestone:** MS-002 — First Sound (status: active)
**Active Feature:** none — awaiting selection. FT-026 (below) closed `done`; FT-025 (below) is `validating` again pending fresh CI.
**Validating Feature:** FT-025 (Add Audio Track and Add Audio Clip Commands and UI), under EP-012 (Real Track/Clip Authoring). Two real, platform-specific bugs found by a real Windows CI failure (run `33693602173`) were root-caused and fixed: (1) a Tauri ACL local-origin mismatch (`invoke_request`'s test helper hardcoded `tauri://localhost`, but Windows' real local origin is `https://tauri.localhost`, confirmed against Tauri 2.11.5's own source/test suite); (2) an under-provisioned test timeout (the idle-gap stress test's 60s bound left too little margin for its real, now-measured ~25-29s local cost across ten real `native_runtime` spawns, widened to 180s — no evidence of an actual deadlock). Both fixes verified locally end to end (`make check-python`, `check-transport`, `check-desktop`, plus a Windows cross-target `cargo check`); full diagnosis in `RSK-017` and FT-025's own Notes. The fix commit (`5875da3`) is confirmed pushed to `origin/main`; a fresh `check` Actions run (`33719557924`) is genuinely `in_progress` on both platforms as of this entry, not yet resolved.
**Blockers:** none currently open.

**FT-026 (Wire Dorian's move_clip Tool to a Live Groq Model), under EP-013 (Dorian's First Live Model Call), is done (2026-09-03).** `corytm.dorian.providers` gives Dorian a provider-neutral `ModelProvider` boundary and its first concrete adapter (`GroqProvider`, Groq-hosted GPT-OSS, per the user's explicit, protected decision recorded in `decisions/ADR-013.md`); `corytm.dorian.agent.propose_move_clip` wires `move_clip`'s existing `MoveClipProposal` through it, reusing ADR-004's validate-then-execute boundary unchanged. The user ran the human-run live evaluation with a real `GROQ_API_KEY` — `2 passed` against the default model (`openai/gpt-oss-20b`), confirming GPT-OSS 20B reliably selects `move_clip` and produces valid arguments for both representative instructions; ADR-013's `openai/gpt-oss-120b` fallback was not needed. Both required evidence items (delivery commit pushed, human live evaluation) now read confirmed/passed; no fresh cross-platform CI was required (pure-Python, per EP-008's own precedent). **EP-013 is done** — MS-002's Outcome's Dorian-semantic-edit half is now genuinely, fully evidenced; only its manual-editing half (FT-025/EP-012 above) remains open.

## Recent meaningful progress

FT-004/EP-001 done (root `Makefile` unifying Python/frontend/native checks). FT-005/TK-006 diagnosed and fixed a real Windows CI failure (CMake's `-G Ninja` auto-detected MinGW instead of MSVC; fixed by dropping `-G Ninja` so CMake uses its per-platform default generator). A real GitHub Actions run now passes on both macOS and Windows. `RSK-001` closed. EP-002 (Continuous Integration & Platform Validation) is done — its Acceptance criteria (CI running and passing on both platforms) is genuinely satisfied. EP-003 (Harness Validation) is done: a genuinely fresh session (no prior conversation history) refined it into FT-006/TK-007, found and fixed a real documentation-accuracy defect (`.claude/memory/README.md`'s "Current state" section falsely claimed zero memory entries existed, when three already did), validated the fix with `make check`, and synchronized PMO state at close. MS-001 (Self-Driving Repository) was achieved on that evidence. The user then explicitly approved the Pre-alpha → Alpha lifecycle transition (`CLAUDE.md` §5); `CLAUDE.md` §1 and `docs/project/plan.md`'s Current Phase were updated accordingly, and MS-002 (First Sound) was created as Alpha's first Milestone. Two foundational architecture decisions blocking all of MS-002 were then researched (two parallel research agents, one retried after a transient failure) and, with the user's explicit approval, recorded as ADR-006 (Tauri 2 as the desktop shell) and ADR-007 (Protobuf schema over a local loopback-socket transport for Python↔Native Audio Runtime, with the socket/framing implementation kept explicitly separable from the durable contract per the user's clarification). `docs/technical/architecture.md` was synchronized to both decisions. MS-002 is now `active`, and EP-004 (Alpha Platform Foundation) was created as its first, `ready` Epic — proving both ADRs work in practice before any real product/domain behavior is built on them. A harness review then found root `README.md` still claiming Pre-alpha after the lifecycle transition (fixed then — ordinary PMO-owned doc sync, not a protected change) and no explicit requirement that Task/session CLOSE hand off uncommitted changes to the human, plus a stale `CLAUDE.md` §8 typing claim ("none of this is configured yet", though Pyright strict and TypeScript `strict: true` were already configured and passing). Both were proposed, explicitly approved by the user per `CLAUDE.md` §5, and applied in the same commit as the Alpha transition and ADR-006/ADR-007 (`7ff9f4b`, "Enter Alpha and establish architecture decisions") — already committed and pushed to `origin/main` before this entry was first written. §7's Close bullet and §8's typing paragraph, as currently written in `CLAUDE.md`, are that approved, applied state, not a pending proposal.

EP-004 execution began: FT-007 (Tauri Desktop Shell) was created under EP-004, refined into TK-008, and both closed done. A local Rust toolchain (`rustup`, stable) was installed; a Tauri 2 project was scaffolded under `src/frontend/desktop/src-tauri/`, its CLI consumed as the npm devDependency `@tauri-apps/cli` (not a separately-installed global tool) so `make check`'s new `check-desktop` target has no hidden machine prerequisite. Both `npm run tauri build` (release bundle: `corytm.app` + a `.dmg`) and `npm run tauri dev` (debug, against the Vite dev server) were verified locally, hosting the existing frontend unchanged. `npm run tauri dev` was directly confirmed by the user, who saw a real window rendering the existing "Frontend toolchain proof" text — genuine human visual confirmation that ADR-006 works in practice. `.github/workflows/check.yml` now installs Rust and attempts the same build on both `macos-latest` and `windows-latest`, but — per `CLAUDE.md` §10, this session cannot push — an actual passing Windows CI run remains unconfirmed, mirroring TK-006's precedent exactly. `RSK-002` moved `open → monitoring` (the Rust/Tauri half is now proven; Protobuf/CMake codegen, the other half, is untouched). A local-only DMG-bundling quirk (a stale mounted disk image blocking a second back-to-back `tauri build`) was diagnosed and captured in a new memory entry (`.claude/memory/tauri-dmg-stale-mount.md`) rather than requiring any code change.

This TK-008 close-out report initially misstated the §7/§8 `CLAUDE.md` amendments above as still pending the user's approval, and named "FT-008"/"FT-009"/"FT-010" in `FT-007.md`, `TK-008.md`, and `risks.md` as if they were already-created records — neither was true. The root cause: this paragraph's own prior wording (written before the amendments were approved) was carried forward and read as still current without cross-checking it against `CLAUDE.md` itself — the higher-authority source per `CLAUDE.md` §4 — which already reflected both amendments as applied; the fabricated Feature IDs were this session's own shorthand for not-yet-created future work, written as bare IDs without flagging that they weren't real. Corrected in place across all four files on the user's flag.

This session independently confirmed, via `gh run view` against the real GitHub Actions run on `e095d54` (the TK-008 commit) rather than taking the user's report on faith, that `check` passed on both `check (macos-latest)` and `check (windows-latest)`. `RSK-002` was updated accordingly: the Rust/Tauri half of that risk is now fully proven cross-platform, closing out TK-008's one remaining unconfirmed item. FT-008 (Protobuf Schema & Bindings) and TK-009 were then created under EP-004, per the prior session's recommendation, to prove RSK-002's other still-unproven half (Protobuf/CMake and uv codegen) before any sidecar/transport work, and both were closed done in the same session. `src/schemas/proof.proto` (one illustrative `ProofMessage`) now proves the codegen pipeline on both sides: Protobuf is vendored into the CMake native build via `FetchContent` (`v36.1`, pulling in abseil-cpp transitively and automatically through protobuf's own CMake), generating and linking real C++ bindings exercised by a new `proof_schema` ctest; the Python core generates matching bindings via a `uv`-managed dev-only codegen tool (`grpcio-tools`, used only for its bundled `protoc`), with `protobuf` itself added as the ADR-007-anticipated runtime dependency, exercised by a new `pytest` round-trip test. Both proofs construct, serialize, deserialize, and assert equality; both pass locally, and full `make check` (Python, native, and frontend/desktop) passes locally end to end. `RSK-002` is now fully closed out on both toolchain halves — only an actual passing Windows CI run for this new protobuf/abseil surface remains unconfirmed from this session (`CLAUDE.md` §10), mirroring TK-006/TK-008's precedent exactly; no CI workflow change was needed since nothing introduces a new system-level prerequisite. `docs/technical/architecture.md`'s Python↔C++ Boundary section was updated to reflect that the schema file now exists as a toolchain proof, distinct from the still-unresolved real product message definitions. A this-session-only environment quirk (this Bash tool's shell not having `~/.cargo/bin` on `PATH`, unrelated to any repository defect) was diagnosed while validating `check-desktop` and captured in a new memory entry rather than requiring any code change.

The previous paragraph's "`RSK-002` is now fully closed out on both toolchain halves" was premature and turned out to be wrong: the user pushed `bef35c2` (TK-009's commit) and the real Windows `check` run genuinely failed — a `LNK2038` C-runtime-library mismatch (protobuf/abseil built `/MT`, our own targets `/MD`) plus the job taking ~53 minutes. This session diagnosed both from real CI evidence (`gh run view`/`gh api`, not the user's report alone) and from reading protobuf's own upstream `CMakeLists.txt` at the vendored tag rather than guessing: `protobuf_MSVC_STATIC_RUNTIME` defaults ON for static libs and forces `/MT` via a directory-scoped `CMAKE_MSVC_RUNTIME_LIBRARY` override that never reaches our own targets; separately, wanting `protoc` at all unconditionally forces the full multi-language `libprotoc` (java/kotlin/objectivec/php/python/ruby/rust/csharp/upb, none of which Corytm uses), and `FetchContent` was doing a full-history clone of protobuf where a shallow one is ~12x faster (measured: 2m35s/355MB vs 12.5s/118MB). `TK-009` and `FT-008` were reopened (`done` → `active`) rather than left standing against this contrary evidence, per `CLAUDE.md` §4/§7 and the user's explicit instruction to do so honestly. The fix — `protobuf_MSVC_STATIC_RUNTIME`/`protobuf_BUILD_PROTOC_BINARIES`/`protobuf_BUILD_LIBPROTOC`/`protobuf_BUILD_LIBUPB` all off, `protoc` obtained from protobuf's own official prebuilt release binaries (SHA256-pinned) instead of built from source, `GIT_SHALLOW TRUE` and `EXCLUDE_FROM_ALL` added (`cmake_minimum_required` bumped 3.22→3.28 to support it) — is applied and locally re-validated on macOS from a genuinely clean state: configure 347.7s→204.1s, a clean build in 5m37s with confirmed no `libprotoc`/`protoc`/language-backend/unused-`absl` targets built at all, both `ctest`s passing, and a full `make check` passing end to end. Full diagnosis and evidence are in TK-009's Notes. What this session cannot verify (`CLAUDE.md` §10): whether `LNK2038` is actually gone on real MSVC, whether the downloaded `win64` `protoc.exe` genuinely runs on Windows, and the real Windows duration after the fix — all pending the next human-triggered Windows Actions run.

The user pushed that fix as `567f353` and ran it: independently confirmed via `gh run view`/`gh api` (run `33483815957`) that the CRT fix genuinely worked (zero `LNK2038`) and duration improved (macOS 10m58s pass; Windows 31m19s, down from ~53 min) — but Windows still failed, further into the build, with a **new** error: `proof.pb.obj : error LNK2001: unresolved external symbol ... fixed_address_empty_string`, `proof_schema.exe : fatal error LNK1120: 1 unresolved externals`. Diagnosed from protobuf `v36.1`'s actual `src/google/protobuf/port.h`, not guessed: `fixed_address_empty_string`'s type is chosen per-translation-unit by an SFINAE probe of `std::string`'s constexpr-default-constructibility, whose result depends on the active C++ standard — confirmed with a standalone reproducer against this machine's own libc++ (`false` under C++17, `true` under C++20). `proof_schema` was set to `cxx_std_20` while `libprotobuf` is unconditionally forced to `cxx_std_17` by protobuf's own CMake (`protobuf_configure_target`); the two TUs disagreed on the symbol's real type — a genuine ODR violation. Checked (not assumed) why this never broke macOS despite the exact same standard split existing there the whole time: MSVC's decorated names encode a global variable's type and correctly refused the mismatched link; the Itanium C++ ABI (macOS/Linux) does not encode a plain variable's type in its mangled name, so the identical violation links silently there — macOS's passing build was never actually correct, just undetected. `protoc`-vs-source version compatibility, which the user specifically asked to rule out first, checked out clean: both are exactly `v36.1`, confirmed via `protoc --version` (`libprotoc 36.1`) and the generated file's own embedded runtime-version comment. Fix: `proof_schema`'s `target_compile_features` changed from `cxx_std_20` to `cxx_std_17`, matching `libprotobuf` exactly — no linker workaround of any kind. For duration, protobuf's `FetchContent` was switched from a git clone to the official source tarball (`URL`/`URL_HASH`, matching the already-established `protoc_binary` pattern); checked first that this doesn't just relocate the cost: file count is nearly identical either way (~3500 files), so this is applied as a legitimate simplification (fewer moving parts, an immutable hash-pinned artifact) rather than a proven speed win — local timing was inconclusive across repeated runs, and Windows CI itself showed no measurable configure-time gain from `GIT_SHALLOW` alone in the prior run either, pointing at per-file checkout overhead (not addressed by either shallow-clone or tarball, both of which fetch the same file count) as the likely real floor; a genuine sparse-checkout was considered and deliberately not attempted this session, as higher-risk/harder-to-validate than justified before correctness lands. Locally re-validated on macOS from a genuinely clean state (both changes together): clean build in 6m30s, `proof_schema`'s own compiler flags now show `-std=gnu++17` matching `libprotobuf`, both `ctest`s pass, full `make check` passes end to end. TK-009/FT-008 remain `active` — this session has now been surprised twice by evidence macOS cannot produce, so it is being explicit that only a real passing Windows Actions run will close this out.

The user pushed that second fix as `0847885` and ran it: independently confirmed via `gh run view`/`gh api` (run `33489961970`) — `check` passed on **both** `check (macos-latest)` (13m15s) and `check (windows-latest)` (30m33s), zero link errors anywhere. TK-009 and FT-008 are now genuinely closed `done`; `RSK-002` closed. Windows duration across the whole saga: ~53min → ~31min → ~30.5min — the real win was the first reopening's build-surface reduction; the second reopening's git→tarball swap made no further measurable difference, matching its own honest prediction at the time. Before closing, reviewed the passing macOS log's warnings as instructed (classify, don't reflexively suppress or clean up): confirmed by grepping actual file paths that every warning is in vendored `_deps/*-src` (JUCE HarfBuzz `-Wnontrivial-memcall` ×20, Tracktion Engine `-Wimplicit-const-int-float-conversion` ×14, protobuf's own `-Wdeprecated-declarations` ×15, one protobuf-owned CMake `CMP0141` policy warning, one informational npm `fsevents` notice) — none in Corytm-owned code, so none were touched; full breakdown in `.claude/memory/native-build-third-party-warnings.md`. EP-004's next piece was then selected per the Next-Work Selection Priority (`docs/project/plan.md`): the ADR-007 transport round trip (FT-009/TK-010), not sidecar orchestration — chosen because it only depends on FT-008 (done) and can be proven as a self-contained Python↔native integration test without first solving Tauri's separate, Tauri-specific child-process shutdown problem, whereas sidecar orchestration is its own, differently-shaped piece of work. `src/backend/audio/transport_proof.cpp` (a JUCE `InterprocessConnection` client) and `src/backend/core/tests/test_transport_proof.py` (an `asyncio` server) were built reusing FT-008's `proof.proto` unchanged: the exact wire framing was read directly from JUCE's own `juce_InterprocessConnection.cpp` (an 8-byte little-endian magic+length header, confirmed rather than assumed) and replicated in Python; a new `proof_proto` CMake library compiles the generated protobuf code once and sets `cxx_std_17` as a `PUBLIC` requirement so any future target linking it inherits the correct standard automatically, structurally preventing a repeat of TK-009's exact ABI bug rather than relying on remembering to set a flag. A new `check-transport` Makefile target (pytest-marker-gated, sequenced after `check-native` since the native executable must already be built) exercises the real round trip: Python spawns the actual built `transport_proof` binary, sends the per-launch secret, receives it validated, sends a real `ProofMessage` command, and receives a real `ProofMessage` event back. Passed on the first real attempt with zero warnings on the new code; a full clean `make check` (all five targets) passes end to end. Given this exact Task family's own hard-earned lesson that a clean macOS build proves nothing about Windows, FT-009/TK-010 are deliberately left `active`, not `done`, and a new `RSK-003` was opened (not closed) to track the genuinely new, Windows-unverified surface (`asyncio` subprocess + loopback socket talking to a real JUCE client process) honestly rather than assume it's fine by analogy to unrelated already-proven pieces.

The user pushed that transport-proof commit as `c7a0919` and ran it: independently confirmed via `gh run view`/`gh api` (run `33496256757`) — `check` passed on both `check (macos-latest)` (10m37s) and `check (windows-latest)` (33m10s), and both logs show the actual round-trip test (`tests/test_transport_proof.py`, `1 passed`) genuinely ran and passed, on Windows too — not just the overall job. `asyncio.create_subprocess_exec`'s Windows Proactor default and the JUCE wire-format replication both worked exactly as reasoned, on the first real attempt. Checked the user's "no new Corytm-owned warnings" claim rather than accepting it: grepped both logs for warnings touching `transport_proof.cpp`/`proof_proto`/`test_transport_proof.py` (none), and confirmed the overall warning profile matches `.claude/memory/native-build-third-party-warnings.md` exactly even with `juce_events`/`juce_core` newly compiled for this Task. FT-009, TK-010, and `RSK-003` all closed. EP-004 now has three of its four scope items proven cross-platform (Tauri shell, schema/codegen, transport); only Tauri sidecar process orchestration (spawning Python + native as managed children, plus ADR-006's explicit shutdown protocol) remains, and DISCOVER on it begins next in this same session.

DISCOVER on sidecar orchestration found real reasons to narrow EP-004's original framing before implementing, not after: neither the Python core nor the Native Audio Runtime has an actual entry point to spawn yet, and `docs/project/plan.md` itself scopes "packaging/installers" to Beta, not Alpha — meaning the frozen-executable scenario ADR-006's Consequences names as `Child.kill()`-unreliable isn't something Alpha should be solving yet. `tauri-plugin-shell`'s real source was fetched (via a throwaway scratch `cargo` project, not guessed from memory or docs) and read directly, which settled two real design questions: `Shell::sidecar()` (bundled-binary, `externalBin`) is the wrong tool for now since it requires exactly the packaging investment being deferred, so plain `Shell::command()` was used instead; and Rust-side calls to `Shell::command()` bypass the JS-invoke ACL/capability system entirely (confirmed by reading the scope-check code), so no `capabilities/*.json` change was needed. FT-010/TK-011 were created scoped accordingly: prove spawn/communicate/explicit-stdin-shutdown against a minimal Python script (`src/backend/core/sidecar_proof.py`), automated via a genuine `cargo test` using `tauri::test::mock_builder()` (confirmed this genuinely spawns real child processes — only the windowing layer is mocked) rather than requiring manual/visual confirmation the way TK-008 partially did. Implementation surfaced two real, fixed-in-place issues: `tauri::test` needed the crate's own `test` feature enabled (added under `[dev-dependencies]` only, so production builds are unaffected), and a `CommandEvent::Stdout` exact-byte comparison against `b"READY"` silently hung until timeout because `tauri-utils`' `read_line` (read directly) includes the line delimiter in what it returns — fixed with a `.trim()`-based comparison chosen specifically for Windows CRLF tolerance, itself unconfirmed on real Windows. `cargo test` passed reliably across three runs with zero warnings; `make check-desktop` and a full clean `make check` (all six targets) passed end to end. FT-010/TK-011 stay `active`, not `done`; a new `RSK-004` tracks the untested Windows surface, consistent with this Task family's now-standard practice.

The user pushed that commit as `5acff08` and ran it: independently confirmed via `gh run view`/`gh api` (run `33501836929`) — macOS passed; Windows failed, but not inside the test. `npm run tauri build` (MSI/NSIS bundles included) succeeded immediately beforehand, `cargo test` finished compiling, and the produced harness `.exe` failed to even start 0.06s later: `STATUS_ENTRYPOINT_NOT_FOUND` (0xc0000139) — a loader-level failure, before any test code, distinct in kind from either risk RSK-004 had already named. Diagnosed from real evidence, not guessed: `cargo tree` comparison across `[dev-dependencies]` showed almost no dependency delta (ruling out "a new test-only crate needs a missing Windows API"); `WebSearch`/`WebFetch` against `tauri-apps/tauri`'s own issue tracker found this exact symptom is a known, reproducible upstream Tauri limitation, unrelated to this project's code; Tauri's own real, currently-shipping `examples/api/src-tauri/build.rs` and `windows-app-manifest.xml` (fetched directly via `gh api`, not paraphrased) confirmed the mechanism precisely: `tauri-build`'s automatic Windows manifest embedding (a `Microsoft.Windows.Common-Controls` v6 dependency declaration) is scoped per-`[[bin]]`-target and never reaches Cargo's auto-generated test harness for the `[lib]` target, so the harness loads legacy COMCTL32 v5 and fails when linked-in Tauri Windows-UI code references a v6-only export. Fixed by replicating Tauri's own proven pattern exactly (not inventing a new one): `build.rs` now uses `WindowsAttributes::new_without_app_manifest()` plus a manual, uniformly-applied `cargo:rustc-link-arg=/MANIFEST:EMBED` (deliberately avoiding the "bin-scoped embed plus bare embed" combination a separate real Tauri issue reports as a "duplicate resource" linker failure); `windows-app-manifest.xml` copied verbatim from Tauri's own shipped file. Verified as far as macOS allows: `cargo check --target x86_64-pc-windows-msvc` succeeds (after installing `llvm-rc` via `brew install llvm`, a pre-existing requirement unrelated to this fix); caught and corrected a real blind spot mid-verification — `#[cfg(windows)]` in `build.rs` reflects the *host*, not the `--target`, so the new function was silently compiled out of that first cross-check, so its body was separately typechecked natively by temporarily lifting the gate. `make check-desktop` and a full clean `make check` still pass on macOS (expected: manifest embedding is meaningless off Windows). FT-010/TK-011 remain `active`; `RSK-004` updated with this specific finding, not closed on local reasoning. EP-004 does not close and no new product-slice Task starts until a real Windows CI run genuinely executes `sidecar_shuts_down_via_explicit_protocol` and passes.

The user pushed the manifest fix as `41d2b9c` and ran it. Independently confirmed via `gh run view`/`gh api` (run `33512738298`), not taken on the user's report alone: `check` passed on both `check (macos-latest)` (18m14s) and `check (windows-latest)` (39m6s). Verified from the actual log text, not just the job conclusion: the Windows test harness (`app_lib-8536336b3aa50ae0.exe` — the exact binary that previously died at the loader level before printing anything) genuinely started this run and printed `test tests::sidecar_shuts_down_via_explicit_protocol ... ok`; full-log searches for `STATUS_ENTRYPOINT_NOT_FOUND` and for `duplicate resource`/`LNK1`/`LNK2`/`fatal error` (the two failure modes the fix specifically targeted) returned zero matches on either platform; macOS re-ran the same test with no regression. Checked warnings before closing, per this Task family's standing practice: zero touch `build.rs`, `windows-app-manifest.xml`, `lib.rs`, `sidecar_proof.py`, `Cargo.toml`, or the `Makefile` on either platform; the MSVC `libprotobuf` warnings present on Windows pre-date this Task (already present in the prior passing run, `0847885`/`33489961970`), and the macOS warning profile still matches `.claude/memory/native-build-third-party-warnings.md` exactly. FT-010, TK-011, and `RSK-004` all closed. EP-004 (Alpha Platform Foundation) is now fully done: all four Features (FT-007 Tauri shell, FT-008 Protobuf schema/codegen, FT-009 ADR-007 transport, FT-010 sidecar spawn/shutdown) are proven cross-platform by real, independently-confirmed CI evidence, and both ADR-006 and ADR-007 work in practice, not just on paper. `RSK-002`, `RSK-003`, and `RSK-004` are all closed. No new Epic was created this session: defining MS-002's next real product slice is a genuine scope decision (see "Next recommended Task" above), left for the next session to confirm with the user before committing to a shape.

The user then gave explicit approval for MS-002's next Epic's scope in detail: a real Python application entry point owning a minimal canonical project (one audio track, one audio clip) that crosses the ADR-007 transport into the Native Audio Runtime, where Tracktion Engine materializes it and produces genuine sound — proven deterministically offline in CI (no audio device) with a separate, human-only local audible smoke test as a distinct acceptance boundary — and asked this session to discover/refine and execute the Epic autonomously until a genuine protected decision, external-evidence boundary, or the human Git boundary. EP-005 (First Sound: Minimal Track and Clip Through Tracktion) was created under MS-002 and refined into three Features/Tasks, mirroring EP-004's own "prove one increment, then the next" shape: FT-011/TK-012 (Corytm Engine domain model — `Project`/`AudioTrack`/`AudioClip`, timing only — plus a real `project.proto` schema and a pure Runtime projection function, with the ADR-007 frame-read/write helpers deduplicated out of FT-009's test into real `modules/runtime/transport.py` production code); FT-012/TK-013 (a `materialize()` function proving, in isolation from protobuf/sockets, that a plain project description becomes a real Tracktion `Edit` — `Edit::createSingleTrackEdit`, `insertWaveClip` against a natively-generated sine-wave fixture, `Renderer::Parameters`/`EditRenderer::render` — rendered offline with no audio device); FT-013/TK-014 (a `wire_codec` protobuf adapter kept in its own translation unit specifically to preserve the `cxx_std_17`/Tracktion-C++-standard boundary TK-012 established, a new `native_runtime` executable, a real `session.py`/`main.py` Python entry point, and an automated end-to-end test proving the whole round trip against the real built binary).

Two real, non-obvious bugs were caught by running against reality rather than trusting analysis, both documented in TK-013's Notes: first, `EditRenderer::render`'s completion callback never fired — a real hang, diagnosed by temporary `std::cerr` instrumentation (removed once understood) rather than more guessing — because the render's background thread needed the calling thread's JUCE message loop actively pumped (a lazy MIDI-input-device scan, unrelated to the already-known audio-device-manager gate, was blocking on it); fixed by replicating `Renderer.test.cpp`'s own real `runDispatchLoopUntil`-based wait pattern instead of the originally-planned plain `condition_variable` wait. Second, `MaterializeProjectCommand::ParseFromArray`'s `[[nodiscard]]` return value was originally ignored, caught by a real compiler warning, not foresight; fixed by checking it and falling back to an empty `ProjectSpec` on parse failure, a real error path `materialize()` already handles correctly. `native_runtime.cpp`'s connection design deliberately splits "receive raw command bytes" (off the message thread, mirroring `transport_proof.cpp` exactly) from "call `materialize()`" (on `main()`'s own thread, which is the real message thread) specifically because of the first finding above.

All three Tasks were validated locally as thoroughly as this session can: each Feature's own isolated proof passes (`project_schema` ctest for the schema/codegen; `materializer_proof` ctest, run twice with identical input and matching sample count/peak amplitude, single-threaded via `EngineBehaviour::getNumberOfCPUsToUseForAudio() = 1` for determinism; `test_first_sound.py` for the full round trip), all 16 Python tests pass, `pyright` (strict, `pyproject.toml`'s `include`/`extraPaths`/`testpaths` extended to cover the new `modules/` and `main.py` — real permanent product code, not another proof script) and `ruff check`/`ruff format --check` pass clean, and — going beyond any individual Task — a full clean `make check` (all five targets: Python, frontend, desktop, native, transport) was run end to end from scratch and passed, with zero new warnings anywhere beyond the pre-existing, already-catalogued vendored third-party set. Running `main.py` directly for real (`PYTHONPATH=modules:generated uv run python main.py` from `src/backend/core/`, the exact invocation now documented in TK-014 since a bare script has no other way to reach the `modules/` package layout pytest's own config handles for tests) produced a genuine, valid RIFF/WAVE file — confirmed via the `file` command — reporting exactly 88,200 samples (2.0s × 44,100Hz) and a peak amplitude of 1.0000; the file was deleted afterward as a trivially-reproducible local artifact, not a repo deliverable.

Three new risks were opened rather than assuming EP-004's already-closed risks cover this new surface by analogy — this project's own hard-won lesson from `RSK-002`'s two real reopenings: `RSK-005` (FT-011's `project.proto` uses nested/repeated composite messages, a materially different shape of generated code than `proof.proto`'s flat message); `RSK-006` (FT-012's real Tracktion `Edit`/render/device-manager code paths, including the newly-discovered lazy MIDI-scan behavior — the Epic's first genuinely new native *behavioral* surface, not just a build-toolchain one); `RSK-007` (FT-013's first real combination of all of the above running together end to end in one process). `docs/technical/architecture.md`'s Python↔C++ Boundary and Unresolved sections were updated to reflect that `project.proto` now carries the first real, if still deliberately minimal, product message definitions — not the general case, and not superseding `proof.proto`, which continues to prove the codegen pipeline unchanged. TK-012/TK-013/TK-014, FT-011/FT-012/FT-013, and EP-005 all stay `active`: per `CLAUDE.md` §10 this session cannot push, so a real Windows CI run confirming this new native/behavioral surface — genuinely different in kind from anything EP-004 already proved — remains the one thing only a human pushing and a real cross-platform Actions run can supply. The one other thing this session genuinely cannot do itself is the human audible smoke test EP-005's own approved scope calls for as a distinct, non-automatable acceptance boundary.

The user then confirmed the human audible smoke test directly: ran the real EP-005 path on macOS (`PYTHONPATH=modules:generated uv run python main.py` from `src/backend/core/`), generated `first-sound.wav`, played it, and audibly confirmed the rendered tone — reported as "unpleasant but clearly audible and stable," which is expected and not a defect (`materialize()`'s fixture is a plain full-scale 440Hz sine, an internal proof-of-pipeline detail with no musical or product claim attached). Recorded as human acceptance evidence in EP-005, FT-013, and TK-014's Notes. Per the user's explicit instruction, this evidence does not by itself move any Task/Feature/Epic state to `done` — real Windows CI confirmation (`RSK-005`/`RSK-006`/`RSK-007`) remains the sole outstanding requirement before EP-005 closes.

A later session independently verified the real GitHub Actions run for EP-005's commit rather than taking the user's report alone, per the user's explicit instruction to do so — the same practice this project has followed for every prior cross-platform surface (TK-009/TK-010/TK-011). `gh run list` identified three green `check` runs after EP-005's work: `602db7e` ("Prove first sound: render a minimal project through Tracktion" — the actual implementation commit), `e9b5ad5` (recording the human audible evidence, docs-only), and `a8a5edd` (a WAV-ignore-rule/Git-handoff governance change, also docs/config-only). The implementation commit, `602db7e` (run `33528900894`), is the one that matters for behavioral evidence: both `check (macos-latest)` and `check (windows-latest)` completed successfully, and — going beyond the job conclusion — the actual downloaded log text was read and grepped to confirm the new EP-005 paths genuinely executed on both platforms, not just that the overall job passed: `project_schema` and `materializer_proof` both report `Passed` inside a real `100% tests passed out of 4` ctest summary on each platform, and `pytest -m transport` genuinely collects and runs `tests/test_first_sound.py` — the automated end-to-end First Sound path — printing `2 passed, 14 deselected` on both macOS and Windows. This is real, independently-confirmed cross-platform behavioral evidence, distinct in kind from the earlier human macOS audible confirmation: the CI evidence proves the automated round trip (sample count/peak amplitude assertions, on both platforms); the human evidence proves actual audibility, which no automated test can. Both are now recorded, kept explicitly distinct rather than merged into one acceptance statement. On this evidence, TK-012/TK-013/TK-014, FT-011/FT-012/FT-013, EP-005, and `RSK-005`/`RSK-006`/`RSK-007` were all reconciled to `done`/`closed` (see each record's own Notes for the specific log lines cited).

Per the user's explicit instruction, the next MS-002 Epic is deliberately not being defined yet. Before that, a repository-structure architecture audit was performed: reconstructing the originally-intended architecture from repository evidence (`CLAUDE.md` at every commit, accepted ADRs, `docs/technical/architecture.md` at every commit, `docs/project/plan.md`, full git history, existing code) and comparing it against the structure that actually emerged through EP-005. Finding: the coarse source layout (`src/{frontend, backend/{core/modules/{engine,dorian,runtime}, audio}}`) was fixed in `CLAUDE.md`/`architecture.md` at the bootstrap commit, before any code existed, and matches the current tree exactly — no drift there. A finer-grained convention the user recalled designing (domain modules split into models/services/repositories/tests, and a native `src/include/tests` separation) was never found anywhere in the repository's history (a full `git log --all -p` grep for the relevant terms returned zero matches) — it was never encoded, so nothing was superseded; `CLAUDE.md` §8's own standing anti-premature-abstraction rule (present since bootstrap, unchanged) already governs the Python side correctly as-is at its current small scale. The one real gap found: the Native Audio Runtime (`src/backend/audio/`) had grown to 11 files with production libraries and one-off proof/ctest executables mixed flat in one directory, with no repository artifact ever having stated a convention for the distinction.

With the user's explicit approval, two changes were made as non-protected housekeeping arising from the audit (no protected-boundary decision was overridden; both were confirmed with the user first per `CLAUDE.md` §5):
1. `CLAUDE.md` §8's "cross-component tests get a dedicated future home" was reworded to name `src/backend/core/tests/` directly, since that home now exists and is populated — wording only, no rule change.
2. The Native Audio Runtime's five proof/ctest executables (`toolchain_proof.cpp`, `proof_schema.cpp`, `project_schema.cpp`, `transport_proof.cpp`, `materializer_proof.cpp`) were moved into a new `src/backend/audio/tests/`, `CMakeLists.txt`'s executable source paths updated accordingly, and `materializer` given a `PUBLIC` include directory so `materializer_proof.cpp` can still reach `materializer.h` by its existing quote-include. Re-verified locally end to end: a full `make check-native` rebuild passed all four ctests (`toolchain_proof`, `proof_schema`, `project_schema`, `materializer_proof`) and built `native_runtime` cleanly, with zero new warnings anywhere (confirmed by grepping the rebuild log for the moved/changed files specifically). The resulting convention — production surface flat at the directory root, proof/ctest executables under `tests/`, public headers exposed via `target_include_directories` rather than relative includes — is now documented in `docs/technical/architecture.md`'s new "Native Audio Runtime Source Layout" section so future native Tasks have a rule to follow. No models/services/repositories subdivision was applied anywhere (Python or native): at current file counts, that would be exactly the premature abstraction `CLAUDE.md` §8 warns against, and no repository evidence ever made it the target shape in the first place.

MS-002's next Epic remains undefined, per the user's own instruction, until a separate conversation makes that product-scope decision.

Before that next Epic, the user requested a focused Python architecture hardening iteration addressing two decisions discovered as real gaps while reviewing EP-005: canonical Corytm domain/application models were plain frozen `dataclasses`, and every `engine`/`runtime`/`generated` import only resolved because pytest's `pythonpath` ini option and Pyright's `extraPaths` each independently inserted `modules/`/`generated/` onto `sys.path` for their own tool run — real program execution had no equivalent, which is exactly why TK-014/EP-005's documented invocation was `PYTHONPATH=modules:generated uv run python main.py` run from inside `src/backend/core/`. Both were addressed as accepted ADRs rather than silent implementation choices, since both are exactly the kind of decision `decisions/README.md` says is worth recording: difficult to reverse once more Engine/Runtime/Dorian code exists, and likely to be silently violated by a future session without a durable rationale to point to.

**ADR-008** adopts Pydantic (v2) as the required modeling framework for canonical/domain/application data: `Project`/`AudioTrack`/`AudioClip` moved from frozen dataclasses to frozen (`ConfigDict(frozen=True)`) `BaseModel`s, with `AudioClip`'s existing invariants (`start_seconds` non-negative, `duration_seconds` strictly positive) expressed as `Field(ge=0)`/`Field(gt=0)` constraints instead of a hand-written `__post_init__` — no new invariant was invented for `AudioTrack`/`Project`, since none was implied by their existing dataclass behavior. `pydantic.ValidationError` subclasses `ValueError`, so the existing `pytest.raises(ValueError)` tests kept passing unchanged; new tests (`test_track.py`, `test_project.py`) prove nested/transitive validation specifically — constructing `AudioTrack`/`Project` from untrusted dict-shaped input (`model_validate`) with an invalid nested `AudioClip` is rejected, which the prior dataclasses never had to prove since they were only ever constructed from trusted in-process Python values. `pydantic`/`pydantic-core` ship prebuilt wheels for Python 3.14 on both macOS and Windows (checked against PyPI's actual release files before adopting, not assumed), so this adds no new build-toolchain requirement for either CI platform.

**ADR-009** makes `src/backend/core` a real, installable Python package rooted at `corytm` (`src/backend/core/src/corytm/`, a standard nested Python src-layout, built by `hatchling`; `[build-system]` was previously entirely absent, so `uv` had been treating this as a non-package "virtual" project). `engine`/`runtime` became `corytm.engine`/`corytm.runtime`; the Protobuf-generated bindings moved from a bare `generated/` to `corytm.generated`; the entry point became a `[project.scripts]` console script (`corytm = "corytm.main:main"`) instead of a bare `main.py`. Import rule adopted: absolute `corytm.`-qualified imports across the architecture's named ownership boundaries (Engine/Runtime/Native Audio Runtime/Dorian), relative imports permitted only between tightly-coupled sibling modules inside the same one of those subpackages (for example `engine/track.py` importing `.clip`) — applied consistently across every existing import in `src/backend/core`. The nested `src/corytm/` layout (rather than a flat `src/backend/core/corytm/`) was chosen deliberately: a flat layout would sit in the same directory the old cwd-based invocation already worked from, so the exact failure mode this migration fixes could keep silently passing undetected. Verified concretely, not just reasoned about: `uv run --project src/backend/core corytm`, invoked with `cwd` set to an unrelated `/tmp` directory (no `PYTHONPATH`, no prior `cd`), ran the real First Sound path end to end — real `native_runtime` process, real Tracktion Engine materialization/render, a genuine WAV file written into that unrelated directory. This is now a permanent automated test (`tests/test_entrypoint.py::test_entry_point_runs_independent_of_caller_working_directory`, `@pytest.mark.transport`, run via `check-transport`), not merely a one-off manual check — it invokes the real `uv run --project ... corytm` subprocess from a real different `cwd`, deliberately not a test that manipulates `sys.path` in a way production execution never would.

Scope was deliberately kept narrow, per the user's explicit instruction: no Engine/Runtime models/services/repositories restructuring, no Dorian product code, no persistence/API behavior, no Native Audio Runtime change, no next-MS-002-Epic definition. `docs/technical/architecture.md`'s Naming section was updated to the new `src/corytm/engine`/`src/corytm/runtime` paths, and two new sections ("Canonical State and Projection" extended; a new "Python Backend Package Layout") point at ADR-008/ADR-009 respectively rather than duplicating their rationale. No `CLAUDE.md` amendment was proposed: both decisions are adequately anchored by their ADRs plus this architecture-doc update, and `CLAUDE.md` §1's own stated design is to point at canonical artifacts rather than duplicate stable rules that already live there. `Makefile`'s `check-python`/`clean` targets and both cross-component tests under `src/backend/core/tests/` were updated to the new import paths; a full local `make check` (all five targets: Python, frontend, desktop, native, transport) passed end to end with the migrated code, including the real native/Tracktion path via `check-transport`, before this iteration closed.

A focused governance and architecture-direction iteration then addressed two remaining gaps the audit surfaced: no documented policy for Python docstrings, and no durable statement of Corytm's intended structural-evolution direction beyond the audit's own narrow Native Audio Runtime fix. `CLAUDE.md` §8 gained a scoped exception to its zero-comments rule: production Python modules, and every production Python class (public or private alike), carry accurate Google-style docstrings; non-trivial public functions/methods do too; trivial private functions/methods are exempt from requiring one merely for coverage. The same amendment corrected two stale path references in `CLAUDE.md` §2/§3 (`src/backend/core/modules/{engine,dorian,runtime}` → the ADR-009-accepted `src/backend/core/src/corytm/{engine,dorian,runtime}`, plus `api/**` reconciled the same way per ADR-009's own "any other Python subpackage" clause) — a small, genuinely overdue truthfulness gap ADR-009's migration had left unfixed in `CLAUDE.md` itself, even though `docs/technical/architecture.md`'s own Naming section was already corrected at the time. Automated enforcement was kept deliberately narrow after confirming empirically (a throwaway `ruff check --select D101` run against a real private class) that pydocstyle/Ruff's `D` rules only ever check *public* definitions, with no mechanism to enforce private-class docstrings at all: `pyproject.toml` now selects only `D100`/`D101`/`D104` (module/class/package presence), ignored for `tests/`, colocated `test_*.py`, and the pre-existing `sidecar_proof.py` toolchain proof — deliberately not the full `D1xx` set, which would have forced docstrings onto every descriptively-named pytest test function, exactly the boilerplate-over-signal outcome ruled out up front. `docs/technical/architecture.md` gained a new "Structural Evolution" section stating the durable direction (domain-oriented, flat-while-homogeneous, restructure only once real heterogeneity emerges, no ceremonial scaffolding) with per-area growth notes for Engine, Runtime, Dorian, Native Audio Runtime, Schemas, and Tests. `.claude/memory/structural-evolution-signals.md` records the provisional, evidence-bearing half — today's concrete file counts confirming Engine/Runtime's flatness is genuinely unforced, the specific signals that would trigger reconsidering it, and the Native Audio Runtime's already-completed `tests/` split as real precedent for what that trigger threshold looks like in practice; `.claude/memory/README.md`'s entry count was updated to match. The docstring policy was then applied to the entire current production Python surface (`corytm/__init__.py`, `engine/__init__.py`, `engine/{clip,track,project}.py`, `runtime/__init__.py`, `runtime/{transport,session,projection}.py`, `main.py`) — module docstrings throughout, class docstrings on every model/protocol (`AudioClip`, `AudioTrack`, `Project`, `FrameWriter`), and Google-style Args/Returns/Raises on every non-trivial public function (`read_frame`/`write_frame`, `to_materialize_command`, `native_runtime_executable`/`materialize_project`, `main`) — while `_build_first_sound_project` (a two-line private fixture builder) and every test file were left untouched, per the policy's own trivial/non-production exemptions. No Engine/Runtime/Native Audio Runtime restructuring, no Dorian code, and no MS-002 Epic definition were introduced — scope stayed exactly governance and documentation, as instructed. A full clean `make check` (all five targets: Python, frontend, desktop, native, transport) passed end to end with the new lint rules and docstrings in place, pyright strict and `ruff check`/`ruff format --check` clean on the newly-documented code.

Per the user's explicit request, a focused Harness/CI Optimization Task addressed the CI-duration concern `RSK-002` had explicitly deferred ("CI duration was never this risk's subject... not chased further"): a normal `check` push had grown to over 30 minutes end to end. Measured from real `gh run view`/`gh api` job/step timestamps across the latest and several prior runs, not assumed: macOS ~25-26min wall, Windows ~36-39min wall, both dominated overwhelmingly by `cmake --build` (15m11s/59% of the macOS step, 22m42s/62% of the Windows step) compiling JUCE/Tracktion Engine/protobuf/abseil — code that never changes between runs at pinned versions. Ranked by measured contribution: native compilation first by a wide margin; `cmake -S` configure second (~4.3min both platforms, almost entirely two full-history git clones — JUCE and Tracktion Engine — never cached across runs); Tauri/cargo build+test third (macOS ~5m19s, Windows ~9m15s combined, also uncached from-scratch Rust compiles); everything else (uv/Python, npm/frontend, ctest, `pytest -m transport`) collectively under 30 seconds per platform and never the bottleneck.

Implemented, in `.github/workflows/check.yml`/`Makefile`/`src/backend/audio/CMakeLists.txt`: `mozilla-actions/sccache-action` wired as both the CMake C/C++ compiler launcher (`CMAKE_C_COMPILER_LAUNCHER`/`CMAKE_CXX_COMPILER_LAUNCHER`, plus the CMake≥3.25 MSVC recipe — `CMAKE_MSVC_DEBUG_INFORMATION_FORMAT=Embedded`/`CMP0141=NEW` — sourced directly from `mozilla/sccache`'s own current docs, keeping the Visual Studio generator per `.claude/memory/cmake-windows-generator.md`'s established precedent rather than switching to Ninja) and the Rust wrapper (`RUSTC_WRAPPER=sccache`) for `cargo test`/`tauri build`; an `actions/cache` entry for the entire `src/backend/audio/build/_deps` tree (sources and already-compiled third-party objects together, not just sources — a full-tree cache measured ~2x faster locally than a sources-only one, see `.claude/memory/native-build-caching.md`) exact-keyed on runner OS/architecture/image identity, generator/build-configuration identity, and `CMakeLists.txt`'s hash — hardened after an audit found the OS-plus-hash key alone couldn't detect a runner-image toolchain upgrade, which Make's mtime-based up-to-date check has no way to catch on its own; an `actions/cache` entry for `~/.cargo/registry`/`~/.cargo/git` keyed on `Cargo.lock`'s hash; `GIT_SHALLOW ON` added to Tracktion Engine's `FetchContent_Declare` (tag-pinned, confirmed working — measured 161s full clone down to ~8s); workflow `concurrency` added so a newer push cancels an in-flight run of the same branch/PR; `paths-ignore` added for genuinely documentation/governance-only paths (`docs/**`, `decisions/**`, `specs/**`, `.claude/**`, `README.md`, `CLAUDE.md` — confirmed by grep that no build/test tooling reads any of them); `CMAKE_BUILD_PARALLEL_LEVEL` set to each platform's actual documented core count (3 macOS, 4 Windows, per GitHub's own runner-specification docs), since the native build had never been parallelized at all. `GIT_SHALLOW ON` was attempted on JUCE too and reverted after a real local failure: JUCE is pinned by a raw commit SHA rather than a tag, and shallow-cloning an arbitrary SHA failed outright (`fatal: unable to read tree`) — caught locally before it could have broken every future CI run, not discovered via a failed push.

A conceptual portable/platform-sensitive job split was evaluated and deliberately not implemented: Python/TypeScript lint-and-test steps already cost under 30 seconds combined per platform against a 25-39 minute pipeline, so deduplicating them would save low single-digit seconds against the fixed overhead of a third job's checkout+toolchain setup — a net-neutral-or-negative trade — and this repository's own history (`RSK-002`'s CRT/link-time/protobuf-ABI failures, `TK-006`'s MinGW auto-detection) shows genuine platform-specific failures have so far only ever surfaced in the native/build-toolchain layer, never in the portable Python/TS checks, giving no evidenced upside to offset that cost.

Locally validated end to end (`actionlint` clean; `make check-python`/`make check-frontend` pass unchanged): a full isolated cold-then-warm native rebuild outside the tracked `build/` directory, proving (a) the corrected shallow-clone config clones cleanly, (b) `sccache` genuinely caches identical compiles once its server/cache-directory lifecycle is controlled correctly, and (c) restoring the full `_deps` tree at a stable path — matching what `actions/cache` does in real CI, where the checkout path is stable run to run — cut a real local configure from ~92s to ~6.8s and the build step from ~110-150s to ~56s, with the third-party compile count dropping from 371 to 133 (of which those 133 still got a 48% `sccache` hit rate on top). Not locally verifiable without a Windows/MSVC machine: the `CMAKE_MSVC_DEBUG_INFORMATION_FORMAT`/`CMP0141` MSVC-specific half of the `sccache` wiring, and `RUSTC_WRAPPER=sccache` against the real Tauri/cargo dependency tree (no local Rust toolchain was installed to test this, since it is `sccache`'s original/best-proven use case and the risk was judged low relative to the untested MSVC path) — both require the next real GitHub Actions push to confirm, alongside the actual warm-cache CI timing itself.

The user then established Corytm's product strategy and business-model direction as canonical (`docs/product/strategy.md`, `docs/product/business-model.md`, `docs/technical/architecture.md`'s Structural Evolution cross-reference, all dated 2026-09-01) and asked a subsequent session to apply the resulting strategy-aware autonomous workflow: re-evaluate MS-002 from current evidence rather than assume the next work is whatever was historically expected, and — since no Protected Product Decision was implicated — discover and execute the next ready MS-002 Epic/Feature/Task under normal Task lifecycle.

That session's CPO+CTO reasoning: `docs/product/strategy.md` §14 and §4 confirm Desktop remains Alpha's technical priority and Alpha is not "finish a full DAW," while the new strategy's Web/Mobile/commercial/generative-audio/Audio-to-Project direction is explicitly Beta-or-later or an open hypothesis (§19), not a reason to start any of that now. MS-002's Outcome (`docs/project/milestones/MS-002.md`) still has two unreached halves — manual Desktop editing and Dorian's semantic edit — and both share one missing foundation EP-005 deliberately deferred: Corytm Engine has no edit *operation* yet (only passive Pydantic models), and `native_runtime` exits after exactly one command, so there is no way to express an edit as anything other than resending the whole project. `docs/technical/architecture.md`'s "Engine Services and Incremental Updates" section already names the illustrative example ("a command to move a clip producing a 'clip moved' event") nothing yet implements. Proving that one mechanism was judged the highest-leverage next step: required by both remaining Outcome halves (work that unlocks other work, `docs/project/plan.md`'s Next-Work Selection Priority #3), reduces a named architectural uncertainty (#4), stays the smallest coherent vertical slice (#5), and — per `CLAUDE.md` §5 — is formalizing behavior MS-002's own Outcome and the architecture doc already agree on, not introducing a new one, so no Protected Product Decision was implicated and no approval stop was needed. EP-006 (First Edit: Incremental Clip Move Through a Live Native Session) was created under MS-002 accordingly (see `docs/project/epics/EP-006.md` and MS-002's own Notes for the full rationale), scoped to exactly one edit operation across three increments mirroring EP-005's own "prove one increment, then the next" shape.

FT-014/TK-015 (EP-006's first Feature/Task) proved the first increment — Engine's edit operation and its wire schema, in isolation from the native runtime, mirroring FT-011/TK-012's own precedent exactly. Implemented via TDD: `AudioTrack.with_clip_moved`/`Project.with_clip_moved` were added to the existing Pydantic domain models (`src/backend/core/src/corytm/engine/{track,project}.py`), rebuilding the targeted clip through full `AudioClip(...)` construction rather than a raw `model_copy(update=...)` patch specifically because pydantic v2's `model_copy` does not re-validate — full construction was chosen so the clip's existing `Field(ge=0)` start-time invariant keeps applying to edits, not just construction, with no new invariant duplicated. `project.proto` gained `MoveClipCommand`/`ClipMovedEvent`; a pure `to_move_clip_command` projection function was added to `runtime/projection.py`; the C++ `project_schema` ctest was extended with both new messages' round trip, reusing `project_proto`'s existing CMake shape unchanged (the same shape RSK-005 already closed for nested/generated-code risk). Validated beyond this Task's own scope per `CLAUDE.md` §7's cross-boundary bar: `uv run pytest -m "not transport"` (26 passed, including 6 new tests), `pyright` (0 errors), `ruff check`/`ruff format --check` (clean after `--fix`/`format` resolved one import-ordering and one line-length issue the initial edit introduced), a full native rebuild of all five targets with zero new warnings (confirmed by reading the log), the full `ctest` suite (4/4 passed), and `uv run pytest -m transport` (3 passed — the cross-language transport/first-sound/entry-point suite, unrelated to this Task's own scope but re-run to confirm the shared schema/generated-bindings change didn't regress EP-005's already-closed round trip). FT-014/TK-015 are `done`; EP-006 stays `active` — its next two Features (native live-session/`Edit`-mutation behavior; the real end-to-end wiring) remain to be refined and executed. No new risk was opened: this Task's shape and risk profile exactly mirror FT-011/TK-012's already-closed precedent (RSK-005), and the genuinely new native-behavioral surface EP-006 anticipates belongs to its next Feature, not this one — consistent with this project's own practice of not opening risks for undefined future work. Not yet independently confirmed on real Windows CI, per `CLAUDE.md` §10 (this session cannot push); no new build-toolchain risk is expected given the reused CMake shape, but per this project's own established practice (RSK-002/005/006/007), that expectation is not treated as proof without a real passing Windows Actions run.

Per the user's explicit request, a narrowly-scoped CI Optimization follow-up Task (TK-016, under a new EP-007/FT-015 — this repository's now-hardened `CLAUDE.md` §7 requires a persisted PMO record before implementation, which the prior, informally-tracked "Harness/CI Optimization Task" paragraph above predates) diagnosed the two concrete inefficiencies that same prior work's own post-optimization real CI evidence exposed. Independently pulled via `gh run view`/`gh api` (run `33564596456`, commit `7ebb101`), not the user's summary alone: macOS genuinely improved as reported (6m12s wall, 91.42% overall / 94.07% C/C++ sccache hit rate); Windows restored its `native-deps` cache successfully yet still spent ~17 minutes in `cmake --build`, with `sccache --show-stats` reporting all-zero across every field — not just zero hits — for the entire job. The MSVC C/C++ half is now conclusively explained as a structural, not-expected-to-recur limitation: CMake's Visual Studio generator has never wired `CMAKE_<LANG>_COMPILER_LAUNCHER` into MSBuild project files (only Makefile/Ninja do — CMake's own tracked issue, reproduced identically by two independent `mozilla/sccache` bug reports), and sccache — unlike ccache/clcache — has no mode to masquerade as `cl.exe` for MSBuild's alternative `CLToolExe` mechanism (a still-open `mozilla/sccache` issue confirms it errors outright). There is currently no way to route MSVC C/C++ compilation through sccache while keeping the Visual Studio generator (preserved per `.claude/memory/cmake-windows-generator.md`'s precedent); the dead Windows launcher flags — one of which (`CMAKE_POLICY_CMP0141`) was independently wrong regardless, missing CMake's required `_DEFAULT_` infix, a typo traced to `mozilla/sccache`'s own README — were removed rather than left silently ineffective. The Cargo/Rust half of Windows's zero sccache activity could not be conclusively root-caused from static log evidence (`RUSTC_WRAPPER` confirmed present in the job's own environment; the sccache action's PATH-exposure mechanism read directly from its source and confirmed cross-platform-identical; hundreds of real `rustc` invocations via two independent call paths both showed zero activity, ruling out a tauri-cli-specific cause) — an explicit `sccache --start-server` step and `SCCACHE_LOG`/`SCCACHE_ERROR_LOG` diagnostic capture were added as a low-risk, evidence-motivated mitigation plus instrumentation for the next real run to settle conclusively, tracked at `RSK-008`.

The native CMake target-graph audit confirmed real, measured duplication — the same Tracktion/JUCE translation units compiled once per target (`toolchain_proof`/`materializer`/`materializer_proof`/`native_runtime`), visible directly in both platforms' build logs — and a consolidation into a shared `tracktion_runtime` library was genuinely attempted and locally built, not just proposed. It was rejected on real evidence: JUCE's module `INTERFACE_SOURCES` propagate through any further real (non-`INTERFACE`) library in a `PUBLIC` link chain to whichever target ultimately builds, so the wrapper library did not eliminate the duplicate compiles — a local rebuild showed a fifth, wasted copy added on top of the original four — and the only way to stop that propagation would also silently drop the include-directory/compile-definition/OS-framework link requirements every consumer's own sources still need, a correctness risk disproportionate to a build-time cost already mostly absorbed by a working sccache on macOS. `CMakeLists.txt` was reverted to its original form (confirmed via `git diff` — zero net change) and re-validated clean (`ctest`: 4/4 passed). The related duplicated Tauri release-build + `cargo test` Rust recompilation was left unchanged for the same reason: its real fix is the same open Rust/sccache question above, not a separate Cargo-profile-unification change risking silently changed test semantics (`debug_assertions`/overflow-checks differ between profiles). TK-016/FT-015/EP-007 stay `active`, not `done` — per this project's own established practice (RSK-002 through RSK-007), the Windows Cargo/Rust mitigation's actual effect, and the real post-fix Windows `cmake --build` duration, remain unconfirmed until the next real Windows GitHub Actions run. No product code, Corytm Engine/Runtime/Native Audio Runtime/Dorian behavior, or EP-006 implementation was touched.

The user pushed the TK-016 fix as `64f6264` and ran it: independently confirmed via `gh run view`/`gh api` (run `33594758288`) — `check` passed on both platforms; macOS improved further (4m38s wall, 99.17% overall / 96.30% C/C++ / 99.83% Rust sccache hit rate, no regression). Windows's plain `sccache --show-stats` again showed all-zero, but the `SCCACHE_LOG`/`SCCACHE_ERROR_LOG` diagnostics added specifically for this purpose resolved the question directly rather than by further inference: the daemon genuinely served the whole Cargo/Rust build with real hits (a `stats after compile result` snapshot recorded 756 compile requests, 615 executed, 522 Rust cache hits against 82 misses — ~86%), then sat idle for exactly 10 minutes 5 seconds after the last Rust compile — sccache's own documented default `SCCACHE_IDLE_TIMEOUT` (600s) — because the ~17-minute native `cmake --build` phase that follows generates no sccache traffic at all (a direct consequence of this same Task's earlier finding that MSVC C/C++ cannot route through sccache under the Visual Studio generator). By the time the job's final `sccache --show-stats` call ran, it silently talked to a freshly auto-spawned, empty replacement server, not the one that did the real work — a stats-reporting artifact, not a caching failure, and the same explanation behind every "zero requests" observation across both runs this Task examined. **Windows Cargo/Rust sccache routing was never actually broken.** Fix applied: `SCCACHE_IDLE_TIMEOUT: "0"` (sccache's own documented setting to disable the idle timeout) added to `check.yml`'s job env; the diagnostic scaffolding that found this (the explicit start-server step, `SCCACHE_LOG`/`SCCACHE_ERROR_LOG`, the log-dump step) was removed now that its purpose is served, rather than left in place indefinitely bloating every future run's log. `docs/project/risks.md`'s `RSK-008` and `.claude/memory/` (`sccache-idle-timeout-final-stats.md`, new) were updated accordingly. `actionlint` re-run clean. TK-016/FT-015/EP-007 all stay `active`, not `done`: every other acceptance criterion is now satisfied, but per this project's own established practice, whether the next real Windows run's plain `sccache --show-stats` output actually shows accurate non-zero stats is not treated as confirmed until that run is independently checked. No product code or EP-006 was touched.

The user pushed the `SCCACHE_IDLE_TIMEOUT: "0"` fix as `2f1f02a` and ran it: independently confirmed via `gh run view`/`gh api` (run `33597567001`) — `check` passed on both platforms, and Windows's plain, uninstrumented `sccache --show-stats` now reports real, accurate cumulative stats (`759` compile requests, `615` executed, `522` Rust cache hits, `82` misses, `86.42 %` hit rate — matching the previous run's debug-log snapshot almost exactly), also echoed by GitHub's own `##[notice]86% - 522 hits, 82 misses, 0 errors` annotation. macOS held its improvement with no regression (`870` requests, `99.17 %` overall). Both platforms' native `ctest` reported `100% tests passed out of 4`. The one acceptance criterion left open across TK-016/FT-015/EP-007 is now satisfied by real evidence, not reasoning alone. A full local `make check` was also run before closing — `check-python`, `check-frontend`, `check-native`, and `check-transport` all passed clean; `check-desktop` could not run locally (no Rust/Cargo toolchain in this environment, a pre-existing limitation) and was instead confirmed via the real CI evidence itself. **TK-016, FT-015, and EP-007 are all closed `done`.** `RSK-008` closed accordingly. No product code, Corytm Engine/Runtime/Native Audio Runtime/Dorian behavior, or EP-006 implementation was touched at any point across this Epic's three sessions — EP-006's next Feature (the Native Audio Runtime keeping a live `tracktion::Edit` and applying a `MoveClipCommand` to it in isolation) is recommended as the next Task, not started.

This session continued autonomously from the recommendation above: created FT-016/TK-017 under EP-006 (native counterpart of FT-014's Engine/wire-schema proof — a live `tracktion::Edit` accepting a clip move without being rebuilt) and executed it. `src/backend/audio/materializer.h`/`.cpp`'s `materialize()` was split via TDD into three reusable functions — `buildEdit` (builds a live `Edit` from a `ProjectSpec`, now also naming the track/clips after their spec ids so they can be looked up later, closing a previously-unused `TrackSpec.id`), `moveClip` (finds a track+clip by id and calls `Clip::setStart` with `keepLength=true`, matching Corytm Engine's own `with_clip_moved` semantics; returns `false` without mutating anything for an unknown id), and `renderEdit` (renders an already-live `Edit`, computing its end time from the `Edit`'s own current clip positions rather than a stale `ProjectSpec` — the change that makes a post-move re-render of the same `Edit` correct) — with `materialize()` now a thin composition of the two, its external behavior unchanged. A new `tests/live_session_proof.cpp` ctest builds one `Edit`, renders a baseline, moves its clip, re-renders the *same* `Edit` object, and confirms the move by reading sub-ranges of the rendered WAV directly (near-silence before the new start, genuine signal after it) — plus confirms `moveClip` rejects an unknown track/clip id without side effects. A new `tests/headless_engine.h` factors out the headless deterministic-rendering `Engine` stub trio that `materializer_proof.cpp` already had and `live_session_proof.cpp` would otherwise have duplicated a third time in `tests/` — exactly the reuse trigger TK-013's own Notes flagged in advance; `materializer_proof.cpp` was refactored onto it and re-verified to still pass unchanged, regression-guarding the `buildEdit`/`renderEdit` split. `native_runtime.cpp`'s own separate, near-identical stub copy (production code from the already-closed FT-010/FT-013) was deliberately left untouched — flagged, not fixed, mirroring TK-013's own precedent for the same kind of deferred dedup.

Validated locally beyond this Task's own scope, per `CLAUDE.md` §7's cross-boundary bar: a full `cmake --build` of all seven native targets (including `native_runtime`, which links the refactored `materializer` library) produced zero new warnings beyond the pre-existing catalogued vendored set; `ctest` reports `100% tests passed out of 5`; `make check-python` (23 passed, pyright 0 errors, ruff clean) and `make check-transport` (3 passed, including the real cross-language First Sound/entry-point round trip) were re-run to confirm no regression on the shared `materializer`/`native_runtime` surface, since `project.proto`/`wire_codec.cpp`/`native_runtime.cpp` themselves were not touched. `docs/technical/architecture.md` needed no wording change — both its "Engine Services and Incremental Updates" illustrative example and its "Native Audio Runtime Source Layout" tests/-vs-root convention already covered this shape, per the same reasoning TK-015 applied to the same section. FT-016/TK-017 stay `active`, not `done`: per this project's own established practice (RSK-005/006/007), this genuinely new native-behavioral surface (a live `Edit` mutated in place after its first render) awaits a real, independently-verified Windows GitHub Actions run before closing — this session cannot push (`CLAUDE.md` §10). EP-006's third increment — wiring Engine's edit operation and this native live-session mechanism together over the real ADR-007 transport into a long-lived `native_runtime` session — is recommended as the next Task, refined once FT-016/TK-017 close.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: rather than trusting TK-017/FT-016's documented `active` status at face value, it independently verified real available evidence first — `gh run view`/`gh api` on run `33602663330` (commit `96fa11f`) showed `live_session_proof` passing alongside `materializer_proof` on both `check (macos-latest)` and `check (windows-latest)`, exactly the real Windows CI confirmation TK-017's own Notes had been waiting on. TK-017 and FT-016 were synchronized to `done` on that evidence — no further code change was needed, only PMO state catching up to reality.

The session then executed EP-006's third and final increment: FT-017/TK-018, wiring the already-proven Engine/wire-schema half (FT-014) and native-isolation half (FT-016) together over the real ADR-007 transport. `src/schemas/project.proto` gained `MoveClipCommand`/`ClipMovedEvent`'s already-existing pair a companion `Command`/`Event` oneof envelope sized to exactly these two known command/event types (grounded in ADR-007's own "serialized protobuf envelope" language, not a new architectural boundary — reasoning recorded on FT-017 itself), plus four additive fields on `ClipMovedEvent` (`moved`, and the same render-evidence shape `ProjectRenderedEvent` already carries) so a move command's single response can confirm both the edit and its re-render, mirroring how `materialize()` already bundles build+render. `native_runtime.cpp`'s single-command-then-exit `main()` became a loop that keeps the `Edit` a materialize command builds alive across subsequent move commands on the same connection, dispatching on the new envelope via `wire_codec.h`'s `decodeCommand`/`encodeProjectRenderedEvent`/`encodeClipMovedEvent`. `src/backend/core/src/corytm/runtime/session.py` factored its spawn/connect/authenticate/teardown block into a private `_connected_native_runtime` context manager (no behavior change) and added `materialize_then_move_clips`, driving a materialize-then-N-moves sequence over one connection. TDD surfaced a real concurrency bug beyond what local reasoning alone would have caught: `NativeRuntimeConnection::waitForCommand`'s `commandReceived` flag was never reset after being consumed, so every session's *second* command silently received stale, already-moved-from bytes instead of the real message — found by manually driving the built binary over a raw socket outside pytest after the full suite hung, and fixed by resetting the flag on consumption. A related design fix: an unknown-id move no longer poisons the whole session's exit code, so a later valid move in the same session still succeeds and reports correctly — required for this Task's own "doesn't end the session" acceptance criterion to actually hold.

Validated locally end to end: a full clean reconfigure + rebuild of all native targets produced no new warning categories (only the pre-existing catalogued vendored JUCE/Tracktion set, appearing in higher counts than usual only because a full clean rebuild — not this project's normal incremental one — recompiles vendored translation units an incremental build wouldn't touch); `ctest` reports `100% tests passed out of 5`, `project_schema` now also covering the envelope and `ClipMovedEvent`'s new fields; `uv run pytest -m transport` reports `5 passed` (the two pre-existing round trips unchanged, plus two new cross-language cases proving a move's effect in a same-session re-render and an unknown-id move's graceful, session-preserving failure); `uv run pytest -m "not transport"` (23 passed), `pyright` (0 errors, after one deprecated-annotation fix), and `ruff check`/`ruff format --check` (clean after auto-fix) all pass. `docs/technical/architecture.md`'s "Python↔C++ Boundary" section gained one sentence naming the new `MoveClipCommand`/`ClipMovedEvent` pair and the `Command`/`Event` envelope. `RSK-009` opened, mirroring RSK-006/007's precedent for a genuinely new combination of already-proven pieces. FT-017/TK-018 and EP-006 stayed `active`, not `done`, pending a real, independently-verified Windows GitHub Actions run — this session could not push (`CLAUDE.md` §10).

The user pushed TK-018's commit (`128e298`) and reported the CI run complete. Independently confirmed via `gh run view`/`gh api` (run `33609898160`), not the report alone — `check` passed on both `check (macos-latest)` (5m8s) and `check (windows-latest)` (22m32s); real job logs on both platforms show `tests/test_incremental_edit.py ..` inside `5 passed, 23 deselected` and `5/5 Test #5: live_session_proof ... Passed` inside `100% tests passed out of 5`. **TK-018, FT-017, and EP-006 are all closed `done`.** `RSK-009` closed accordingly. EP-006's Acceptance criteria are now fully satisfied on real evidence: a real edit intention — moving a clip — is proven both as a Corytm Engine operation (TK-015) and as a `MoveClipCommand` applied to an already-live native `Edit` over the real transport (TK-018), with real cross-platform CI evidence on the native/live-session half. MS-002's shared incremental-edit mechanism now exists.

MS-002's Outcome still has two unreached halves, both now unblocked by EP-006: (1) a user manually editing through real Desktop UI — no real project-creation/track/clip UI exists yet, only the Tauri shell scaffold and EP-005/EP-006's own hardcoded fixture scripts; (2) Dorian performing a real semantic edit — Dorian doesn't exist yet at all; ADR-004 defines its tool-boundary shape but nothing has been built against it. Recommended as MS-002's next Epic: **Dorian's first real semantic edit** (call `with_clip_moved` through a governed Dorian tool per ADR-004, reusing EP-006's now-proven command/live-session mechanism unchanged) — smaller marginal scope than standing up real Desktop editing UI from scratch, more directly de-risks Corytm's AI-native core positioning during Alpha (`CLAUDE.md` §3's CPO Mandate: "technical de-risking, architectural leverage... weigh more than commercial immediacy"), and completes the second, harder-to-de-risk half of MS-002's Outcome. Not started this session — `CLAUDE.md` §6 caps one session at one Task, already spent on TK-018; presented here as a recommendation to confirm or redirect before Epic/Feature/Task-level definition begins.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: independently confirmed via `gh run list`/`gh run view` that the most recently documented state (EP-006/FT-017/TK-018 closed `done` on run `33609898160`, commit `128e298`) is genuinely accurate before relying on it — no stale record found; the current `HEAD` (`7614403`) is a docs-only PMO-sync commit correctly excluded from CI by `paths-ignore`. It then took up the prior session's recommendation: MS-002's next Epic, Dorian's first real semantic edit. Checked first that this doesn't implicate a Protected Product Decision (`CLAUDE.md` §5): MS-002's own Outcome and ADR-004 already require Dorian to perform a real semantic edit through the governed application path, so realizing that is formalizing already-agreed behavior, not introducing a new one — the actual protected territory (LLM/provider integration, model routing, Allegro/Virtuoso/Maestro tiers, BYOK) was deliberately kept out of scope, mirroring EP-005/EP-006's own precedent of proving a mechanism with the smallest possible harness before any real product surface exists around it.

EP-008 (Dorian's First Semantic Edit) was created under MS-002, refined into a single Feature/Task (FT-018/TK-019) — judged the right size, unlike EP-005/EP-006's multi-increment shape, because the only genuinely new surface is Python-side glue over two mechanisms (`Project.with_clip_moved`, `materialize_then_move_clips`) EP-006 already proved cross-platform, so there was no new native-behavioral risk to isolate before combining. Implemented via TDD: a new `corytm.dorian` package (`tools.py`) defines `MoveClipProposal` — a frozen, `extra="forbid"` Pydantic model standing in for a model's untrusted tool-call arguments, rejecting a missing field, an out-of-range `new_start_seconds`, or an unexpected extra field before anything executes — and `move_clip`, the single trusted function that authorizes and executes a validated proposal by calling Engine's `with_clip_moved` (the canonical-state update) and then EP-006's `materialize_then_move_clips` (the live-session re-render), both completely unchanged. Both test files were confirmed failing (`ModuleNotFoundError`) before the module existed, matching the plan exactly with no design change needed during implementation.

Validated via the canonical `make check-python`/`make check-transport` targets: 28 passed / 6 deselected (including the new colocated `dorian/test_tools.py` validation-rejection tests and `test_dorian_move_clip.py`'s unknown-clip-id case, which confirms `ValueError` is raised before any native process is spawned) and 6 passed / 28 deselected (including `test_dorian_move_clip.py`'s end-to-end case against the real, already-built `native_runtime` binary — the original `Project` confirmed unchanged, the returned `Project` confirmed moved, and the returned `ClipMovedEvent`'s render fields confirmed reflecting the move); `pyright` (0 errors) and `ruff check`/`ruff format --check` clean. No native or schema file was touched (confirmed via `git status`). `docs/technical/architecture.md`'s "Human/Dorian Shared Application Path" section and its Structural Evolution "Dorian" bullet were updated from "designed to"/"not yet started" to state this as current reality, since both were stale the moment this Task's code landed.

FT-018, TK-019, and EP-008 were all closed `done` **locally**, deliberately without waiting for a fresh cross-platform CI run — a first for this project's product-code work, and explicitly reasoned rather than assumed: this Epic introduced no native/build-toolchain code and reused EP-006's already independently-verified cross-platform mechanisms completely unchanged, so there is nothing new for a Windows run to disprove, unlike every EP-004/EP-005/EP-006 native-behavioral Task (RSK-002 through RSK-009) where exactly that kind of change repeatedly surfaced real, unpredictable platform differences. This mirrors the earlier ADR-008/ADR-009 Python-hardening iteration's own precedent (also closed locally, no CI-wait caveat) rather than the native-behavioral one. No new risk was opened, consistent with that same reasoning.

MS-002's Outcome still has two open threads even with EP-008 done: no model/provider is actually wired up to call Dorian's new tool yet (proposals are still constructed directly in tests, standing in for a model's output — deliberately out of EP-008's scope as protected/premature territory), and no real Desktop project-creation/editing UI exists (only the Tauri shell scaffold and EP-005/EP-006/EP-008's own hardcoded fixture scripts exercise the stack). Recommended as MS-002's next Epic: real Desktop project/track/clip editing UI, wiring the existing Tauri shell to Corytm Engine/Runtime through the sidecar so a human can manually create a project and move a clip through actual UI, completing the manual-editing half of MS-002's Outcome — chosen over live model/provider wiring because it touches no Protected Product Decision (`CLAUDE.md` §5's provider-strategy/BYOK/tier list), while provider wiring would first need an explicit provider-selection decision from the user. Not started this session — `CLAUDE.md` §6 caps one session at one Task, already spent on TK-019; presented here as a recommendation to confirm or redirect before Epic/Feature/Task-level definition begins.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: `git log`/`git status` confirmed `HEAD` (`7352337`, TK-019's commit) matches this file's own documented state exactly, working tree clean, already pushed to `origin/main` — no stale record to reconcile there. A separate, genuine PMO-hygiene defect was found and fixed while re-reading the risk register: `RSK-004`'s Status column still read `monitoring` even though its own Mitigation text ends "**Closed:** ... Both risks this entry tracked ... are now genuinely resolved by real cross-platform evidence" (recorded when `41d2b9c` closed it) — the narrative had been updated at closure but the structured Status field never was. Corrected to `closed`; ordinary PMO-owned doc sync per `CLAUDE.md` §3/§4, not a protected change, so applied directly.

Next-work selection then evaluated both threads MS-002's Outcome still leaves open (per EP-008's own recommendation above): Dorian-model wiring was set aside again for the same reason as before — it needs an explicit provider-selection/BYOK decision (`docs/product/strategy.md` §10's protected list), not yet made. Desktop editing UI was examined more closely this time, since it was recommended as not touching protected territory. That examination found a real gap the prior recommendation hadn't surfaced: "wiring the existing Tauri shell to Corytm Engine/Runtime through the sidecar" requires *some* real command/response channel between Tauri's Rust core and the Python sidecar carrying actual domain operations (create a project, move a clip) — and no such channel exists yet. FT-010/TK-011 only proved process lifecycle (a stdout `READY` signal, a stdin `SHUTDOWN` message) against a throwaway `sidecar_proof.py`, not a structured command protocol against the real `corytm` entry point; `docs/technical/architecture.md`'s own Unresolved section explicitly still lists "frontend design-system, component, state-management, and rendering choices" as deliberately open. Deciding how Desktop reaches Corytm Engine for the first time is the same class of decision ADR-006/ADR-007 already were — establishing how two of the architecture's named primary components communicate, for the first time, in a way future work will build on and that would be expensive to reverse — so, per `CLAUDE.md` §5 ("introducing a new architectural layer... or altering any other foundational architecture boundary") and its own explicit instruction ("When uncertain whether a proposal is protected, surface it for approval rather than deciding silently"), this was not decided silently. A recommendation was formed (reuse ADR-007's already-proven Protobuf-over-loopback-socket pattern symmetrically, with Rust as a second protocol client, over inventing a new stdio/JSON-line protocol on top of the existing sidecar spawn channel or a new HTTP/WebSocket layer) and put to the user rather than implemented, alongside the alternative of unblocking Dorian-model wiring instead. No Task was started this session; `Active Task` stays `none` pending that answer.

The user approved reusing ADR-007's typed Protobuf/local-loopback-socket pattern for the Desktop↔Python channel, with three explicit constraints: keep sidecar stdio lifecycle-only (no domain commands over stdin/stdout); Python core stays the sole authority over the Native Audio Runtime (Rust never connects to `native_runtime` directly); and use the narrowest implementation that does not unnecessarily couple the new Desktop channel to the existing Python↔native one. Recorded as **ADR-010** (`decisions/ADR-010.md`), including a second listener/secret pair fully independent of the Python↔Native Audio Runtime one, and `docs/technical/architecture.md` gained a new "Desktop↔Python Boundary" section plus an updated Unresolved entry. EP-009 (Desktop Command Channel) was created under MS-002, scoped — mirroring EP-004's own "prove the mechanism, then build on it" shape — to proving the channel itself before any real UI-facing domain command or Desktop UI work; refined into FT-019/TK-020 (the toolchain half: a Desktop-facing schema and Rust codegen, mirroring FT-008/TK-009 exactly), deferring the actual socket/transport proof to a later Feature, mirroring how FT-008 preceded FT-009 in EP-004's own sequence.

TK-020 was implemented via TDD: `src/schemas/desktop.proto` (one illustrative `DesktopProofMessage`, deliberately separate from `project.proto`'s native-facing envelope per ADR-010), and `src-tauri/build.rs` gained a `compile_desktop_proto()` step generating Rust bindings via `prost`/`prost-build`. Getting a `protoc` executable for Rust needed its own real design check rather than reaching for the first library found: the community `protoc-prebuilt` crate was fetched and read directly (not assumed from its name), and found to perform no cryptographic verification of what it downloads — only a `--version` string check — a real step down from this project's own established bar for this exact artifact (SHA256-pinned in CMake since TK-009/RSK-002); `protobuf-src` (compiling `protoc` from source at Rust build time) was also considered and rejected as needless duplicate build effort. `build.rs` instead downloads and SHA256-verifies the identical official `protoc` v36.1 release CMake already pins, independently of `check-native`'s build order (`Makefile`'s `check` target runs `check-desktop` before `check-native`), using `ureq`/`sha2`/`zip` — small, well-known, non-async build-only dependencies chosen over an unverified niche crate. `ureq` 3.x's actual API (`.call()?.body_mut().as_reader().read_to_end(...)`) and the `zip` crate's `by_name`/`std::io::copy` extraction were confirmed against real fetched documentation before writing the code, not guessed.

The new `desktop_proto_tests::desktop_proof_message_round_trips` test was confirmed genuinely failing to compile first (`include!` targeting a not-yet-generated file), then implementation made it pass. Validated locally beyond the Task's own minimum: a `cargo clean` followed by a fresh `cargo test` confirmed the entire pipeline — network download, SHA256 verification, zip extraction, codegen — genuinely works from a state with nothing cached (51s clean rebuild, test passing), not merely from a warm/lucky cache; `cargo build` showed zero new warnings; the full `make check-desktop` (release build, `.app`/`.dmg` bundling, `cargo test`) passed end to end, both the new test and the pre-existing `sidecar_shuts_down_via_explicit_protocol` test green; `git status` confirmed no file under `src/backend/audio/`, `src/backend/core/`, or `src/schemas/project.proto`/`proof.proto` was touched.

`RSK-010` was opened, not closed: this is a genuinely new cross-platform build-toolchain surface (a Rust build-time network fetch, SHA256 check, and zip extraction) exercised only on macOS this session, and this project's own history (RSK-002 through RSK-009) is direct, repeated precedent against assuming a clean macOS build predicts Windows behavior. TK-020, FT-019, and EP-009 all stay `active`, not `done`, pending a real, independently-verified Windows GitHub Actions run — this session cannot push (`CLAUDE.md` §10). Next: once that CI run confirms this surface, EP-009's remaining Feature (the actual Desktop↔Python socket transport — Python core opening the second listener, Rust connecting/authenticating/round-tripping a command) is the recommended next Task; real UI-facing domain commands and any Desktop UI/state-management work stay deliberately out of scope until the channel itself is proven end to end.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: rather than trusting TK-020/FT-019's documented `active` status at face value, it independently verified real available evidence first — `gh run list`/`gh run view`/`gh api` on run `33618388669` (commit `136abd7`) showed `check` passing on both `check (macos-latest)` and `check (windows-latest)`, and going beyond the job conclusion, both downloaded logs were grepped directly and show `test desktop_proto_tests::desktop_proof_message_round_trips ... ok` genuinely printed on both platforms with zero `warning:` lines touching `build.rs`/`lib.rs`/the desktop crate and zero `FAILED`/`error[` matches on Windows — the exact confirmation `RSK-010` was waiting on. TK-020, FT-019, and `RSK-010` were synchronized to `done`/`closed` on that evidence. Separately confirmed the newer `55ab538` commit (establishing `docs/product/design.md` as canonical) is correctly docs-only and untouched by CI (`paths-ignore`, confirmed via `gh run list --commit` returning zero runs) — no PMO record referenced it, so nothing needed reconciling there.

The session then took up the prior session's recommendation: EP-009's second Feature, the real Desktop↔Python socket transport. Checked first that this doesn't implicate a Protected Product Decision (`CLAUDE.md` §5): ADR-010 and EP-009's own already-approved scope explicitly call for exactly this ("the actual socket/transport proof... Python core opening the second listener, Rust connecting/authenticating/round-tripping a command"), so this is realizing already-agreed architecture, not introducing a new one. FT-020/TK-021 were created and refined, mirroring FT-009/TK-010's own shape one boundary later, with the two sides' roles swapped per ADR-010 (Python opens the listener and prints the handshake; Rust connects as the client).

Implemented via TDD, in the order the Rust test itself drives: `src/frontend/desktop/src-tauri/src/lib.rs` gained a `desktop_channel_tests` module containing `desktop_channel_round_trips_over_the_second_transport`, confirmed genuinely failing first (`corytm serve exited before completing the handshake`, since `main()` had no `serve` branch yet and silently fell through to its existing render-and-exit default). `src/backend/core/src/corytm/runtime/desktop.py` (new) then implemented `serve_desktop_channel()`: prints the existing `READY` signal, opens a second `asyncio.start_server` listener on its own port with a fresh `secrets.token_hex(16)` secret independent of whatever `_connected_native_runtime` later uses, prints a `DESKTOP <port> <secret>` line, requires the connecting client's first frame to equal the secret before accepting anything else, exchanges one `DesktopProofMessage` command/event round trip (replying with a distinct payload string that proves genuine server-side decode/re-encode rather than an echo), then blocks on stdin for `SHUTDOWN` exactly like `sidecar_proof.py` already does. `main.py` gained a one-line `serve` branch (`sys.argv[1:] == ["serve"]`) ahead of its existing default behavior, which the Task deliberately left untouched. `Makefile`'s `check-python` codegen line was extended to also compile `desktop.proto` for Python (previously Rust-only per TK-020's own explicit scope), giving `corytm.generated.desktop_pb2` for the first time. On the Rust side, `Cargo.toml`'s dev-dependency `tokio` gained the `net`/`io-util` features (no `Cargo.lock` change resulted — the underlying crates were already pulled in transitively), and the new test module implements `read_frame`/`write_frame` as local test-only functions replicating `corytm.runtime.transport`'s exact `<II>`-little-endian magic+length wire format — deliberately not a new reusable Rust production module, mirroring `transport_proof.cpp`/`test_transport_proof.py`'s own original precedent of proving a wire format in test code before any shared helper existed for it (that deduplication, on the Python↔native side, only happened later at FT-011/TK-012).

The round trip passed on the first real attempt after implementation, with no unexpected findings this time (unlike several of this Epic's prior Tasks). Validated locally beyond this Task's own minimum: `cargo test` (all 3 tests) with zero new warnings; `make check-python` (28 passed/6 deselected, pyright 0 errors, ruff clean); `make check-native` (`ctest` 5/5, untouched by this Task but re-run since `check` runs it before `check-transport`); `make check-transport` (6 passed/28 deselected — `test_entrypoint.py` specifically re-confirmed passing unchanged, proving `corytm`'s no-argument behavior is genuinely unaffected by the new `serve` branch); a full clean `make check` (all five targets) and `make check-desktop` (release build, `.app`/`.dmg` bundling, `cargo test`) both passed end to end. `git status` confirmed only this Task's own named files changed — no file under `src/backend/audio/` or `src/schemas/project.proto`/`proof.proto` was touched. A stray `first-sound.wav` produced in `src/frontend/desktop/src-tauri/` by the TDD red-phase run (the pre-implementation fallback path rendering into `cargo test`'s then-current working directory) was deleted as a trivially-reproducible local artifact, mirroring EP-005's own precedent, not a repo deliverable.

`RSK-011` was opened, not closed: this is a genuinely new combination — Rust as a raw TCP client authenticating against a real spawned Python server for the first time, `corytm`'s own real entry point branching into a persistent server mode, and a second concurrent `asyncio` listener port — exercised only on macOS this session, and this project's own history (RSK-002 through RSK-010) is direct, repeated precedent against assuming a clean macOS run predicts Windows behavior for a new process/socket combination. FT-020, TK-021, and EP-009 all stay `active`, not `done`, pending a real, independently-verified Windows GitHub Actions run — this session cannot push (`CLAUDE.md` §10). Once that run confirms this surface, EP-009 will be fully done (both its Features proven cross-platform), and the next MS-002 work — real UI-facing domain commands and/or Desktop UI/state-management, deliberately deferred by both of EP-009's Features — becomes a fresh scope decision for a subsequent session.

The user pushed TK-021's commit (`d88aabd`) and reported the CI run complete. Independently confirmed via `gh run list`/`gh run view`/`gh api` (run `33637336480`), not the report alone — `check` passed on both `check (macos-latest)` (5m22s) and `check (windows-latest)` (28m33s); both real downloaded logs show `test desktop_channel_tests::desktop_channel_round_trips_over_the_second_transport ... ok` inside `3 passed; 0 failed`, alongside the two pre-existing Rust tests unaffected, and `tests/test_entrypoint.py .` genuinely passing inside `6 passed, 28 deselected` on **both** platforms — confirming `corytm`'s no-argument behavior is genuinely unaffected by the new `serve` branch on Windows too, not merely macOS. Grepping both full logs for `warning:` touching the changed files, and for `FAILED`/`error[` on Windows, returned zero matches in either case. **TK-021, FT-020, `RSK-011`, and EP-009 are all closed `done`.** EP-009's Acceptance criteria are now fully satisfied on real evidence: both Features (Rust Protobuf codegen toolchain, the socket round trip) are proven cross-platform, and `docs/project/milestones/MS-002.md` was synchronized with EP-009's closure.

MS-002's Outcome still has two open threads, unchanged in kind from EP-008's own recommendation: no model/provider is wired up to call Dorian's tool yet (needs an explicit provider-selection/BYOK decision, `docs/product/strategy.md` §10's protected list — not yet made), and no real Desktop project-creation/editing UI exists — EP-009 proved the channel real UI would need, but built no UI, no domain command beyond the proof message, and no frontend design-system/state-management/rendering choice (all still explicitly open per `docs/technical/architecture.md`'s Unresolved section). Recommended as MS-002's next Epic: a first real UI-facing domain command over EP-009's now-proven channel (for example, a `CreateProjectCommand` or equivalent minimal command that lets a human trigger a real Corytm Engine operation from an actual Tauri window, however minimal the UI) — chosen over Desktop's full editing UI (too large a jump before any real command exists) and over Dorian-model wiring (blocked on a protected provider decision) as the smallest next vertical slice that makes real progress on the still-open manual-editing half of this Milestone's Outcome. Not started this session — `CLAUDE.md` §6 caps one session at one Task, and this session's own Task (TK-021) was already executed in the immediately preceding session; this session's own work was reconciling PMO state to the real CI evidence, per its continuity-check obligation, not new implementation. Presented here as a recommendation to confirm or redirect before Epic/Feature/Task-level definition begins.

A later session took up that recommendation, refining it into EP-010 (Desktop's First Real Domain Command), scoped into three increments mirroring this Milestone's own established shape: production sidecar spawn/shutdown wiring (FT-021, this Epic's first Feature — the real app has never itself spawned anything; only tests have), a real command/event pair reusing the already-proven move-clip mechanism (a later Feature), and a minimal UI trigger (a later Feature still). Checked first that this doesn't implicate a Protected Product Decision: MS-002's own Outcome already requires manual editing through the real stack, so this Epic formalizes already-agreed behavior.

FT-021/TK-022 executed FT-021's own scope. `spawn_desktop_sidecar<R: tauri::Runtime>` was factored out of TK-021's own test-only spawn/handshake-parsing logic into real production code in `lib.rs`, generic over `Runtime` (confirmed against `tauri_plugin_shell::ShellExt`'s own trait bound, not assumed) so both the real app and `tauri::test::MockRuntime` call the identical function — mirroring this project's own established practice of promoting test-proven logic into shared production code once genuinely reused (TK-012's earlier `transport.py` precedent). `run()` now registers the shell plugin, spawns the sidecar via a background `tauri::async_runtime::spawn` task from `.setup()` (not blocking startup), stores the resulting `CommandChild` in `Manager`-based app state, and — having switched from `.run(context)` to `.build(context)?.run(callback)` specifically to gain access to `RunEvent` — writes `SHUTDOWN` to the child on `RunEvent::ExitRequested`/`Exit`, best-effort and idempotent. The existing `desktop_channel_round_trips_over_the_second_transport` test was updated to call the new shared function instead of its own copy, with its own round-trip assertions otherwise unchanged. Every relevant Tauri API (`Builder::build`/`App::run(callback)`, `Manager::manage`/`state`, `tauri_plugin_shell`'s exact `Receiver` type) was confirmed by reading the actual vendored crate source before use, not guessed — `tauri::async_runtime::Receiver` (already re-exported by `tauri` itself) was used in place of a direct `tokio` dependency after confirming `Command::spawn()`'s real return type, avoiding an unnecessary new `Cargo.toml` entry entirely.

Validated beyond this Task's own minimum: `cargo build --lib` and, distinctly, `cargo build --release --bin app` (the actual shipped binary) both clean with zero warnings; `cargo test` (all 3 tests) passes; `make check-desktop` and a full clean `make check` (all five targets) both passed end to end. `git status` confirmed only `lib.rs` changed under `src/` (no `Cargo.toml`/`Cargo.lock` change) — nothing under `src/backend/audio/`, `src/backend/core/`, or any schema file was touched.

`RSK-012` was opened, not closed: this is a genuinely new combination — `async_runtime::spawn` fired during `.setup()`, cross-task `Manager`-based shared state, and `Builder::build().run(callback)` observing `RunEvent` for the first time (every prior Task used the simpler `.run(context)`) — exercised only via `cargo test`'s mock app and a local release build on macOS this session, never a real running app on either platform. FT-021, TK-022, and EP-010 all stay `active`. Two things remain before this Task can close, deliberately kept distinct: a real, independently-verified passing Windows GitHub Actions run (this session cannot push, `CLAUDE.md` §10), and the human confirmation step TK-022's own acceptance criteria calls for — launching `npm run tauri dev`, quitting the app, and confirming via `ps`/Activity Monitor that the spawned `corytm serve` process is actually gone afterward. This session deliberately did not attempt that confirmation itself: unlike EP-005's audible check (running a CLI command the user already expected), actually opening a real GUI window on the user's own desktop without them first choosing to do so was judged an unwarranted surprise action, better left to the user to trigger and observe directly.

The user then performed that confirmation directly on macOS and reported real `ps` evidence, not just a visual impression: while `npm run tauri dev` was open, `ps aux | grep -i "corytm serve"` showed both the `uv run --project ../../../backend/core corytm serve` launcher and the spawned `.../corytm serve` Python process genuinely running; after closing the Corytm window (the real window-close path, confirmed to be what was used rather than `Ctrl+C`, which would have bypassed the `RunEvent` shutdown hook entirely), the identical check returned zero matches. This confirms the real production app — not only `cargo test`'s mock app — genuinely spawns the sidecar via `spawn_desktop_sidecar` and that the `RunEvent::ExitRequested`/`Exit` handler's `SHUTDOWN` write reaches the child in time for a clean exit. TK-022's Notes and `RSK-012` were updated with this evidence. The sole remaining item before TK-022, FT-021, and EP-010 can close is a real, independently-verified passing Windows GitHub Actions run.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: `git status` confirmed a clean working tree with `HEAD` (`09aa8b9`) already pushed to `origin/main`, matching this file's own documented state exactly. Rather than trusting the prior session's "left active pending Windows CI" note at face value, real evidence was independently pulled: `gh run list` confirmed run `33644724394` is the `check` run for `09aa8b9` (verified by `git log -1` and by matching `headSha` in the run's own JSON, not assumed from recency); `gh run view`/`gh api` showed `check` passing on both `check (macos-latest)` and `check (windows-latest)`, and the downloaded job logs were grepped directly — both platforms show `desktop_channel_round_trips_over_the_second_transport ... ok` inside `3 passed; 0 failed` and `tests/test_entrypoint.py .` inside `6 passed, 28 deselected`; Windows produced zero `warning:` lines, macOS's 185 were all confirmed under vendored `_deps/juce-src`; no `FAILED`/`error[` on either platform. This is exactly the real Windows confirmation `RSK-012` and TK-022's own acceptance criteria were waiting on. **TK-022 and FT-021 are closed `done`; `RSK-012` is closed.** EP-010 stays `active` — increment 1 of 3 (production sidecar spawn/shutdown) is done; increments 2 (a real command/event pair over the Desktop channel) and 3 (a minimal UI trigger) remain.

Before refining EP-010's next increment, the user asked for a PMO operating-model review: Epic/Feature/Task currently sit at a near-1:1 Feature:Task ratio (21 Features to 22 Tasks — confirmed by mapping every Task's `Feature:` field — with only FT-005 having two Tasks), and every Feature has so far forced its own independent commit/push/CI-confirmation cycle, per `CLAUDE.md` §6's current "one session executes exactly one Task" rule and §7/§10's Task-level Close/Git-handoff wiring. The proposed change makes Feature the normal unit of session scope and external delivery (commit/push/CI confirmation), with Task as a bounded internal implementation/validation step (still requiring its own prospective scope/acceptance criteria and local validation) — several Tasks integrating into one Feature before a git/CI cycle. This is a governance change to `CLAUDE.md` §5/§6/§7/§10 and format.md, requiring explicit user approval before it is applied; it was reviewed and proposed, not applied, in this session — see the session's own proposal for the exact diffs and open questions. No product implementation was started this session; EP-010's next increment remains the recommended next work once the operating-model question is resolved.

That operating-model work was then reviewed and refined across further rounds, explicitly approved, and applied to `CLAUDE.md`, `docs/project/format.md`, `docs/project/plan.md`, and this file — canonical now in those documents, not restated here. **Granularity note:** every narrative paragraph above this one, and every EP-001–EP-010/FT-001–021/TK-001–022 record it references, was produced and sized under the prior Task-per-session delivery model — accurate, evidence-backed history, not a template for how large a new Feature should be. Future sizing follows `docs/project/format.md`'s Sizing test and Task-creation Threshold, not this history's own granularity.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: `git status`/`git log` confirmed a clean working tree with `HEAD` (`1641872`, the operating-model amendment's own commit) already pushed to `origin/main`, matching this file's own documented state exactly — no stale record to reconcile. It then took up EP-010's own recommendation, first re-reading the newly-applied Feature-level delivery model, EP-010's Sizing note, and the actual code (`desktop.py`, `session.py`, `dorian/tools.py`, `lib.rs`, `build.rs`, `App.tsx`) rather than assuming the increment-2/3 framing still fit as originally worded. Confirmed EP-010's own increment 2/3 shape still holds as one coherent capability (a real move-clip command/event pair over the Desktop channel, plus a UI trigger for it) and created **FT-022** accordingly, decomposed into **TK-023** (backend command/event wiring: reuse `project.proto`'s `MoveClipCommand`/`ClipMovedEvent` by reference per ADR-010 rather than a new wrapper envelope, since only one Desktop command type exists so far; extract a `move_clip_in_session` function shared between Dorian's existing tool and the new Desktop handler, kept in `runtime` — not `dorian` — since `docs/technical/architecture.md`'s Dependency Direction forbids Runtime depending on Dorian) and **TK-024** (the UI trigger, gated behind TK-023). Both meet the Task-creation Threshold: each is a genuinely new technical surface (real product schema crossing into Rust codegen for the first time; Tauri's app-command ACL system for the first time) and a meaningful independently resumable package. `RSK-013`/`RSK-014` opened for each, mirroring this project's own established practice for a new toolchain/behavioral combination. No Protected Product Decision is implicated (EP-010's own Notes already established this). Before writing any product code, Tauri's exact app-defined-command ACL mechanism was confirmed against Tauri's own real, currently-shipping `examples/api/src-tauri` (`build.rs`'s `AppManifest::new().commands(&[...])`, the auto-generated `permissions/autogenerated/*.toml`, and `capabilities/*.json`'s bare `"allow-<command>"` references) fetched via `gh api`, not guessed — recorded in FT-022's Notes for TK-024 to apply directly.

TK-023 was then implemented via TDD end to end: `move_clip_in_session` (new, in `runtime/session.py`) composes Engine's `with_clip_moved` and `materialize_then_move_clips`, shared by both `dorian.tools.move_clip` (refactored to delegate, `corytm.runtime` still never depending on `corytm.dorian`) and `runtime/desktop.py`'s `serve_desktop_channel`, which now reads a real `MoveClipCommand` against a hardcoded fixture project and returns a real `ClipMovedEvent` — no longer `DesktopProofMessage`. On the Rust side, `build.rs` was extended to also compile `project.proto` for Rust, and the existing Desktop-channel round-trip test was updated to send/receive the real message pair. A first attempt to also promote `write_frame`/`read_frame` to shared production functions in this same Task was caught and reverted after `cargo build --lib` showed genuine `dead_code` warnings (no production caller existed yet) — deferred to TK-024 instead, mirroring TK-022's own precedent of promoting test-proven logic to production exactly when a production caller needs it. A full clean `make check` (all five targets, including a from-scratch native rebuild and `make check-desktop`) passed end to end with zero new warnings; `git status` confirmed only the declared files changed. TK-023 closed `done` on local validation alone — this session corrected its own initially-drafted acceptance criteria, which had wrongly mirrored the prior per-Task CI-gating pattern; under the now-applied Feature-level model, Windows CI evidence gates FT-022 as a whole (once pushed), not each Task individually (`docs/project/format.md`'s Task Format: a Task only carries its own Evidence field for an explicit `CLAUDE.md` §6 intermediate-checkpoint exception, which does not apply here).

TK-024 followed, also via TDD: `write_frame`/`read_frame` were promoted to real production functions now that a genuine caller exists; a new `#[tauri::command] async fn move_clip` connects to the Desktop channel using port/secret captured into managed state right after `spawn_desktop_sidecar` succeeds, sends one hardcoded `MoveClipCommand` matching `desktop.py`'s fixture exactly, and returns the real `ClipMovedEvent` as JSON; `build.rs`/`capabilities/default.json` wire its ACL permission per the pattern confirmed above; `.gitignore` gained an entry for the resulting auto-generated `permissions/autogenerated/`, mirroring the existing `/gen/schemas` precedent. The prior raw-socket test was replaced by one driving the real `move_clip` command directly — strictly stronger coverage, though confirmed (by reading `tauri-utils`' `mock_context` source) not to exercise real ACL dispatch, which only the human confirmation step below can. `@tauri-apps/api` was added and `App.tsx` gained one button invoking the command and displaying its result, validated via `tsc -b`/`vite build`/`oxlint`/`vitest` (no component-testing framework introduced, matching this Task's function-over-design scope). A full clean `make check` passed end to end again. `docs/technical/architecture.md`'s Unresolved section was synchronized: the Desktop channel now carries a real UI-facing domain command, not only a proof-of-mechanism message.

TK-024 stayed `active`, not `done`, pending exactly the human confirmation its own acceptance criteria named. The user then ran `npm run tauri dev` on macOS, clicked the button, and reported the real returned result (`moved: true`, `start_seconds: 1`, `rendered_sample_count: 132300` — exactly matching `(1.0 + 2.0) × 44100`, `peak_amplitude: 1`), independently confirming the canonical-state edit, the real re-render, and the real ACL/invoke wiring all worked correctly in the actual production app — not merely `cargo test`'s mock context, which this session had confirmed does not exercise real ACL dispatch. **TK-024 and both of FT-022's Tasks are now closed `done`.**

FT-022 itself stays `active`, per the user's explicit instruction: it reaches `validating` only once its delivery commit is confirmed pushed (not yet true — this session cannot push, `CLAUDE.md` §10), and `done` only once a real, independently-verified passing Windows GitHub Actions run confirms `RSK-013`/`RSK-014`. EP-010 stays `active` accordingly — it has no further scope once FT-022 closes. Recommended once both resolve: EP-010 will be fully done, and the next MS-002 thread — Dorian-model/provider wiring (still blocked on an explicit provider-selection decision) or real Desktop project-creation/persistence UI beyond this one hardcoded move — becomes the next scope decision.

The user did push FT-022's delivery commit (`c88d7a6`) and ran it; a real `check` run started on both `macos-latest` and `windows-latest` (`33657379249`). That run never completed: both platforms' `cargo test` step hung indefinitely at the new `desktop_channel_tests::move_clip_command_moves_the_fixture_clip_and_renders_the_effect` test — libtest's own "still running" watchdog fired, but the test never finished, panicked, or timed out on its own. A later session was asked to independently diagnose this rather than assume the command itself was broken (real macOS UI/manual acceptance and a full local `make check` had both already passed before push).

Systematic diagnosis (each step reproduced locally, not reasoned about abstractly): direct Python-only driving of `corytm serve` proved the command/event round trip itself works correctly end to end. Removing the locally-built `native_runtime` binary reproduced a genuine, indefinite hang — not the fast `FileNotFoundError` its own code should produce — which led to two distinct, real, confirmed defects, not one: (1) `Makefile`'s `check` target ran `check-desktop` (needs `native_runtime` for this Feature's new test, for the first time) *before* `check-native` (builds it) — invisible locally, where a previously-built binary already exists, but real on a fresh CI checkout, where nothing restores it; (2) a genuine `asyncio` teardown deadlock, confirmed with a minimal isolated reproduction before touching any production file: Python 3.12.1 changed `asyncio.Server.wait_closed()` to also wait for every accepted connection to drop, not just the listening socket, and both `runtime/desktop.py`'s `serve_desktop_channel` and `runtime/session.py`'s `_connected_native_runtime` close that connection's writer only on their success path — any exception after a client connects but before that point leaves the server waiting forever on a connection nobody will ever close, symmetrically deadlocked against the client (Rust's own `move_clip()`, itself with no timeout). This is a pure Python-stdlib mechanism, explaining why both platforms hung identically, and was newly exposed by this Feature specifically because it is the first Desktop-channel path where a real post-connection failure is reachable at all — prior Desktop-channel tests never had a failure mode to trigger it.

Both were fixed via TDD (two new regression tests written first, confirmed failing with a 15s timeout against the unfixed code, then passing in under a second after the fix — `tests/test_desktop_channel.py` and a new case in `tests/test_move_clip_in_session.py`), plus the `Makefile` reorder. The Rust test itself was separately hardened as defense in depth — a `SidecarGuard` RAII wrapper (`CommandChild` has no `Drop`-based cleanup, confirmed against `tauri_plugin_shell`'s source) and a bounding timeout on the previously-unbounded `move_clip(app.state()).await`, matching the pattern its sibling awaits already used. Full detail, including the exact reproduction steps, is in FT-022's own Notes. Validated: the exact previously-hanging test run repeatedly (13+ times, with and without `native_runtime` present) with zero hangs and zero leaked processes, and a full clean `make check` (all five targets) passing end to end. Per `CLAUDE.md` §6, this is not an infrastructure-only failure, so FT-022 reconciles `validating → active` rather than staying `validating` pending a mere re-run; `RSK-013`/`RSK-014` remain open, genuinely unconfirmed by the one real run obtained (it never reached either risk's own surface). The next Feature does not start while FT-022 requires this corrective work.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: rather than trusting the prior session's documented `active` status at face value, real evidence was independently pulled first. `git log`/`git status` confirmed the corrective fix is committed as `e3bd5a3` and already pushed (`origin/main`'s `HEAD` matches exactly — this repository's own governance never has Claude push, so this was done directly by the user between sessions). `gh run list`/`gh api` against `e3bd5a3` found a real `check` run (`33666555272`) genuinely in progress on both `check (macos-latest)` and `check (windows-latest)` — not yet complete, so not yet evidence either way. On that basis FT-022 reconciles `active → validating` (delivery commit confirmed pushed, required evidence genuinely pending, distinct from the corrective work itself still being unpushed) — see FT-022's own Notes for the full reconciliation record. `status.md`'s Active/Validating Feature pointers were updated accordingly.

Before selecting any new work, the dependency-safety check (`CLAUDE.md` §6) was applied rather than assumed: `RSK-013`/`RSK-014` explicitly name the still-unconfirmed surface (the Desktop channel's new `project.proto`-based command/event pair, and Rust's first `prost`-generated nested-message codegen) as exactly the mechanism any further real Desktop UI/editing work would build on next, so starting that now would depend on this pending evidence turning out correct — the check's own named case for waiting, not mere subsystem proximity. Dorian-model/provider wiring remains separately blocked on an explicit, not-yet-made protected provider-selection decision (`docs/product/strategy.md` §10), independent of this Feature. No Feature is safe to make Active while FT-022 resolves; this session waits for the in-progress run rather than starting new implementation work.

The user reported the watched run complete. Independently confirmed, not taken on the report alone: `gh run view --json status,conclusion,headSha,jobs` showed `33666555272` `"status":"completed"`, `"conclusion":"success"`, `headSha` matching `e3bd5a3` exactly, both `check (macos-latest)` (8m3s) and `check (windows-latest)` (~28m32s) `"conclusion":"success"`. Went beyond the job conclusion, per this project's own established practice: both full job logs were downloaded (`gh run view --log`) and grepped directly. Both platforms show `test desktop_channel_tests::move_clip_command_moves_the_fixture_clip_and_renders_the_effect ... ok` — the exact test that hung indefinitely in the prior run, now genuinely completing — inside `test result: ok. 3 passed; 0 failed`; zero occurrences of libtest's hang-watchdog line on either platform; zero `error[`; both new regression tests (`tests/test_desktop_channel.py`, `tests/test_move_clip_in_session.py`) genuinely ran on both platforms; zero warnings touch `desktop.py`, `session.py`, or `lib.rs`. This is real, independently-confirmed cross-platform evidence that the corrective fix genuinely works, not just a green checkmark. **FT-022, TK-023, TK-024, and EP-010 are all closed `done`; `RSK-013` and `RSK-014` are closed.** `docs/project/milestones/MS-002.md` was synchronized with EP-010's closure.

MS-002's Outcome still has two open threads, unchanged in kind from EP-008's own recommendation: no model/provider is wired up to call Dorian's tool yet (needs an explicit provider-selection/BYOK decision, `docs/product/strategy.md` §10's protected list), and real Desktop project-creation/persistence UI beyond this one hardcoded move does not exist. This session examined the second thread rather than deferring it by habit: "project creation, opening, or persistence" was EP-010's own explicit Out-of-scope language, and building it now means deciding, for the first time, how a Corytm project is actually stored and reopened — squarely the kind of decision `CLAUDE.md` §5 names directly ("changing the local-first persistence strategy") and `docs/product/business-model.md`'s "shared-project/local-first-cloud principles" list as a Protected Product Decision. Establishing that strategy for the first time is the same class of act as changing it — there is no existing strategy to silently default to — so, mirroring this Milestone's own ADR-006/ADR-007/ADR-010 precedent of surfacing exactly this kind of foundational, expensive-to-reverse decision rather than deciding it silently, this was not started. Both of MS-002's remaining threads are therefore presented to the user as a choice rather than one being picked autonomously: (1) Dorian-model/provider wiring, which needs an explicit provider-selection/BYOK decision first, or (2) real Desktop project-creation/persistence UI, which needs an explicit local-first persistence-strategy decision first. `Active Feature` stays `none — awaiting selection` pending that answer.

A subsequent session evaluated the local-first persistence-strategy decision blocking MS-002's remaining Desktop project-creation/persistence thread, per the user's explicit request, without implementing product code. It proposed the narrowest viable decision (a single local JSON file, Corytm Engine as sole reader/writer, reusing `project.proto`'s existing `Command`/`Event` oneof and ADR-010's channel rather than any new transport or architectural layer), named and rejected three alternatives (protobuf-as-file-format, SQLite, a generic pluggable storage-backend abstraction), and separately assessed that the next substantial Desktop UI Feature (project create/open/save) is a genuine trigger to materialize `docs/product/design.md`'s design-system direction for the first time, and that a narrow, procedural UI/UX review skill grounded in that document is now justified.

The user approved both, with two refinements to the persistence decision before acceptance: (1) explicitly disclaiming any backward-compatibility guarantee for Alpha-era project files rather than implying indefinite compatibility from the mere presence of a version field; (2) reframing the durable contract as Corytm's own versioned JSON format — whose initial payload shape mirrors the canonical Pydantic `Project` model, with Pydantic named only as the current implementation mechanism — rather than letting Pydantic's exact serialized shape become the permanent contract by default. **ADR-011** (Local JSON File as Corytm's Alpha Project Persistence Format) was recorded incorporating both refinements; `docs/technical/architecture.md` gained a new "Local Project Persistence" section, and its Unresolved section was updated to distinguish what ADR-011 now resolves (the local single-file storage mechanism) from what remains genuinely open (cloud/sync persistence, migration/backward-compatibility policy, any future project-bundle/resource format, and the actual project-creation/opening/saving commands and UI still to be built on this mechanism).

The user also approved creating `.claude/skills/ui-ux-review/` now — a narrow, procedural checklist (reuse/composition, semantic tokens, dark/light support, accessibility, component-catalog impact, third-party primitive fit, surface-specific visual behavior, Dorian integration) grounded directly in `docs/product/design.md` rather than restating it, scoped to user-facing Feature Review steps, and deliberately not a general-purpose design agent; `.claude/skills/README.md`'s "Current state" was updated to reflect it. Per the user's explicit instruction, EP-011 and any Feature/Task decomposition were deliberately not created: the persistence/channel/UI three-part split discussed during evaluation is a hypothesis for a future session to size fresh under `docs/project/format.md`'s Sizing test and Task-creation Threshold, not a pre-approved structure. No product code was touched.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: `git log`/`git status` confirmed `HEAD` (`61d2772`, the ADR-011/UI-UX-review-skill commit) matches this file's own documented state exactly, working tree clean and already pushed — no stale record to reconcile. It then took up the recommendation ADR-011 had left open: real Desktop project-creation/persistence UI was now unblocked. **EP-011 (Real Desktop Project Persistence)** was created under MS-002, sized (per its own Sizing note) into a backend-first Feature — mirroring EP-009→EP-010's own precedent — and **FT-023 (Engine Persistence and Desktop Channel Create/Open/Save Commands)** was created and decomposed into **TK-025** (the Engine persistence module) and **TK-026** (the Desktop channel's new commands and multi-command session restructuring). No Protected Product Decision was implicated: ADR-011 already settled the storage mechanism; this Epic only builds the real commands/UI on top of it.

TK-025 implemented ADR-011's envelope as `corytm.engine.persistence` (`save_project`/`load_project`, a `ProjectFileError` for a corrupt/mismatched-version file), via TDD, with 7 passing tests. TK-026 extended `project.proto`'s `Command`/`Event` oneof with `CreateProjectCommand`/`SaveProjectCommand`/`OpenProjectCommand` and their events, and restructured `serve_desktop_channel()` from "accept one connection, handle exactly one hardcoded `MoveClipCommand`, close" into a multi-command session loop (`_run_session`/`_dispatch_command`) — mirroring the multi-command loop `native_runtime.cpp` already established at FT-017/TK-018 — seeded with the existing hardcoded fixture project so the already-shipped move-clip button's behavior is unchanged. Reusing `project.proto`'s own `Command`/`Event` oneof for the Desktop channel (rather than a bare message, TK-023's original choice) was a deliberate, reasoned reversal: TK-023's own Notes had predicted this project's precedent (an enveloping oneof only once ≥2 real command types exist) would apply "once" a second Desktop command type existed — this Feature adds three at once, so the existing `move_clip` Rust caller and its test were updated to the new enveloped wire format alongside it, with no change to the JSON shape returned to the frontend. Three new automated Python tests drive the real `corytm serve` sidecar over one connection (create→save; open+re-materialize+move-clip; move-clip alone with no prior command, proving no regression), using a new `_FakeStdin` helper so the test can exercise the server's full lifecycle — including the post-session stdin wait — without hitting pytest's own stdin-capture guard. During Review, a dead-code path was found and removed: `_dispatch_command`'s originally-planned `current_project is None` guards for `save_project`/`move_clip` could never trigger, since the session loop always seeds the slot first — simplified to a plain `Project` parameter.

Validated beyond both Tasks' own minimum: a full clean `make check` (all five targets, including a from-scratch native rebuild, `make check-desktop`'s release build/`.app`/`.dmg` bundling, and `cargo test`) passed end to end with zero new warnings anywhere. `docs/technical/architecture.md`'s "Local Project Persistence", "Desktop↔Python Boundary", and Unresolved sections were synchronized. `RSK-015` opened for the genuinely new combination this Feature introduces (a Desktop-channel multi-command session; Rust's first decode of a discriminated `Event` oneof response) — locally verified on macOS only; a real, independently-verified passing Windows GitHub Actions run remains the one thing this session cannot supply (`CLAUDE.md` §10). FT-023 stays `active`, not `validating`, pending that push. Once it closes, EP-011's remaining scope — a native OS file dialog (`@tauri-apps/plugin-dialog`) and a minimal UI to trigger create/open/save — is recommended as the next Feature.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: `git status`/`git log` confirmed a clean working tree with `HEAD` (`72a3141`, TK-025/TK-026's own delivery commit) already matching `origin/main` exactly — the user pushed it between sessions, which this repository's own governance never has Claude do directly. Rather than trusting FT-023's documented `active` status at face value, real evidence was independently pulled: `gh run list`/`gh api` confirmed `72a3141d65dda763663dde02e78c699c032a36e8` is genuinely on `origin/main`, and that a real `check` Actions run (`33673521046`) — matched to this exact commit's `headSha`, not assumed from recency — was in progress on both platforms (a duplicate same-commit run, `33673520421`, had already self-cancelled, consistent with this repository's own `concurrency` setting). On that basis FT-023 reconciles `active → validating` (delivery commit confirmed pushed, required Windows CI evidence genuinely pending) — see FT-023's own Notes for the full reconciliation record. `status.md`'s Active/Validating Feature pointers and EP-011's Notes were updated accordingly.

Before selecting any new work, the dependency-safety check (`CLAUDE.md` §6) was applied rather than assumed: `RSK-015` explicitly names the still-unconfirmed surface (the Desktop channel's new multi-command session loop; Rust's first decode of a discriminated `Event`-oneof response) as exactly the mechanism EP-011's own remaining scope — a native file dialog and UI calling these same `create_project`/`save_project`/`open_project` commands — would build on next, so starting it now would depend on this pending evidence turning out correct. Dorian-model/provider wiring remains separately blocked on an explicit, not-yet-made protected provider-selection decision (`docs/product/strategy.md` §10), independent of this Feature. No Feature is safe to make Active while FT-023 resolves; this session watches the in-progress run rather than starting new implementation work.

The user then reviewed that block against its first real use and judged it too conservative: an open Risk naming a surface the new Feature merely consumes was being treated as sufficient reason to wait, even when the new Feature's own contract is already locally validated and a plausible CI failure would most likely need only an internal or platform-specific fix rather than invalidate that contract — exactly what happened above. The user proposed the corrected test (implementation dependency alone does not block; only a plausible failure that would materially invalidate an assumption the new Feature relies on — a command/event shape, function signature, lifecycle guarantee, ordering contract, persistence semantic, or other established interface/behavior — does; bounded downstream rework from a later internal fix is an accepted cost; and if the Validating Feature's resolution does turn out to invalidate an assumption the new Active Feature already built on, that Active Feature pauses and reconciles against it) and asked for a sanity check against this exact FT-023 → EP-011 case before approving. That check confirmed the corrected rule would not block EP-011's next Feature here: this project's own risk history (`RSK-002` through `RSK-015`) shows FT-023-shaped failures are almost entirely internal/platform-specific (an `asyncio` teardown bug, a build/link mismatch) that don't change the already-locally-validated `Command`/`Event` shapes or the connect/send/receive pattern EP-011's next Feature would reuse (the same pattern `RSK-014` already closed cross-platform for `move_clip`). With that confirmed, the user approved and **`CLAUDE.md` §6's dependency-safety paragraph was amended accordingly** — the Risk-present branch no longer treats the new Feature's work touching the named surface as sufficient by itself; it now applies the same invalidation test as the Risk-absent branch, with the broadened contract examples above, and a new closing sentence requires pausing the new Active Feature if the Validating Feature's resolution later does invalidate an assumption it already built on. The one-active/one-validating cap and the requirement to reconcile a Validating Feature's evidence immediately on resolution are both unchanged.

Re-read for internal consistency after applying (one redundant clause tightened during that re-read, no substance change). On that corrected rule, and with FT-023's real Windows run (`33673521046`) still in progress, this session proceeded rather than waiting synchronously for CI: **FT-024 (Native File Dialog and Create/Open/Save UI)** was created under EP-011 and made the new Active Feature, decomposed into **TK-027** (Rust: `tauri-plugin-dialog` registration, a persistent Desktop-channel connection replacing the previous one-shot-per-command pattern, `create_project`/`save_project`/`open_project` commands, `move_clip` migrated onto the same shared connection) and **TK-028** (the minimal React UI triggering native save/open dialogs and these commands, plus the human confirmation step EP-011's own acceptance criteria requires for real native-dialog behavior). See `docs/project/epics/EP-011.md` and `docs/project/features/FT-024.md` for the full record.

TK-027 was implemented via TDD: the new multi-command-sequence `cargo test` was written first and confirmed failing, then the persistent connection, `send_command` helper, and `create_project`/`save_project`/`open_project` commands were added, with `move_clip` migrated onto the shared connection. That test, and the pre-existing `move_clip` test, then failed with "timed out waiting for the sidecar to terminate" — a real design bug caught by running against reality, not a test artifact: holding the connection open (rather than reopening it per call, `move_clip`'s original pattern) meant the Python session loop stayed blocked reading the next command and never reached its post-session stdin `SHUTDOWN` wait, which would have hung the real production app on quit too. Fixed by explicitly closing the held connection before writing `SHUTDOWN`, both in test cleanup and in `run()`'s own shutdown handler. TK-028 added the New Project/Save/Open UI controls (Save/Open resolving a real path via `@tauri-apps/plugin-dialog` before invoking the command), with Move Clip defensively disabled once a project without the hardcoded fixture's ids might be current — flagged, not fixed, since the underlying gap (the Desktop channel session has no defined recovery for a command naming a nonexistent id) belongs to `runtime/desktop.py`, outside this Feature's declared scope. `RSK-016` opened for the two genuinely new surfaces (`tauri-plugin-dialog`; the persistent mutex-guarded connection). The `ui-ux-review` skill found no blocking gaps in the added UI. A full local `make check` (all five targets, including a from-scratch native rebuild and `make check-desktop`'s release build) passed end to end with zero new warnings; `git status` confirmed only this Feature's own named files changed.

The FT-023 CI run (`33673521046`) completed during this implementation work. Per instruction, its evidence was independently reconciled at this natural checkpoint (TK-027/TK-028 already locally validated) rather than left for later: `gh run view --json status,conclusion,headSha,jobs` confirmed `headSha` matches `72a3141` exactly, both platforms `"conclusion":"success"` (macOS 7m33s, Windows 28m2s); both full job logs were downloaded and grepped directly, showing `tests/test_desktop_channel_session.py` genuinely running and `test desktop_channel_tests::move_clip_command_moves_the_fixture_clip_and_renders_the_effect ... ok` inside `3 passed; 0 failed` on Windows too — `RSK-015`'s own named surface, confirmed clean, with zero `FAILED`/`error[` anywhere. **FT-023, TK-025, TK-026, and `RSK-015` are all closed.** This also confirms the corrected dependency-safety check's own prediction from the sanity check above: FT-023 resolved with no failure of any kind, so FT-024 — already implemented in parallel by this point — needed no pause or reconciliation against it.

FT-024 stays `active`: implementation and local validation are complete, but two things remain outside this session's own reach — the human confirmation step (real native save/open dialogs via `npm run tauri dev`, deliberately left for the user to trigger and observe directly, mirroring TK-022's own precedent) and, after a human pushes this Feature's delivery commit, a real independently-verified passing Windows GitHub Actions run confirming `RSK-016`.

The user then performed that human confirmation directly: New Project and Save worked (real native save dialog, clean quit/relaunch), but Open failed with `failed to read event: early eof`. Per the user's explicit instruction, this was not recorded as passed, `RSK-016` stayed open, and the failure was diagnosed as a real FT-024 product-path defect rather than retried.

Root cause, traced end to end rather than assumed: the saved file held a genuinely empty project — `create_project` produces zero tracks, and nothing before FT-024 had ever sent a zero-track project through the real materialize/render path (every prior Feature used a hardcoded one-track/one-clip fixture). `native_runtime.cpp`'s `materialize()` correctly sends back a valid `ProjectRenderedEvent` for an empty project (zero sample count/peak amplitude), but the *process's own exit code* also reflects that render as unsuccessful — a convention from EP-005/EP-006 meant to signal outcome to whatever spawns it. `corytm.runtime.session._connected_native_runtime` (shared by `materialize_project` and `materialize_then_move_clips`) treated any non-zero exit code as a hard failure and raised `RuntimeError`, even though the accurate `Event` had already been successfully read — and that exception, raised as the caller's `async with` block unwinds, always won over the already-successful read. Nothing in the Desktop channel's session loop catches it, so it crashed the whole `corytm serve` process before any response frame reached Rust — exactly "early eof." Confirmed by direct reproduction, not reasoning alone: calling `materialize_project` with an empty project raised the identical `RuntimeError: native_runtime exited with code 1`; driving the exact New→Save→quit→relaunch→Open path via two independent `serve_desktop_channel()` sessions (mirroring a real relaunch as closely as this project's in-process test harness allows) raised the client-side mirror of the same symptom, `IncompleteReadError: 0 bytes read on a total of 8 expected bytes`.

Fixed via TDD: two new tests (`tests/test_first_sound.py::test_materialize_project_with_no_tracks_returns_a_zero_render_event`, `tests/test_desktop_channel_session.py::test_open_project_after_relaunch_with_no_tracks_succeeds`) were written and confirmed failing with those exact errors first. `_connected_native_runtime` no longer raises based on `native_runtime`'s exit code — a caller that already read every `Event` it expected already has the real outcome; the process is still awaited/reaped so it can't linger, and a genuine hang still surfaces as `TimeoutError`. Both callers' docstrings corrected to match. This touches `corytm.runtime.session` (CTO/AI & Sound ownership), outside TK-027/TK-028's own declared Rust/frontend scope — touched directly as the smallest necessary change, per `CLAUDE.md` §7, since it is FT-024's own genuine root cause, not an unrelated opportunistic fix. Both new tests now pass; a full local `make check` (all five targets) re-passed end to end, including `make check-python` (38 passed, pyright 0 errors, ruff clean) and `make check-transport` (12 passed) re-run explicitly since shared production code changed. `git status` confirmed only `session.py` and the two test files changed beyond FT-024's prior diff.

**FT-024 remains `active`; `RSK-016` remains open**, exactly as instructed: the fix is implemented and locally validated, but the human confirmation step must be repeated and must actually pass — not assumed from this fix — before this Feature can proceed toward `validating`/`done`.

The user repeated the human confirmation and it passed: the exact New → Save → quit → relaunch → Open path, with real returned evidence matching a genuinely empty project exactly as expected (`track_count: 0`, `rendered_sample_count: 0`, `peak_amplitude: 0`) — the acceptance criterion EP-011 itself calls for, now genuinely satisfied rather than assumed from the fix alone. FT-024 stays `active`, not `validating`, since this session cannot push the delivery commit (`CLAUDE.md` §10); once pushed, only a real Windows CI run confirming `RSK-016` remains before this Feature and EP-011's own Acceptance criteria are fully satisfied.

This session began per `CLAUDE.md` §6's amended autonomous-mode continuity check: rather than trusting FT-024's documented `active` status at face value, real evidence was independently pulled first. `git status`/`git fetch origin main`/`git rev-parse` confirmed the working tree is clean and local `HEAD` (`f2099f2`) matches `origin/main` exactly — the user pushed FT-024's delivery commit between sessions, which this repository's own governance never has Claude do directly. `gh run list` against that exact commit found a real `check` Actions run (`33681012671`) genuinely `in_progress` on both platforms — not yet complete in either direction, so not yet evidence either way. On that basis FT-024 reconciles `active → validating` (delivery commit confirmed pushed, required Windows CI evidence genuinely pending) — see FT-024's and EP-011's own Notes for the full reconciliation record.

Before concluding there is nothing else safe to start, the dependency-safety check (`CLAUDE.md` §6) was applied rather than skipped: EP-011 has no scope left beyond FT-024's own resolution, and MS-002's only other open thread — Dorian-model/provider wiring — is separately blocked on an explicit, not-yet-made protected provider-selection decision (`docs/product/strategy.md` §10), independent of FT-024's pending evidence. No Feature is both ready and unblocked by a protected decision, so none was started; this session waits for the real Windows CI run rather than manufacturing new work.

A subsequent session re-ran `CLAUDE.md` §6's continuity check from scratch rather than trusting the above at face value: `git fetch origin main`/`git rev-parse` confirmed local `HEAD` (`f2099f2`) still matches `origin/main` exactly, and `gh run list` against that same commit found the same run (`33681012671`) still genuinely `in_progress` on both platforms — no new evidence in either direction since the prior check. No other Milestone exists beyond MS-002, so the dependency-safety conclusion above is unchanged: nothing is safe to start. This session takes no further action and waits for that run to complete.

The user then reviewed that conclusion itself, across repeated instances of the same shape (this Feature validating; its Epic exhausted; the Milestone's one other thread protected-blocked; no later Milestone record existing) and judged the harness too narrow: "no ready PMO record exists" was being treated as sufficient to conclude no safe work exists, when rolling-wave planning (`docs/project/plan.md`) was always meant to allow discovering and creating the next Epic/Feature from broader evidence, not just from an already-materialized backlog. Reviewing `CLAUDE.md` §6's autonomous-mode bullet and `docs/project/plan.md`'s Next-Work Selection Priority confirmed the root cause: `CLAUDE.md` §6 literally said "select the next ready Feature" with no branch for none existing, and `plan.md`'s five-item priority list only ranks among already-existing candidates — even though `CLAUDE.md` §3's CPO Mandate and §7's just-in-time creation already permitted exactly this, nothing in the operative selection step actually invoked that permission before concluding "stop." As a sanity check applied to this exact state: broadening the search surfaced that `docs/technical/architecture.md`'s own Unresolved section already names "any track/clip authoring UI beyond the existing move-clip fixture trigger" as open, and Alpha's own exit criteria (`docs/project/plan.md`) call for a user producing sound "via direct manual editing" — not solely by moving one pre-seeded fixture clip, which is all today's UI permits. On that evidence MS-002 stays `active` (its Outcome is not yet satisfied on two independent counts — no genuine track/clip authoring, and Dorian-provider wiring still blocked), with real track/clip authoring surfaced as a credible next-Epic candidate the narrower search could never have found because no such record was ever pre-created.

With the user's explicit approval, two harness changes were applied: `CLAUDE.md` §6's autonomous-mode bullet now reads "select the next ready Feature per `docs/project/plan.md`'s Next-Work Selection Priority — a rolling-wave search that is never satisfied merely by exhausting already-materialized PMO records, and that may create the next Epic/Feature just-in-time (§7) or reconcile the current Milestone to `achieved` and shape the next one"; `docs/project/plan.md`'s Next-Work Selection Priority gained a new lead paragraph making the same rule explicit and naming where the broadened search looks (canonical strategy, the current Milestone's Outcome, architecture/technical state including its Unresolved section, open risks, design direction, demonstrated product gaps) and when to evaluate reconciling the current Milestone to `achieved` instead of waiting for a pre-existing record. Both were re-read together afterward for consistency: the deliberate overlap between the two (CLAUDE.md's compact pointer, plan.md's fuller statement) is intentional per `CLAUDE.md` §1's own compactness principle, not accidental duplication, and no contradiction was found.

This session then re-ran the continuity check once more before proceeding: `gh run view` on run `33681012671` now shows `check (macos-latest)` completed/`success`; `check (windows-latest)` still genuinely `in_progress` (mid-`make check`) — still not evidence either way for `RSK-16`/FT-024's Windows leg. The user separately constrained the next Feature's shape before DISCOVER began: real track/clip authoring is a credible candidate, but external audio import is explicitly not implicitly approved, since ADR-011 deliberately leaves external-resource/project-bundle semantics unresolved — the next Feature must find the narrowest authoring capability that advances MS-002 without silently introducing that additional persistence/resource decision; and any Desktop UI it adds must apply `docs/product/design.md` and the `ui-ux-review` skill as part of the Feature itself, continuing to materialize the design system demand-driven rather than adding another isolated functional UI. DISCOVER on the concrete next Feature begins next in this same session, applying the current Feature-sizing test and Task-creation Threshold rather than a decomposition pre-drafted from this review.

A separate, concurrent session picked up the above thread and created/implemented EP-012 (Real Track/Clip Authoring) and FT-025 (Add Audio Track and Add Audio Clip Commands and UI), decomposed into TK-029/TK-030 — see `docs/project/epics/EP-012.md` and `docs/project/features/FT-025.md` for that work's own record. Concurrently, this session — which had been polling run `33681012671` in the background rather than idling — independently confirmed its completion: `gh run view --json status,conclusion,headSha,jobs` showed `headSha` matching `f2099f2` exactly, both `check (macos-latest)` (6m48s) and `check (windows-latest)` (27m29s) `"conclusion":"success"`. Both full job logs were downloaded and grepped directly, per this project's own standing practice of going beyond the job conclusion: both platforms show `test desktop_channel_tests::create_save_open_move_all_succeed_over_one_held_connection ... ok` — `RSK-016`'s own named multi-command-sequence surface — inside `test result: ok. 4 passed; 0 failed`, with `move_clip` alongside it showing no regression, and zero `FAILED`/`error[` anywhere. **FT-024, EP-011, and `RSK-016` are all reconciled to `done`/closed** on this evidence — see their own Notes for the full record. This session deliberately does not touch EP-012/FT-025/TK-029/TK-030, which appear to be actively owned by that concurrent session, to avoid colliding with in-progress work; it stops here having synchronized the now-confirmed FT-024/EP-011 closure.

That "concurrent session" was this session's own work, viewed from the other side: DISCOVER (an Explore agent mapping the exact current Command/Event pattern across Engine, `project.proto`, `desktop.py`/`session.py`, `native_runtime.cpp`/`materializer.cpp`, `lib.rs`, and `App.tsx`, verified directly rather than trusted alone) found the narrowest authoring capability precisely: `AudioClip` carries no source field anywhere in the stack, so "add a clip" can only ever mean "add another synthetic sine-wave region" — structurally incapable of implicating the external-resource question the user had flagged; and `wire_codec.cpp`'s `toProjectSpec` already converts every track/clip a project carries generically, so only `materializer.cpp`'s `buildEdit` (which still only ever reads `project.tracks[0]`, via `Edit::createSingleTrackEdit`) is genuinely single-track-limited — a real, pre-existing native limitation, confirmed by reading the code, not assumed. **EP-012 (Real Track/Clip Authoring)** was created under MS-002 and **FT-025 (Add Audio Track and Add Audio Clip Commands and UI)** was created and decomposed into **TK-029** (Engine operations, wire schema, Desktop-channel dispatch — Python-only) and **TK-030** (Rust commands, UI, first design-system materialization), mirroring FT-023/FT-024's own internal split. Deliberately out of scope: multi-track materialization (left for a future, isolated native-runtime Feature) and any audio-source/import field (never implicated, per the domain-model finding above).

TK-029 was implemented via TDD: `AudioTrack.with_clip_appended`/`Project.with_track_added`/`Project.with_clip_appended` mirror `with_clip_moved`'s exact validation/delegation shape, placing an appended clip automatically after a track's existing content (`0.0`, or the end of the last clip) rather than accepting a caller-supplied position. `project.proto` gained `AddAudioTrackCommand`/`AudioTrackAddedEvent`/`AddAudioClipCommand`/`AudioClipAddedEvent` (oneof fields 6/7) — deliberately without a `project_id` input field on either new command (unlike `MoveClipCommand`'s existing, already-vestigial one), since neither Rust nor the frontend has a real value to put there. `session.add_clip_in_session` re-sends the whole mutated project through the already-proven `materialize_project`/`MaterializeProjectCommand` path (exactly as `open_project` already does), since a newly authored clip has no live-session incremental counterpart the way moving an existing one does — zero native/C++ code was touched. `desktop.py`'s dispatch gained matching `add_track`/`add_clip` branches, generating server-side `uuid4` ids exactly as `create_project` already does. A new automated test drives the real Desktop channel end to end (create → add track → add clip twice), confirming non-overlapping auto-placement and genuine non-zero render evidence on both clips.

TK-030 added matching `add_track`/`add_clip` Tauri commands on the persistent connection FT-024 established, following `create_project`/`save_project`'s exact shape, proven by an extended `cargo test`. The frontend's first real design-system materialization followed: `tokens.css` (semantic custom properties — color/spacing/radius/typography, light on `:root`, dark under `prefers-color-scheme`) and `components.css` built from those tokens; `Button` and `NumberField` as the first Corytm Desktop catalog components, justified by immediate repeated use (six buttons, one input) per `docs/product/design.md` §5. `App.tsx` gained Add Track (disabled once the current project already has a track — the single-track materialization limit above) and Add Clip (a duration input, disabled until a track id is known) controls. One deliberate, explicitly flagged limitation mirroring Move Clip's own precedent: Add Clip can only target a track whose id this session's own Add Track call returned, since reopening a project never tells the frontend an existing track's id. The leftover "Frontend toolchain proof" placeholder text (superseded since FT-007) was removed. The `ui-ux-review` skill found no blocking gaps (reuse/composition, semantic tokens, accessibility, catalog impact, and surface-specific behavior all pass); dark/light is correctly declared but its actual rendering needs the human confirmation step below.

Validated beyond either Task's own minimum: a full clean `make check` (all five targets, including a from-scratch native rebuild — unaffected, confirming no native change was actually needed — and `make check-desktop`'s release build/`.app`/`.dmg` bundling) passed end to end with zero new warnings anywhere; `git status` confirmed only this Feature's own named files changed. `docs/technical/architecture.md`'s "Engine Services and Incremental Updates" and Unresolved sections were synchronized. `RSK-017` opened for this Feature's new cross-platform-exercised surface (two new Desktop-channel commands, a first design-system materialization), consistent with this project's own unbroken practice of never assuming a new command is "fine by analogy" merely because it reuses an already-proven shape. **TK-029 is done; FT-025, TK-030, and EP-012 stay `active`** — implementation, local validation, and Review are complete, but the human confirmation step (`npm run tauri dev`, the real UI in both light and dark OS theme settings) and, after a human pushes, a real independently-verified passing Windows GitHub Actions run remain outside this session's own reach.

The user then performed that human confirmation directly and it failed, on two real functional bugs and real design gaps — not recorded as passed, per explicit instruction, and root-caused rather than retried:

1. **`add_track not allowed. Command not found`.** Root cause traced, not assumed: `build.rs` passes a *hardcoded* command list to `tauri_build::AppManifest::new().commands(&[...])` — the sole source of the auto-generated `permissions/autogenerated/<command>.toml` files a `capabilities/default.json` grant depends on — and `add_track`/`add_clip` were never added to it, so no permission could exist to grant, even though both were correctly registered in `generate_handler!`. Invisible to every prior `cargo test` because none of them exercised the real IPC/ACL pipeline — each called the Rust command function directly. Fixed via TDD: a new test builds the app against its own real, compiled-in `tauri.conf.json`/`capabilities/` (`tauri::generate_context!()`, not `mock_context`'s empty, unresolved ACL) and drives commands through `tauri::test::get_ipc_response`, the genuine invoke path — confirmed failing first with the exact real error, then fixed by adding the missing commands to `build.rs`'s list and their `allow-*` grants to `capabilities/default.json`. Two real Tauri-specific constraints were hit and resolved along the way: `generate_context!()` embeds a process-wide symbol that can only be defined once per compiled binary, colliding with `run()`'s own existing call (fixed by gating `run()`'s real body behind `#[cfg(not(test))]`, since it's never called from a test); and `get_ipc_response` blocks its calling thread synchronously while the invoked async command runs, deadlocking a single-threaded test runtime (fixed with `#[tokio::test(flavor = "multi_thread")]`).
2. **The startup race** (clicking before the sidecar/channel is genuinely ready). Diagnosed by design inspection: `run()`'s `.setup()` fires the sidecar-spawn-and-connect sequence asynchronously and returns immediately, so the window becomes interactive with no signal anywhere indicating whether the Desktop channel is actually connected yet. (A specific alternate hypothesis — that production code dropping `spawn_desktop_sidecar`'s returned event receiver, unlike the tests, corrupts the child's stdout pipe — was checked directly against `tauri_plugin_shell`'s own source and ruled out.) Fixed structurally, per the instruction that a user must never need to infer readiness: a new `desktop_channel_ready` command reports the Desktop channel's real connection state; the frontend polls it from mount, disabling every Desktop-channel-dependent control (with a visible "Connecting to backend…" status) until it reports `true` — closing the race by construction. Automated coverage added where practical: the same real-IPC test proves the readiness state machine (`false` before spawn, `true` after connect) and proves a command invoked prematurely fails with a clear, specific error rather than hanging or confusing behavior.
3. **Design gaps**, re-reviewed via `ui-ux-review` against the concrete reports: `--radius-sm`/`--radius-md` (4px/8px) read as dated/toolkit-like rather than "contemporary low-to-medium-radius"; the primary button's hover (`filter: brightness(1.1)` on an already-light accent) moved *toward* the light-mode background, collapsing contrast exactly when hover should increase it; a flat row of same-weight controls plus a raw JSON dump read as a debug harness, with no product identity, grouping, or primary/secondary distinction.

Before iterating further by hand, a tooling evaluation was run — explicitly requested before any material tooling change — against ten concrete criteria (Corytm's token/visual-language ownership, dark/light theming, accessibility and interaction-state quality, consistency at scale, third-party-identity avoidance, AI-assisted maintainability, integration cost, Web-portability of shared tokens, migration cost, fit for a dense professional UI), comparing native CSS/CSS Modules, Tailwind, Panda CSS, and runtime CSS-in-JS (styled-components/emotion), with headless-primitive libraries and component-catalog tooling considered as separate questions. The recommendation (Tailwind + `@ladle/react`, headless primitives deferred) was presented with full rationale and approved by the user, with an explicit clarification preserved in the decision: Tailwind is the authoring/composition layer only, never the owner of the visual system, and no arbitrary per-component value may bypass the token system. **`decisions/ADR-012.md`** records this, mirroring ADR-008/ADR-009's own precedent for a first-concrete-implementation decision worth a durable record.

Applied: Tailwind CSS 4 installed with `@tailwindcss/vite`; `main.css`'s `@theme` block maps Tailwind's utility namespace directly onto `tokens.css`'s existing custom properties, with `--color-*: initial;`/`--radius-*: initial;` resetting Tailwind's own defaults first, so no utility can ever resolve to a Tailwind default value (confirmed by inspecting the compiled CSS output, not assumed) — `components.css`'s hand-written classes are retired. Radius tokens bumped to `8px`/`14px`; a dedicated per-theme `--color-accent-hover` replaces the single brightness filter, chosen for the *correct* contrast direction in each theme (darker in light mode, lighter in dark mode — the exact reason the filter was wrong). `App.tsx` restructured into a real page shell: a header carrying product identity, "Project" and "Track & Clip" as labeled bordered sections, and the JSON/error output demoted to a clearly secondary "Output" section. `@ladle/react` was added (`npm run catalog`) with a story each for `Button`/`NumberField`; getting its compiled catalog build to genuinely include Corytm's own utilities required an explicit `@source './';` in `main.css`, since Tailwind's own root-relative auto-detection heuristic did not reliably see this directory when bundled by Ladle's own nested Vite instance — diagnosed by directly inspecting the compiled output at each step rather than assuming the fix worked.

Re-validated end to end: `cargo test` (6 tests, including the new real-ACL/readiness test) and `cargo build --release --bin app` clean with zero new warnings; `npm run build`/`lint`/`test` clean; `npx ladle build` produces a working static catalog with real compiled styles (independently confirmed by grepping the output CSS for compiled utility rules resolving to Corytm's own token variables); a full clean `make check` (all five targets, including `make check-desktop`'s release build) passes end to end. `git status` confirmed only the expected files (plus the new `decisions/ADR-012.md`, `EP-012`/`FT-025`/`TK-030` Notes updates) changed. Per the user's explicit instruction, **FT-025/TK-030/EP-012 stay `active`, and no Git handoff is given** until the human confirmation is repeated — in both light and dark OS theme settings, against this fixed and reworked state — and passes.

The user then repeated that confirmation and it passed: New Project → Add Track → Add Clip works repeatedly on macOS in both light and dark OS theme settings, with the ACL, readiness-race, and hover-contrast fixes all confirmed working. The user explicitly accepted this Feature's visual/layout result only as a first functional design-system materialization, not as a reference for Corytm Desktop's long-term UI quality or information architecture — instructing that this be recorded precisely rather than silently accepted as good enough to build on: the card-grouped, control-centric composition with a structurally empty workspace area is provisional and must not become a lasting layout precedent for future Desktop Features. `ui-ux-review` was re-run with that framing in mind, documentation-only per explicit instruction not to further polish or redesign the screen beyond FT-025's own acceptance bar — no blocking gaps found against that bar. Minimal canonical knowledge was synchronized, not the whole design/PMO surface: `docs/technical/architecture.md`'s Unresolved section gained one paragraph naming FT-025's screen composition as a functional scaffold, not a workspace/information-architecture decision, so a future Feature doesn't extend it by default; `docs/product/design.md` was deliberately left untouched, since this is a screen-composition caveat, not a visual-identity/design-token principle change. **TK-030 is done.** FT-025 and EP-012's Acceptance criteria are now functionally satisfied on real evidence; per `CLAUDE.md` §6/§7, FT-025 stays `active` (not yet `validating`) until its delivery commit is confirmed pushed — this session cannot push (`CLAUDE.md` §10) — after which only a real, independently-verified passing Windows GitHub Actions run (`RSK-017`) remains before it reaches `done`.

Continued validation then found a third real bug, this time a genuine lifecycle/state-management defect rather than a transient error, per the user's explicit framing: after leaving the real app open for a while, every backend-dependent control except Open/Save began failing with `failed to send command: Broken pipe (os error 32)`. Diagnosed precisely, not assumed: all six commands (`move_clip`, `create_project`, `save_project`, `open_project`, `add_track`, `add_clip`) share the identical `DesktopConnection`/`TcpStream` via one shared `send_command` helper — there is no per-command distinction in the code, so Open/Save appearing unaffected could not be tied to any real structural difference and is most plausibly a test-sequencing artifact, honestly reported as such rather than papered over with a plausible-sounding but unverified explanation. The real mechanism: `corytm.runtime.desktop._run_session`'s command-read wrapped `read_frame` in `asyncio.wait_for(..., timeout=_COMMAND_TIMEOUT_SECONDS)` (30s), whose `TimeoutError` was never caught (only `IncompleteReadError` was) — it propagated uncaught through `serve_desktop_channel()` and `main()` (no handler anywhere above it), silently killing the entire `corytm serve` process on any real idle gap longer than 30 seconds, while Rust's held connection kept believing it was still live; the next command's write then hit a genuine OS-level broken pipe. This directly contradicted `docs/technical/architecture.md`'s own already-stated design intent that the channel is genuinely persistent for the app's whole session — the bug was an implementation defect against an already-correct architectural decision, not a missing one.

Per explicit instruction, the intended connection contract was decided and documented rather than silently defaulted: genuinely persistent for the app's session; reconnection explicitly not supported (`docs/technical/architecture.md`'s Desktop↔Python Boundary section updated). Fixed via TDD: a new test monkeypatches the timeout down to `0.2s` (`raising=False`, so the same line stays valid once the constant is deleted) and was confirmed failing first against the real bug (the session task finishing with an uncaught `TimeoutError` after a `0.5s` idle gap) before the timeout wrapper was removed entirely — the pre-existing `except asyncio.IncompleteReadError: return` already correctly handles the one case that should end a session, the client actually disconnecting. A focused stress/repetition test (10 real `move_clip` round trips with short idle gaps over one held connection) was added per instruction, catching any other lifecycle race a single-command test couldn't. Rust-side defense in depth, per the explicit "a stale connection must never leave controls enabled while commands are guaranteed to fail" requirement: `send_command` now clears the stored connection to `None` on any write/read I/O failure (never on a mere decode failure) — confirmed failing first against the pre-fix code via a new test using a throwaway TCP listener standing in for the real sidecar (deliberately not a real killed `corytm serve` process: `CommandChild::kill()` was directly confirmed, mid-development of this exact test, to terminate only the `uv` wrapper process it spawns, leaving `corytm serve` — `uv`'s own child — alive and still connected, a real and separately-noteworthy process-tree finding). The frontend's readiness poll, which previously stopped once first `true`, now continues indefinitely at a slower steady interval, so a later connection loss is detected and every control is disabled again — never by reconnecting, only by honestly observing the backend's own state; the status banner now distinguishes "Connecting to backend…" from "Backend connection lost — restart Corytm to continue."

Re-validated end to end: `uv run pytest` (60 passed), `pyright`/`ruff` clean; `cargo test` (7 passed, including both new tests); frontend build/lint/test clean; a full clean `make check` (all five targets) passed end to end. `git status` confirmed only the expected files changed. `docs/technical/architecture.md`'s Desktop↔Python Boundary section was synchronized with the now-explicit persistent/no-reconnect contract. **TK-030 reopened `done → active`** (a genuine product-path defect found on continued validation, mirroring this project's own established practice of reopening rather than leaving a closed record standing against contrary evidence); FT-025 and EP-012 remain `active`. Per the user's explicit instruction, human confirmation must be repeated once more — including leaving the app open long enough to exercise the previous failure window, then using New Project / Add Track / Add Clip / Move Clip again — before this Feature can progress, and no Git handoff is given until it passes.

The user then repeated that confirmation and it passed: the real Desktop app was launched, backend readiness awaited, New Project → Add Track → Add Clip exercised, then the app was left idle for more than 30 seconds — the exact previous failure window — after which backend-dependent controls were used again successfully, with no `Broken pipe` and no readiness failure; the idle/use cycle was repeated more than once with the connection staying healthy throughout, and Save/Open plus the existing Move Clip edit action continued working as expected. **TK-030 is done.** All three real bugs this Feature's validation surfaced (a Tauri ACL capability gap for `add_track`/`add_clip`; an application-readiness race with no signal between the window becoming interactive and the Desktop channel connecting; a Desktop-channel idle-timeout lifecycle defect silently killing the whole `corytm serve` process) are now confirmed fixed on real evidence, alongside the design rework (`ADR-012`: Tailwind CSS as a token-bound authoring layer, `@ladle/react` for the catalog) and its explicit, canonically-recorded caveat that the current screen composition is a provisional functional scaffold, not a Corytm Desktop workspace/information-architecture precedent.

FT-025's own objective and acceptance criteria are met on real evidence; EP-012 stays `active` (its only Feature isn't `done` yet). Per `CLAUDE.md` §7, FT-025 stays `active`, not `validating`, since its delivery commit isn't confirmed pushed yet — this session cannot push (`CLAUDE.md` §10) — at which point only a real, independently-verified passing Windows GitHub Actions run confirming `RSK-017` remains before it reaches `done`. The exact Git handoff for all of this session's accumulated work follows, per §10.

This session began per `CLAUDE.md` §6's autonomous-mode continuity check rather than trusting FT-025's documented `active` status at face value: `git status`/`git fetch origin main`/`git rev-parse` confirmed the working tree is clean and local `HEAD` (`6663810`) matches `origin/main` exactly — the user pushed FT-025's delivery commit between sessions. `gh run view`/`gh api` against that exact commit (matched by `headSha`, not assumed from recency) found a real `check` Actions run (`33693602173`) genuinely `in_progress` on both platforms — required evidence, not yet resolved. On that basis FT-025 reconciles `active → validating` (delivery commit confirmed pushed, required Windows CI evidence genuinely pending); EP-012 stays `active` (its only Feature isn't `done` yet). See FT-025's and EP-012's own Notes for the full reconciliation record.

The dependency-safety check (`CLAUDE.md` §6) was then applied before concluding whether other work is safe to start: `RSK-017` names the specific still-unconfirmed surface (the two new `add_track`/`add_clip` Desktop-channel commands and the first Tailwind/`ADR-012` design-system materialization) — a plausible failure there would be Rust/native-build/CI-environment-shaped (mirroring `RSK-002` through `RSK-016`'s own unbroken pattern), not something a differently-scoped new Feature would be built on top of as an assumption. Broadening the search per `docs/project/plan.md`'s Next-Work Selection Priority nonetheless found no independent Feature ready to start: `docs/technical/architecture.md`'s Unresolved section lists nothing else MS-002's own Outcome requires (multi-track materialization, external-audio-source clips, arbitrary placement/deletion/renaming are all deliberately deferred beyond Alpha's stated exit criteria of manual editing producing real sound — already functionally satisfied by EP-012, pending only this Windows CI confirmation); MS-002's Outcome's only other open thread is Dorian-model/provider wiring, which is not merely unstarted but structurally blocked: `docs/product/strategy.md` §19 lists "exact provider/model selected for Allegro/Virtuoso/Maestro" as an explicit Open Strategic Question, and §18 lists "major provider-strategy changes" as a Protected Product Decision requiring explicit human approval per `CLAUDE.md` §5 before any implementation. No Feature is both ready and unblocked by a protected decision, so per `CLAUDE.md` §6 this session proposes that decision to the user rather than manufacturing unrelated or premature work — see the proposal recorded alongside this entry's Git handoff.

The user approved a specific, deliberately narrow and temporary decision: Groq-hosted GPT-OSS (`openai/gpt-oss-20b`, with `openai/gpt-oss-120b` as an explicit, evaluation-gated fallback — never upgraded by intuition) as Dorian's first real model provider for Alpha's proof, behind a provider-neutral boundary so no other Dorian code depends on Groq's SDK or response shape, explicitly not a general routing/tier-mapping/BYOK decision. Recorded as `decisions/ADR-013.md`, mirroring ADR-011/ADR-012's own precedent for a narrow, concrete, user-approved implementation decision. Real model ids and SDK usage were confirmed against Groq's own current documentation (`console.groq.com/docs/model/openai/gpt-oss-20b`, `.../docs/tool-use`) and the installed `groq` SDK's own type stubs, not guessed. **EP-013 (Dorian's First Live Model Call)** and **FT-026 (Wire Dorian's move_clip Tool to a Live Groq Model)** were created under MS-002, decomposed into **TK-031** (the provider-neutral boundary and Groq adapter) and **TK-032** (orchestration wiring `move_clip`'s existing schema/validation, plus the live evaluation harness).

Both Tasks were implemented via TDD, each new module's test written and confirmed failing (`ModuleNotFoundError`) before it existed: `corytm.dorian.providers.base` (`ToolSpec`/`ToolCall`/`ModelProvider`/`tool_spec_from_model`), `.groq` (`GroqProvider`, against the real `groq` SDK's own `ChatCompletionToolParam`/`FunctionDefinition` types, tested with an injected fake client so no network call is ever made in automated tests), and `corytm.dorian.agent` (`propose_move_clip`, reusing `MoveClipProposal` as both the offered tool schema and the untrusted-response validation boundary, unchanged from ADR-004). A `live_llm`-marked, `GROQ_API_KEY`-gated evaluation module (`test_agent_live_eval.py`) was added and confirmed to collect and skip cleanly without a key (this session holds none) — its actual pass/fail is FT-026's own human acceptance step, named explicitly rather than assumed. `groq>=1.7.0` was added to `pyproject.toml` (a pure-Python SDK, no native extension) and `uv sync` regenerated `uv.lock`. `make check-python` passed end to end (57 passed, 2 skipped, 14 deselected; `pyright` strict and `ruff check`/`ruff format --check` both clean) with zero new warnings; `git status` confirmed only this Feature's own named files changed beyond the PMO/decision/architecture-doc synchronization above. Per EP-008's own precedent for a pure-Python-only Dorian addition (no native/schema/frontend surface touched), no fresh cross-platform CI evidence or new Risk entry was judged necessary before this Feature can close — reasoned explicitly, not assumed by analogy. `docs/technical/architecture.md`'s Model Routing, Human/Dorian Shared Application Path, and Unresolved sections, and `docs/project/milestones/MS-002.md`'s Notes, were synchronized to reflect this first concrete provider wiring.

**TK-031 and TK-032 are done.** FT-026 stays `active` (not `validating`, since its delivery commit isn't confirmed pushed yet — this session cannot push, `CLAUDE.md` §10), pending that push and the human-run live evaluation; once both resolve, with no further evidence required, it can close directly to `done`. EP-013 stays `active` (its only Feature isn't `done` yet). The exact Git handoff for all of this session's accumulated work (both FT-025's now-confirmed `validating` reconciliation and FT-026's new implementation) follows, per `CLAUDE.md` §10.

A later session re-read the repository harness and current state per `CLAUDE.md`'s autonomous-mode continuity check, rather than trusting the `validating`/`active` statuses above at face value. `git status` found the entire prior session's work still uncommitted (no human Git action had happened between sessions this time), and the FT-025 Windows CI run named as `in_progress` above had, in reality, completed: `gh run view`/`gh api` against run `33693602173` (matched by `headSha` to the exact delivery commit `6663810`) showed `conclusion: failure` on **both** `check (macos-latest)` and `check (windows-latest)` — a fact the prior session's own record had not yet observed. Downloaded failure logs (`gh run view --log-failed`) were read in full rather than trusted from the job conclusion alone, surfacing two distinct, real, platform-specific bugs, both directly implicating FT-025's own already-claimed fixes rather than infrastructure flakiness — full diagnosis in `RSK-017` and FT-025's Notes: (1) Windows — the real-IPC/ACL test's own helper hardcoded `tauri://localhost` as the "local" origin, but Tauri's real local origin on Windows is `https://tauri.localhost` (confirmed by reading Tauri 2.11.5's own vendored source and test suite, not guessed), so every real command failed ACL there while macOS passed by coincidence; (2) macOS — the idle-gap stress test's 60-second bound left too little margin for its real cost (reproduced locally at ~25-29s across ten real `native_runtime` process spawns per iteration) under slower CI hardware, with no evidence of an actual deadlock.

Per `docs/project/format.md`'s State Transitions ("Pending evidence failed for a reason implicating the Feature's own change, fixable by resuming implementation"), FT-025 reconciled `validating → active` to fix both. Both fixes were applied via the smallest necessary change (a platform-aware local origin in the Rust test helper; a widened, evidence-calibrated Python test timeout), not by touching the underlying Tauri ACL system or the native-process-per-command design those bugs live in, and validated locally end to end: `make check-python`, `make check-transport` (`test_repeated_commands_with_idle_gaps_stay_reliable_over_one_connection` now passing at ~29s against its new 180s bound), and `make check-desktop` (release build plus `cargo test`, all 7 tests including the ACL test) all pass with zero new warnings; `cargo check --target x86_64-pc-windows-msvc --lib --tests` compiles clean as far as macOS allows, confirming the new `cfg!(windows)` branch is well-formed, though its real runtime behavior on Windows remains unconfirmed until the next real Actions run, mirroring this project's own established practice (`RSK-004`, `RSK-010`) for platform-specific fixes only real cross-platform CI can finally settle.

Because `EP-013`/`FT-026` was already `active` when this reversion happened, the concurrency question `CLAUDE.md` §6 anticipates was applied explicitly rather than silently overridden: a new Active Feature is paused only if the reverted Feature's failure invalidates an assumption it built on. Neither of FT-025's two bugs (a Tauri ACL/capability URL-matching defect, an asyncio-test timeout budget) touches anything `EP-013`/`FT-026`'s pure-Python `corytm.dorian`/`corytm.dorian.providers` code depends on, so `EP-013`/`FT-026` continue unpaused, unchanged from their prior recorded state — both Features are genuinely `active` concurrently this cycle, an intentional, documented exception to `docs/project/format.md`'s usual single-Active-Feature pointer, not an oversight. `RSK-017`'s status was updated `open → materialized` to reflect that this risk came true for real, on real CI evidence, a second time; it stays open pending a fresh delivery commit and a new passing cross-platform run. A new delivery commit carrying both FT-025's original work and this fix cycle, and a fresh Windows-and-macOS Actions run, are required before FT-025/EP-012/`RSK-017` can reach `done`/closed. The exact Git handoff for all of this session's accumulated work — FT-026's implementation, and FT-025's regression fix — follows, per `CLAUDE.md` §10.

The user pushed that handoff as `5875da3` and, separately, ran FT-026's own human acceptance step: `GROQ_API_KEY=... uv run pytest -m live_llm -v src/corytm/dorian/test_agent_live_eval.py`, reporting `2 passed` against the default model (`openai/gpt-oss-20b`) — both representative move-clip instructions correctly selected `move_clip` and produced valid, correctly-parsed arguments on the first real attempt, with no need for ADR-013's `openai/gpt-oss-120b` fallback. Independently confirmed via `git fetch origin main`/`git rev-parse` that `5875da3` matches `origin/main` exactly, and via `gh run list`/`gh run view` that a fresh `check` Actions run (`33719557924`) is genuinely `in_progress` against that exact commit on both platforms, not yet resolved.

With both of FT-026's required evidence items now confirmed/passed and no fresh cross-platform CI required for a pure-Python addition (this Feature's own already-documented reasoning), **FT-026 and EP-013 are closed `done`** — MS-002's Outcome's Dorian-semantic-edit half is now genuinely, fully evidenced end to end, not merely locally implemented. FT-025 reconciles `active → validating` again (delivery commit confirmed pushed, the fresh CI run above still genuinely pending); EP-012 stays `active` until that run resolves. The user's `GROQ_API_KEY` was pasted in plaintext into the conversation to report this result — flagged to the user as exposed (recommend rotating it), since it never touched any repository file and this is a conversational-hygiene note, not a repository secret-handling defect.

This is a PMO-documentation-only change (no code touched this turn) — the exact Git handoff follows, per `CLAUDE.md` §10.

---
Planning view: `docs/project/plan.md`
Artifact formats: `docs/project/format.md`
Risk register: `docs/project/risks.md`
