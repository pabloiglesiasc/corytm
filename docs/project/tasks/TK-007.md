# TK-007 — Correct Stale "Current State" Claim in Memory README

Status: done
Feature: FT-006

## Objective

Fix `.claude/memory/README.md`'s "Current state" section, which asserts "Zero memory exists... nothing has happened yet that would produce a genuine, evidence-anchored observation," even though three real, evidence-anchored memory entries already exist (`cmake-windows-generator.md`, `github-actions-windows-runner.md`, `tracktion-engine-cmake-integration.md`), created during TK-002/TK-006/TK-005 respectively and cited from each originating Task's Knowledge impact field. `CLAUDE.md` §4 requires docs to reflect current reality, and the memory mechanism's own governing doc is currently wrong about its own state.

## Scope / Out of scope

In scope: rewriting only the "Current state" section of `.claude/memory/README.md` so it accurately reflects that memory entries exist, without duplicating their substantive content.

Out of scope: creating, editing, or promoting any of the three existing memory entries themselves; any other section of `memory/README.md`; any other PMO, spec, decision, or skill file; any product/domain code or configuration.

## Acceptance criteria

`.claude/memory/README.md`'s "Current state" section no longer claims zero memory exists, and accurately names the existing entries as evidence without duplicating their content. The rest of the file (mechanism description, governance, promotion rules) is unchanged. `make check` passes against the resulting repository state.

## Dependencies

None.

## References

`.claude/memory/README.md` (the file being corrected); `docs/project/tasks/TK-002.md`, `docs/project/tasks/TK-005.md`, `docs/project/tasks/TK-006.md` (each Task's Knowledge impact field — the origin of the three existing memory entries).

## Notes

This Task is itself the concrete exercise FT-006/EP-003 require: a real documentation-accuracy defect, discovered during this session's own DISCOVER step by cross-referencing `memory/README.md` against the actual contents of `.claude/memory/`, not a contrived example invented to exercise the process. Pure-docs change: TDD is skipped per `CLAUDE.md` §7.
