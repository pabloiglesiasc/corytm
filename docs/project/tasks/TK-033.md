# TK-033 — Canonical Testing & Validation Policy Doc and CLAUDE.md Pointer

Status: done
Feature: FT-028

## Objective

Write `docs/technical/testing.md` as Corytm's canonical Testing & Validation Policy — the four-tier validation model, the per-subsystem invalidation input-set table (including cross-subsystem edges), and the clean-build trigger list, all as decided in the user-approved testing-architecture review — and add a single cross-reference sentence from `CLAUDE.md` §7's Validate bullet, with no other rule change.

## Scope / Out of scope

In scope: `docs/technical/testing.md` (new file); one sentence added to `CLAUDE.md` §7's existing Validate bullet.

Out of scope: any other `CLAUDE.md` wording change; any Make/tooling/test-layout change (TK-034/TK-035); the skill (TK-036, which depends on this Task's finished content).

## Acceptance criteria

- `docs/technical/testing.md` states, for Corytm's five real subsystems (`check-python`, `check-native`, `check-desktop`, `check-transport`, `check-frontend`): a four-tier model (inner loop → affected-subsystem → Feature-close → CI/external/manual) naming what to run at each tier; an invalidation input-set table naming exactly which files invalidate each subsystem's last-recorded pass, including that `check-desktop`/`check-transport` are also invalidated by `check-native`'s own inputs; a clean-build trigger list (native `CMakeLists.txt`, `build.rs` logic changes, package-lock changes, local toolchain changes) distinguished from changes that do NOT require a clean rebuild because existing native dependency tracking already covers them (schema changes, `Cargo.lock`/`pyproject.toml` dependency bumps).
- The doc explicitly preserves every existing evidence guarantee named in the approved review (real cross-platform CI, real IPC/ACL/process/native/device tests, human-only manual evidence, `live_llm` live-provider evals) as never locally substitutable, and explicitly rejects an automated test-result fingerprint/cache ledger, giving the git-diff-based rationale from the review instead.
- `CLAUDE.md` §7's Validate bullet gains exactly one sentence pointing to `docs/technical/testing.md`; no other line in `CLAUDE.md` changes.

## References

The user-approved testing-architecture-and-policy review conducted earlier this session (measured local timings, command-graph tracing, duplication findings, tiered-policy design, and the addendum on local cache/artifact reuse and evidence-caching) is this Task's primary source — not a separate repository artifact, but this conversation's own analysis, now being formalized into a canonical doc per the user's explicit approval.

## Dependencies

None.

## Notes

`docs/technical/testing.md` written per the acceptance criteria: five-subsystem table, four-tier model, the exact git-diff-based invalidation rule with its input-set table (including the `check-native`→`check-desktop`/`check-transport` cross-subsystem edges), the clean-build trigger table, the local artifact/cache-reuse guidance (sccache's local gap named, not yet fixed — TK-034), an explicit "why no automated test-result cache" section carrying the review's own rejection rationale, and a closing section stating exactly what the policy does not change (Tier 4 evidence, `live_llm` exclusion, `make check`'s own meaning). `CLAUDE.md` §7's Validate bullet gained exactly one pointer sentence; `git diff CLAUDE.md` confirms no other line changed. Done — no code, no tests affected by this Task.
