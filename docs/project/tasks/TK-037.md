# TK-037 — CLAUDE.md/format.md Constitutional Consolidation

Status: done
Feature: FT-029

## Objective

Edit `CLAUDE.md` §6 in exactly the two places the harness-architecture review identified as procedural elaboration rather than constitutional rule, and synchronize `format.md`'s two existing cross-references to the relocated procedure — with every other line of both files verified unchanged.

## Scope / Out of scope

In scope: `CLAUDE.md` §6 (the dependency-safety-check paragraph; the "autonomous" interaction-mode bullet); `docs/project/format.md` (its two "(`CLAUDE.md` §6)" cross-references to the dependency-safety check).

Out of scope: any other `CLAUDE.md` section or line; any `format.md` content beyond the two named cross-references; the skills themselves (TK-038, which depends on this Task's finished wording).

## Acceptance criteria

- The dependency-safety-check paragraph in `CLAUDE.md` §6 is replaced by a compact statement of the rule itself (implementation dependency or subsystem proximity alone never blocks a new Active Feature; only a plausible invalidation of an already-established assumption does) plus a pointer to the `next-work-selection` skill for the full procedure, and a pointer to `docs/project/format.md`'s State Transitions for what happens to a Validating Feature's own state on evidence failure (already canonical there, not restated).
- The "autonomous" bullet inside §6's "Three interaction modes" paragraph is simplified to: apply `state-reconciliation` first; if it confirms the existing Active Feature has safe, executable work remaining, continue it directly (the fast path, stated explicitly); otherwise apply `next-work-selection` to choose the next Feature; the Protected Decision gate and the execute/close/recommend/stop flow remain stated inline (constitutional, not procedural).
- `git diff CLAUDE.md` shows changes confined to these two passages; every other section (§1–§5, §7–§11, and the rest of §6) is byte-identical.
- `format.md`'s two "(`CLAUDE.md` §6)" references to the dependency-safety check become "(`next-work-selection` skill)"; `git diff docs/project/format.md` shows no other change.

## References

The harness-architecture review conducted earlier this session (this conversation's own analysis, not a separate artifact); `CLAUDE.md` §5 (modifying `CLAUDE.md` always requires explicit user approval — already given for this specific extraction in the user's own approval message).

## Dependencies

None.

## Notes

Both `git diff` checks confirmed clean before closing: `CLAUDE.md`'s diff is confined to exactly the two named passages (the dependency-safety-check paragraph and the "autonomous" bullet), `format.md`'s to exactly its two "(`CLAUDE.md` §6)"→"(`next-work-selection` skill)" cross-reference updates. One deliberate correction made mid-edit: the dependency-safety-check replacement initially claimed `format.md`'s State Transitions covers "the resulting effect on a concurrently Active Feature" when a Validating Feature's evidence fails — checked against `format.md`'s actual State Transitions table and found that table only covers the Validating Feature's *own* transitions (`validating → active`/`blocked`), not the cross-Feature pause-and-reconcile effect, which was never canonical anywhere. Corrected before proceeding: that specific guidance is scoped to `next-work-selection` (TK-038) instead of mis-attributed to `format.md`, keeping `format.md` itself untouched beyond its two named cross-references per this Task's own out-of-scope boundary.
