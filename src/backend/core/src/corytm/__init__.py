"""Corytm's Python application core.

Owns canonical musical/project state (`corytm.engine`) and its projection
into the Native Audio Runtime (`corytm.runtime`). See
`docs/technical/architecture.md` for the full system shape.
"""
