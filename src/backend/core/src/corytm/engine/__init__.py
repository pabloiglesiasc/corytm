"""Corytm Engine: canonical musical/project domain models.

Owns the source-of-truth representation of a Corytm project. Runtime
projections (`corytm.runtime`) and the Native Audio Runtime derive from
this state; they never originate it.
"""
